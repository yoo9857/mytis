# 작업지시서 — 티스토리 자동 발행 시스템

인수인계용 문서입니다. **먼저 이 문서를 끝까지 읽고, 그다음 `README.md`를 보세요.**
README는 "쓰는 법", 이 문서는 "왜 이렇게 만들었고 어디가 위험한지"를 다룹니다.

작성 기준일: **2026-08-01** (§1~6 은 2026-07-27 기준, 이후 세션 기록은 §7-1~7-4)

---

## 1. 이 프로젝트가 하는 일

주제 한 줄이나 뉴스 기사 URL을 주면, 글을 쓰고 이미지를 만들어 **티스토리에 발행까지** 끝냅니다.

```
주제/기사URL
  → codex CLI 가 웹 검색하며 집필 (SEO/GEO 구조로)
  → 유튜브에서 관련 공식 영상 찾아 임베드
  → 라이선스 프리 사진 확보 → 카드/사진 렌더링
  → Chrome 자동화로 로그인·이미지 업로드·본문 입력·카테고리·태그·발행
```

**티스토리 Open API는 2024년부터 신규 키 발급이 막혔습니다.** 그래서 브라우저 자동화가
유일한 방법입니다. 다른 우회로(이메일 발행, 모바일 앱 API 리버싱)는 폐지됐거나 약관 위반입니다.

---

## 2. 새 컴퓨터에서 세팅하기

### 2-1. 필수 준비물

| 항목 | 확인 |
|---|---|
| Node.js **22 이상** | `node --version` — 유튜브 기능이 JS 런타임으로 Node 22+ 를 씁니다 |
| Google Chrome | 설치되어 있으면 됨 (Playwright가 자동으로 찾음) |
| codex CLI + 로그인 | `codex --version`, `codex exec "hi"` 가 동작해야 함 |

### 2-1-1. 유튜브 기능을 쓸 때만 필요한 것

기사 기반 글쓰기는 아래 없이도 전부 동작합니다. **유튜브 영상을 소재로 쓰거나
장면을 캡처할 때만** 필요합니다.

```powershell
pip install -r requirements.txt
```

| 항목 | 왜 필요한가 | 없으면 |
|---|---|---|
| `yt-dlp[default]` | 자막·영상 접근 | 자막을 못 읽어 영상 소재 모드가 꺼짐 |
| **`[default]` 부분** | 유튜브 서명 검증용 EJS 스크립트 | **403 Forbidden** |
| **Node 22+ JS 런타임** | 같은 서명 검증. `--js-runtimes node` 로 지정 | ANDROID 클라이언트로 폴백 → **403** |
| `opencv-python-headless` 4.x | 얼굴이 잘 나온 프레임·대표 이미지 선별 | 선별 없이 첫 후보를 씀 |
| **ffmpeg + ffprobe** | 프레임 추출. **ffmpeg 만 있으면 안 되고 ffprobe 도 있어야 함** | 캡처 실패 |

**ffmpeg 설치** — `.tmp/ffmpeg/` 는 `.gitignore` 대상이라 클론해도 따라오지 않습니다.

```powershell
# 방법 1: winget (있으면 가장 간단)
winget install Gyan.FFmpeg

# 방법 2: 직접 받아서 프로젝트 안에 두기 (winget 이 없을 때)
$dst = "C:\mytis\.tmp\ffmpeg"
New-Item -ItemType Directory -Force $dst | Out-Null
Invoke-WebRequest "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip" -OutFile "$dst\ff.zip"
Expand-Archive "$dst\ff.zip" -DestinationPath $dst -Force
```

`src/ytShot.js` 의 `findFfmpegDir()` 이 `.tmp/ffmpeg/` 아래를 훑어 찾고,
없으면 PATH 의 `ffmpeg` 를 씁니다. 환경변수 `FFMPEG_DIR` 로 직접 지정해도 됩니다.

`npm run doctor` 가 이 항목들을 점검합니다.

codex CLI가 없으면: `npm i -g @openai/codex` 후 `codex` 실행해 로그인.

### 2-2. 설치

```powershell
git clone https://github.com/yoo9857/mytis.git
cd mytis
npm install          # playwright chromium 도 함께 받습니다
```

### 2-3. 계정 설정 — **여기가 가장 중요합니다**

```powershell
Copy-Item .env.example .env
notepad .env
```

`.env`에 채울 값:

```ini
KAKAO_ID=카카오계정
KAKAO_PW=비밀번호
TISTORY_BLOG=            # 비워두세요. 아래 login 이 자동으로 찾아 넣습니다
TISTORY_CATEGORY=방송     # 발행할 카테고리 이름
```

> **`.env`는 절대 커밋하지 마세요.** `.gitignore`에 들어 있지만, 실수로 `-f`로 강제 추가하지 마세요.
> `profile/` 폴더에는 로그인 쿠키가 들어갑니다. 이것도 절대 공유·커밋 금지입니다.

### 2-4. 최초 로그인 (컴퓨터마다 1회 필요)

```powershell
npm run login
```

Chrome이 열리고 카카오 로그인이 자동 진행됩니다.
**카카오가 2단계 인증이나 새 기기 등록을 요구하면 그 창에서 직접 통과시켜야 합니다** (최대 3분 대기).
새 컴퓨터·새 IP에서는 요구할 가능성이 높습니다. 이건 자동화로 못 뚫습니다.

성공하면 블로그 주소를 자동으로 찾아 `.env`의 `TISTORY_BLOG`에 써 넣습니다.

### 2-5. 점검

```powershell
npm run doctor     # 블로그·계정·세션 유효성
npm run verify     # 로그인 화면 셀렉터가 현재 티스토리와 맞는지 (계정 없이도 실행 가능)
```

`doctor`가 "로그인 세션 유효 — 무인 실행 준비 완료"를 찍으면 끝입니다.

### 2-6. 첫 발행은 반드시 비공개로

```powershell
npm run post -- "테스트 주제" --private --show
```

결과 확인 후 공개 발행하세요.

---

## 3. 반드시 알아야 할 함정 4가지

이 4개 때문에 며칠 날릴 수 있습니다. 전부 실제로 겪고 고친 것들입니다.

### ① 티스토리 세션 쿠키는 디스크에 안 남는다

티스토리 인증 쿠키(`__T_`, `__T_SECURE`)는 **만료 시각이 없는 세션 쿠키**입니다.
브라우저를 닫으면 사라져서, Chrome 프로필 디렉터리를 재사용해도 로그인이 풀립니다.

→ 해결: 로그인 성공 시 `ctx.storageState()`로 쿠키를 `profile/session.json`에 저장하고,
다음 실행에서 `ctx.addCookies()`로 재주입합니다. (`src/browser.js`의 `saveSession`/`restoreSession`)

**이 파일을 지우면 다시 로그인해야 합니다.**

### ② 발행 순서를 틀리면 설정이 날아간다

**카테고리·태그는 에디터 화면**에 있고, **글 주소·공개 범위·발행 버튼은 발행 레이어** 안에 있습니다.
발행 레이어를 먼저 열고 `#category-btn`을 누르면 **레이어가 닫혀버려서** 이후 공개설정과
발행 버튼을 못 찾습니다.

→ 반드시 이 순서: `제목 → 본문 → 카테고리 → 태그 → 완료(레이어 열기) → 글 주소 → 공개 범위 → 발행`

### ③ 본문은 위지윅 모델에 넣어야 저장된다

티스토리는 **기본모드(위지윅)의 내용을 기준으로 글을 저장**합니다.
HTML 모드 CodeMirror에 `setValue()`로 값만 넣고 **그대로 발행하면**, 위지윅에는 반영되지 않아
**본문이 통째로 사라지고 업로드해 둔 이미지만 남습니다.**

→ 해결: `tinymce.activeEditor.setContent(html)`로 위지윅에 직접 주입합니다.
(HTML 모드에 넣고 기본모드로 되돌리는 방법도 시도했지만 실제로는 반영되지 않았습니다.)

`src/tistory.js`의 `setBody`는 아직 이 순서로 동작합니다.

1. HTML 모드 `setValue()` → 기본모드 복귀 → `wysiwygTextLength()`로 검증
2. 200자 미만이면 → 기본모드에서 `tinymce.activeEditor.setContent(html)` 폴백 → 재검증
3. 둘 다 실패하면 스크린샷을 남기고 예외

**2026-07-27 실측: 1번 경로는 본문 0자를 반환하며 매번 실패하고, 2번 폴백이 9,378자를 채웠습니다.**
즉 실제로 글을 살리는 건 폴백입니다. 1번은 사실상 죽은 경로이고 약 6초를 낭비합니다.

> **그러니 검증 단계(`wysiwygTextLength` > 200)를 절대 지우지 마세요.**
> 이게 없으면 본문 0자인 빈 글이 그대로 발행됩니다.

### ④ 스킨 CSS가 제목 굵기를 지운다

스킨에 따라 본문 안의 `h2`가 `font-weight: 400`으로 렌더링됩니다.
`em` 단위로 크기를 주면 본문과 구분이 안 됩니다.

→ 해결: `src/html.js`의 `S` 객체에서 **px 단위 + font-weight 명시**로 못박았습니다.
현재 h2 = 28px/800, h3 = 19px/700, 본문 = 17px.

### ⑤ 카테고리를 비워 두면 직전 글의 카테고리를 물려받는다

티스토리에서 카테고리를 **지정하지 않는 것**은 "분류 없음"이 아니라
**"직전에 발행한 글의 카테고리를 그대로 쓴다"** 입니다.
로그에는 "카테고리 미지정"이라고만 찍혀서 눈치채기 어렵습니다.

실제로 2026-07-27 테스트에서 클래식 음악 글이 조용히 `방송잡탕`에 들어갔습니다.

→ 해결 (`src/tistory.js`의 `selectCategory`):
- 설정이 비어 있으면 **명시적으로 "카테고리 없음"을 고르고 경고**합니다
- `config.json`의 `blog.category`를 **`"auto"`** 로 두면 `src/category.js`가
  글의 제목·키워드·태그·소제목을 카테고리 이름·별칭과 대조해 점수로 고릅니다
- 확신이 약하거나 1·2위가 붙어 있으면 **고르지 않고** `blog.categoryFallback`으로 물러납니다.
  조용히 틀린 곳에 넣는 것보다 낫습니다

카테고리 이름이 영문이거나(`Unity`) 본문 용어와 다르면(`재태크` ↔ "재테크")
`blog.categoryAliases`에 낱말을 보태세요.

### ⑥ 발행되면 인라인 CSS의 콜론 뒤에 공백이 붙는다 — 스킨 다크모드가 통째로 죽는다

`src/html.js`는 `color:#555`처럼 **공백 없이** 내보내지만,
티스토리 에디터를 거쳐 저장되면 브라우저가 스타일을 재직렬화해
**`color: #555` (콜론 뒤 공백)** 로 바뀝니다.

`skine.md`는 인라인 스타일을 **문자열 부분일치**(`[style*="color:#555"]`)로 잡아
다크모드를 덮는데, 공백 때문에 **하나도 걸리지 않았습니다.**

> 2026-07-27 실측: 26종 중 **19종이 죽어 있었습니다.**
> 다크모드에서 '목차'·'이 글의 핵심'·표 헤더가 흰 박스로 남고,
> 참고자료·캡션 글씨가 진회색이라 안 보였습니다.

→ 해결: `skine.md`의 H 섹션에서 **공백 없는 형태와 있는 형태를 항상 함께** 적습니다.
**새 규칙을 추가할 때도 반드시 두 벌을 같이 쓰세요.** 한 벌만 쓰면 또 죽습니다.

### ⑦ `adsbygoogle.push({})` 는 넘긴 요소를 채우지 않는다

`push({})`는 인자와 무관하게 **문서 순서상 아직 안 채워진 첫 번째 `<ins>`** 를 채웁니다.
그래서 "화면에 들어온 광고만 push" 하는 지연 로딩을 순진하게 만들면 순서가 어긋납니다.

검색결과나 `#앵커`로 글 중간에 진입 → 3번 광고가 먼저 보임 → `push({})`가 **1번**을 채움
→ 3번은 영영 빈칸으로 남고 콘솔에
`All 'ins' elements in the DOM with class=adsbygoogle already have ads in them` 이 뜹니다.

→ 해결 (`skinecode.md`의 `ads()`): 광고를 모아 **문서 순서로 정렬한 뒤**,
i번째가 화면에 들어오면 아직 push 하지 않은 0..i번째를 **순서대로 한꺼번에** push 합니다.
push 횟수와 채워지는 `<ins>`가 항상 1:1로 맞습니다.

> 삽입 순서가 아니라 **문서 순서**로 정렬해야 합니다.
> 사이드바가 왼쪽인 레이아웃에서는 삽입 순서와 문서 순서가 달라질 수 있습니다.

또 하나: 사이드바처럼 **내부 스크롤이 생기는 컨테이너** 안의 광고는 페이지를
아무리 스크롤해도 뷰포트와 교차하지 않아 영영 안 채워집니다.
그래서 문서 끝 근처에 닿으면 남은 광고를 순서대로 밀어 넣는 안전장치를 뒀습니다.

### ⑦-2 티스토리는 `<iframe>` 의 style 속성을 지운다 — 유튜브 임베드가 4:3 이 된다

`src/html.js`의 `renderEmbed`는 래퍼 `<div>`에 `padding-bottom:56.25%`를 주고
안쪽 `<iframe>`을 `position:absolute; width:100%; height:100%`로 채우는 정석 방식입니다.

그런데 **티스토리 sanitizer 가 `<iframe>` 의 style 속성만 지웁니다.**
(`<div>` 의 인라인 style 은 그대로 남습니다)

> 2026-07-27 실측 — 발행된 글:
> - 래퍼 div : `padding-bottom:56.25%` 유지 → 780×439 (16:9) ✅
> - iframe : **inline style = null** → 300×225 (**4:3**), `position: static`
>
> 결과: 검은 16:9 박스 안에 작은 화면이 떠 있는 모양이 됩니다.

→ 해결: iframe 을 **인라인 style 로 채우려 하지 말고 스킨 CSS 로** 채웁니다.
`skine.md`의 `#article-view .sk-embed > iframe` 규칙이 그 일을 합니다.
`html.js`는 보조로 `width`/`height` **속성**도 함께 넣습니다(속성은 살아남습니다).

CSS 로 고치므로 **이미 발행된 글도 스킨만 올리면 함께 복구됩니다.**
(실측: 300×225 → 780×439 로 복구 확인)

### ⑦-3 위키미디어 인물 검색은 `nameEn` 이 있을 때만 하라

`src/photo.js` 는 예전에 `person.nameEn || person.nameKo` 로 검색했습니다.
그런데 프롬프트는 `nameEn` 을 **"위키백과·위키미디어 공용에 실제로 등재된 로마자 표기"** 로
정의하고, 확실하지 않으면 비우라고 지시합니다.
즉 **빈 `nameEn` 은 "위키미디어에 없는 사람"이라는 신호**입니다.

한글 이름으로 폴백하면 흔한 이름이 엉뚱한 문서에 걸립니다.

> 2026-07-27 실측 — 나는 솔로 글(출연자: 정숙·영호·순자):
> `nameEn` 이 전부 빈 문자열인데 "정숙" 으로 위키미디어를 검색해
> **전혀 상관없는 사람 사진과 한자 문서 이미지**가 대표·본문에 들어갔습니다.

→ 해결: `(e.nameEn || '').trim()` 이 있는 인물만 검색합니다.
일반인이 방송 활동명으로 나오는 글에서는 인물 사진을 아예 시도하지 않습니다.

### ⑦-4 인물 사진이 대표 이미지를 차지하면 글과 어긋난다

위키미디어 인물 검색은 `"이름 + concert/performance"` 로만 찾으므로
사실상 **공연 무대 사진**이 걸립니다. 그런데 대표 이미지에는 글의 헤드라인이 얹힙니다.

> 실측: 시구 기사에 **"메츠 시구, 8-3 승리"** 라는 글자와 **콘서트 무대 사진**이
> 겹쳐 나왔습니다. 연예 글이면 주제가 무엇이든 같은 콘서트 사진이 대표가 됐습니다.

→ 해결: 인물 사진은 텍스트가 없는 **본문 슬롯(1번부터)** 에만 넣습니다.
대표 이미지는 글이 지정한 `photoQuery`(장면 검색어)를 씁니다.
예전 동작이 필요하면 `images.personPhotoOnThumb: true`.

### ⑦-5 언론사 사진은 축소본이 걸린다 — 화질이 무너진다

기사 본문에 박힌 이미지는 목록용 축소본인 경우가 많습니다.

> 실측(서울En) — 같은 사진의 세 가지 형태:
> ```
> SSC_..._V.jpg.webp   660x503  123KB   ← 기사 본문에 박힌 것
> SSC_....jpg.webp     760x580  167KB   ← _V 를 떼면 더 큼
> SSC_....jpg          760x580  380KB   ← .webp 도 떼면 압축이 훨씬 덜 됨
> ```

→ 해결 두 가지를 **함께** 걸어야 합니다.
1. `src/photo.js` 의 `upgrade()` — `_V`/`.webp` 를 떼고 화질 좋은 순으로 시도.
   (한 장은 이 방법으로 **4000×2667 원본**까지 나왔습니다)
2. `src/images.js` 의 `clampToSource()` — **원본보다 크게 렌더링하지 않는다.**
   760px 사진을 1200px 카드로 늘리면 눈에 띄게 뭉개집니다. 15%까지만 허용합니다.

또 하나: 언론사는 같은 사진을 크기별 변형으로 내보내므로 **URL 전체로 중복을 판정하면
같은 컷이 두 번 실립니다.** `photoKey()` 가 확장자와 `_V`·`_L`·`-300x200` 같은
접미사를 떼고 비교합니다.

### ⑧ 스티키 헤더를 축소하려면 히스테리시스가 필요하다

`#header`는 `position: sticky`라 **문서 흐름에서 자리를 그대로 차지합니다.**
`.is-scrolled`가 붙으면 `h1` padding 과 `#gnb` height 가 줄어 헤더가 **약 32px 낮아지고**
(실측 145px → 113px), 그만큼 아래 콘텐츠가 위로 튑니다.

예전에는 임계값이 `y > 8` 하나뿐이었습니다. 맨 위에서 조금만 왔다갔다 해도
클래스가 on/off 를 반복하며 콘텐츠가 32px 씩 튀었고,
`transition: padding .25s, height .25s`가 그걸 애니메이션으로 늘려
**헤더가 흔들리는 것처럼** 보였습니다.

→ 해결: 켜는 지점(72px)과 끄는 지점(24px)을 벌려 뒀습니다.
그 사이에서는 아무 일도 일어나지 않아 경계에서 진동하지 않습니다.
**임계값을 하나로 되돌리지 마세요.**

### ⑨ 사이드바는 '전체를 한 덩어리로' 고정해야 한다

목차(`.sk-toc--rail`)만 `position: sticky` 였습니다.
sticky 요소는 화면에 붙어 있는 동안에도 **문서 흐름의 자리는 그대로**여서,
뒤따르는 형제(광고·최근글·카테고리)가 그 밑으로 스크롤해 들어가
**목차가 그것들을 통째로 덮었습니다.**

개별 요소를 각각 sticky 로 만들면 서로 겹칠 수밖에 없습니다.

→ 해결 (`skine.md`): 1024px 이상에서 **`#aside` 컨테이너 하나만 고정**하고,
안쪽(`.sk-toc--rail`, `.sk-adzone--side`)은 전부 `position: static` 으로 되돌립니다.
내용이 화면보다 길면 사이드바 안에서 스크롤됩니다.

헤더가 압축되면 높이가 달라지므로, JS 가 실제 높이를 `--sk-header-now` 에 넣어 주고
사이드바의 `top`·`max-height`가 그 값을 씁니다.

### ⑩ 본문 사진 비율을 원본 방향과 무관하게 고르면 얼굴이 잘린다

`images.js` 의 `pickAspect()` 는 예전에 `bodyAspects` 에서 **해시로 아무 비율이나**
골랐습니다. 세로 비율(`3:4`·`2:3`)이 가로 사진에 걸리면 `background-size: cover` 가
양옆을 잘라냅니다.

> 2026-07-28 실측 — 고준희 글 3번째 이미지:
> 544×339 가로 캡처 → **292×390 세로 프레임**. 얼굴이 왼쪽 끝에서 반쯤 잘리고
> 오른쪽 인물과 자막 글자가 사라졌습니다. 짧은 변 기준 축소 탓에 292px 로
> 작아지기까지 했습니다.

→ 해결: ① 원본과 **방향이 같은 후보만** 남기고 ② 크롭 손실이 20% 를 넘으면 버립니다.
남는 후보가 없으면 **원본 비율을 그대로** 씁니다. 연출 다양성보다 안 잘리는 쪽이 우선입니다.

검증(같은 코드, 원본 방향만 다름): 가로 650×364 → 628×419 가로 / 세로 580×869 → 666×999 세로.

### ⑪ SNS 임베드 — 조사 없이 손대면 전부 되돌아간다

전부 2026-07-28 에 직접 확인한 것들입니다. **원래 방식으로 되돌리지 마세요.**

| 항목 | 실측 |
|---|---|
| `publish.twitter.com/oembed` | 막힘 (X 에러 페이지) |
| `cdn.syndication.twimg.com/tweet-result` | **동작.** 단 `token` 이 필요한데 **트윗 ID 로 계산**한다 (react-tweet 와 동일 식). 모르고 아무 값이나 넣으면 404 라 "막혔다"고 오판하기 쉽다 |
| X 타임라인 스크래핑 | 동작하나 **연속 호출 시 429**. 게다가 **삭제된 트윗 ID 도 그대로 준다** (BTS 계정의 2020년 글이 전부 404). 검증 없이 임베드하면 빈 박스가 실린다 |
| 인스타 `/embed/` | **브라우저 UA 를 보내면** 600KB JS 앱 페이지가 온다. **UA 를 빼야** 파싱 가능한 80KB 마크업이 온다 |
| 인스타 없는 게시물 | 200 + `EmbedSidecar` 를 주고 `EmbedBrokenMedia` 를 함께 담는다. 종류만 보고 통과시키면 **깨진 회색 박스**가 실린다 |
| 인스타 공식 oEmbed | **동작.** 2026-06-15 부터 토큰·앱등록 불필요. 없는 글엔 400 + Media Not Found. 단 응답에 `author_name` 이 **없다** |
| 인스타 게시일 | **공개 경로가 하나도 없다.** `/embed/` 의 `Time` 값은 요청 시각이다. shortcode 디코딩은 샘플별 오차가 7년·14년으로 제각각이라 폐기했다 → codex 가 확인한 날짜만이 유일한 근거 |
| 계정명 규칙 추측 | **불가능.** 공식 계정에 소속사 이름이 붙는다 (아이브 → `@IVEstarship`, 효린 → `@officialhyolyn_`). 어떤 조합 규칙으로도 못 만든다 → codex 웹검색으로 찾는다 |
| 짧은 영문명 대조 | `IVE` 가 3글자라 "4자 이상" 가드에 걸려 **정작 맞는 계정을 놓쳤다**. 짧은 이름은 낱말 단위 정확일치로 통과시킨다 (`Drive` 는 여전히 탈락) |

**인스타의 구조적 한계**: 로그인 없이 계정의 최신 글 목록을 얻을 방법이 없습니다.
검색엔진 색인도 부실하고 스크래퍼를 차단합니다. codex 가 게시물 주소를 준
경우에만 동작하며, 이는 매번 되지는 않습니다. X 는 안정적입니다.

### ⑫ 영상 장면 캡처 — 실측으로만 알 수 있었던 것들 (2026-07-28)

`ytShot.js` 를 파이프라인에 붙이면서 겪은 것 전부입니다. **되돌리면 다시 깨집니다.**

**seek 방식 세 가지가 모두 실패했다.** 한 페이지를 열어 두고 이동하는 방식은 안 된다.

| 시도 | 결과 |
|---|---|
| `v.currentTime = t` (정지 상태) | 유튜브가 **새 구간을 아예 받아오지 않는다.** currentTime 만 목표값이 되고 readyState 1 · seeking true 로 굳는다 |
| `v.currentTime = t` + `play()` | 재생은 되지만 `buffered` 가 [0,37] 에서 늘지 않는다. MSE 라 플레이어가 버퍼를 직접 관리한다 |
| `#movie_player.seekTo(t, true)` | **플레이어가 리셋된다.** currentTime 0 · readyState 0 · buffered 빈 배열 |

→ 해결: 장면마다 **`?t=<초>s` 로 새로 연다.** 실측 161초 → 7초 만에 buffered [161,185].
가까운 장면은 한 번의 로드로 묶어 찍는다(버퍼가 약 25초를 덮는다).
**759초에 1/3 → 71초에 3/3** 으로 바뀌었다.

**광고 중에 찍으면 광고가 발행된다.** `<video>` 에 흐르는 것이 본편이 아니다.
`#movie_player.ad-showing` 을 보고 건너뛰거나 기다리고, **찍기 직전에 한 번 더 확인**한다
(중간광고는 불쑥 끼어든다). 45초 안에 안 끝나면 그 장면을 버린다.

**화질은 두 겹 문제였다.** 하나만 고치면 여전히 저화질이다.
- 유튜브는 플레이어 크기에 맞춰 화질을 고른다 → `setPlaybackQualityRange('hd1080')`
- `el.screenshot()` 은 **CSS 크기**로 찍는다 → 뷰포트 1920×1080 + 전체화면

> 실측(원본 1080p): 1280×900 뷰포트 → CSS 880×495 / 전체화면 → 1920×1080

**전체화면에서는 `.ytp-chrome-top` 이 안 지워진다.** 유튜브가 새 클래스 체계를 쓴다.
영상 제목이 대표 이미지에 그대로 찍혔다. `.ytp-overlays-container` 가 공통 부모다.

**`CascadeClassifier` 는 한글 경로를 못 읽는다.** 계정 이름이 한글이면 site-packages
경로가 통째로 한글이 되어 **조용히 빈 분류기**가 만들어지고 `!empty()` 로 터진다.
`cv2.imread` 와 같은 문제다 → ASCII 경로로 복사해 쓴다.
**opencv 5.x 는 `CascadeClassifier` 자체가 없다** — `requirements.txt` 가 `<5` 로 고정한다.

**대표 이미지를 시간순 첫 장면으로 고정하면 안 된다.** 그 장면에 얼굴이 없으면
그대로 실린다(실측: "빨간 바지 입고 등장" 컷이라 몸통만 나왔다).
codex 가 `isHook` 으로 감정이 터지는 장면을 지목하고, 그중 얼굴이 가장 크게
잡힌 것을 코드가 고른다.

**목록 카드는 정사각으로 자른다.** 16:9 대표 이미지의 좌우가 날아가
문구가 끊긴다("스펙 다 갖췄는데 0표" → "펙 다 갖췄는데").
비율은 유지하고 **글자만** 가운데 정사각(짧은 변의 90%) 안에 둔다.

---

## 4. 코드 구조

```
src/
  cli.js            명령 라우팅 (login/post/draft/publish/queue/news/probe/verify/doctor)
  run.js            생성→이미지→HTML→발행 파이프라인
  config.js         config.json + .env 병합

  mode.js           **글의 용도(주제/기사/영상)와 모드별 수행 단계** — 4-2 절 참고
  socialEmbed.js    공식 X·인스타 근황 게시물 임베드 (AI 탐색 + 코드 검증)
  schema/social...  공식 계정·게시물 구조화 출력 스키마

  category.js       글 내용에 맞는 카테고리 고르기 (blog.category = "auto")
  prompt.js         집필 지시문 (일반 글 / 뉴스 해설 / 후킹 규칙 / 이미지 브리프)
  schema/           codex 구조화 출력 스키마 (아티클·사진·뉴스피드)
  codexWriter.js    codex exec 실행 및 결과 정규화
  newsFeed.js       최신 기사 소재 탐색 (화제도 점수)
  fetchArticle.js   기사 본문 추출 (광고·관련기사 제거)

  youtube.js        유튜브에서 관련 공식 영상 검색 → 임베드
  photo.js          라이선스 프리 사진 확보 (위키미디어→Pexels→codex→Openverse)
  cardStyles.js     트렌디 연출 (neon/pop/zine/poster/clean)
  cardLayouts.js    사설 연출 (editorial/panel/spotlight/figure/band) + 렌더러
  images.js         카드·사진 PNG 렌더링

  html.js           아티클 JSON → 티스토리 HTML
  browser.js        Chrome persistent context + 세션 쿠키 저장/복원 + 대화상자 정책
  kakaoLogin.js     카카오 로그인 · 로그인 판정 · 블로그 자동 탐지
  tistory.js        에디터 조작 및 발행
  verifySelectors.js 로그인 화면 셀렉터 점검
  queue.js          주제·기사 큐
```

---

## 4-1. 자동 발행 알고리즘 (전 구간 한 장 요약)

`npm run post -- "<주제 | 기사URL | 유튜브URL>"` 하나로 아래가 끝까지 돕니다.
사람이 개입하는 지점은 **최초 1회 카카오 로그인**뿐입니다.

```
입력 판별
 ├ 유튜브 URL → ytClip.fetchClip: 자막만 받아온다 (--skip-download, 영상 파일은 절대 안 받음)
 │              자막이 없으면 일반 기사 모드로 강등
 ├ 기사 URL   → fetchArticle: 본문 추출 (광고·관련기사 제거)
 └ 그냥 주제  → 검색으로 집필

1단계 · 집필  (codex + 웹검색, 1~3분)
   prompt.js 지시문 + article.schema.json 구조화 출력
   교차검증 2~3매체 · 7섹션 · 표 1개 이상 · FAQ 5개 · 출처 배열
   → entities(인물), imageBriefs(이미지 지시), embeds(영상), urlSlug(영문)

2단계 · 공식 영상  (youtube.js)
   유튜브 검색 → 공식 채널 우선 → **제목 관련성 필터**
   관련 영상이 없으면 0개. 무관한 예능 영상을 넣느니 비워 둔다
   영상 소재 글(fromClip)이면 이미 장면 임베드가 있으므로 이 단계를 건너뛴다

3단계 · 공식 SNS 근황  (socialEmbed.js)  ← AI 가 찾고, 코드가 검증한다
   ① codex 웹검색으로 공식 X·인스타 계정과 최근 게시물(주소+게시일+사진여부)을 받는다
      계정만 찾고 게시물이 0건이면 한 번 더 물어본다 (편차가 커서)
   ② 코드가 전부 다시 확인한다 — codex 결과를 그대로 믿지 않는다
      · X   : tweet-result 로 생존·작성자·사진·게시일 확인
      · IG  : 공식 토큰리스 oEmbed 로 존재 확인 + /embed/ 로 작성자 확인
      · 공통: 프로필 이름이 인물명과 겹치는가 / 팬 계정 아닌가 /
              maxAgeDays 이내인가 / 사진이 있는가
   하나라도 못 넘으면 넣지 않는다. 틀린 계정보다 0개가 낫다

4단계 · 이미지  (photo.js → images.js)
   사진 확보 우선순위: 원문 기사 사진(useSourcePhoto) → 위키미디어 인물 →
                      Pexels/codex/Openverse 스톡
   프레임 결정: **원본 사진의 방향을 먼저 본다** (가로 원본에 세로 프레임 금지)
              크롭 손실 20% 초과 후보 탈락 → 남는 게 없으면 원본 비율 그대로
   크기 상한 : 원본보다 크게 그리지 않는다 (clampToSource, 15%까지만 허용)

5단계 · HTML  (html.js)
   목차·직답·핵심요약·표·FAQ·JSON-LD·출처·이미지출처
   임베드는 전부 **스크립트 없는 iframe** + width/height 속성 (티스토리가 style 을 지움)

6단계 · 발행  (browser.js → kakaoLogin.js → tistory.js)
   세션 복원 → 로그인 확인 → 이미지 업로드 → 제목 → 본문(위지윅 직접 주입) →
   카테고리 → 태그 → 발행레이어 → 글주소 → 공개범위 → 발행
   ※ 이 순서를 바꾸면 안 된다 (함정 ②)
```

**설계를 관통하는 원칙 하나** — 확실하지 않으면 넣지 않는다.
영상·SNS·인물사진 모두 검증을 통과한 것만 싣습니다. 조용히 틀린 것이
실리는 쪽이 비어 있는 쪽보다 훨씬 나쁘기 때문입니다.

---

## 4-2. 용도(모드)별로 무엇이 달라지는가

**같은 파이프라인이지만 입력에 따라 전혀 다른 글을 씁니다.** 모드는 `src/mode.js`
한 곳에서만 정하고, 각 단계는 `can(mode, '단계이름')` 으로 **묻기만** 합니다.

> **단계마다 조건문을 새로 세우지 마세요.** 예전에는 `isUrl(topic)`, `!clip`,
> `article.fromClip` 같은 판단이 4개 파일에 흩어져 있었고, 한 단계가 모드를
> 놓치면 조용히 다른 모드처럼 동작했습니다. 실제로 두 건의 사고가 났습니다(아래).

| | **주제** | **기사** | **영상** |
|---|---|---|---|
| 입력 | `"청년도약계좌 조건"` | 기사 URL | 유튜브 URL |
| 소재 확보 | 없음(검색만) | `fetchArticle` 본문 추출 | `ytClip` **자막만** (영상 파일 안 받음) |
| 집필 지시문 | `buildArticlePrompt` | `buildNewsPrompt` | `buildClipPrompt` |
| 글의 성격 | 정보 정리 | 교차검증 뉴스 해설 | **서사 에세이** (1인칭·시간순) |
| 섹션 제목 | 검색 질문형 | 검색 질문형 | **이야기의 마디** |
| 표 | O | O | **X** (흐름을 끊음) |
| 원문 사진 | X | O (`useSourcePhoto`) | X |
| 인용기사 사진 | X | O | **X** ← 사고 ① |
| 장면 캡처 | X | X | **O** (`ytShot`) |
| 유튜브 임베드 검색 | O | O | **X** (같은 영상 장면이 이미 있음) |
| 공식 SNS 근황 | O | O | **X** ← 사고 ② |
| 이미지 비율 | 연출용 혼합 | 연출용 혼합 | **원본 그대로** (자막 잘림 방지) |

### 모드가 섞여서 난 사고 두 건 (2026-07-28)

**① 영상 글에 무관한 사진이 대표로 들어갔다.**
'인용 기사에서 사진 더 긁어오기' 단계에 영상 글 예외가 없었습니다.
codex 가 배경조사로 인용한 기사(23기 여출연자 스펙)에서 사진을 가져와
**전혀 다른 영상의 썸네일**("아빠가 사주셨죠")이 대표 이미지가 됐고,
그 위에 "말의 순서가 남긴 간격" 이라는 글 헤드라인이 얹혔습니다.
→ `relatedArticlePhotos: false` (영상 모드)

**② 있을 리 없는 SNS 계정을 1분 동안 찾았다.**
나는솔로 출연자 '영숙·영철·영식·광수·옥순' 의 공식 계정을 검색했습니다.
방송용 이름을 쓰는 일반인이라 존재하지 않는데 codex 호출을 한 번 버렸습니다.
→ `socialEmbeds: false` (영상 모드)

### 모드별 기술 스택

| 모드 | 외부 의존 | 없으면 |
|---|---|---|
| 주제 | codex CLI | 동작 불가 |
| 기사 | codex CLI, Playwright(본문 추출) | 동작 불가 |
| 영상 | codex CLI + **yt-dlp[default]** + **Node 22+** + **Chrome** + **ffmpeg/ffprobe** + **opencv 4.x** | 아래 참고 |

영상 모드만 준비물이 많습니다. `npm run doctor` 가 점검합니다.

- `yt-dlp[default]` — 자막. `[default]` 를 빼면 **403**
- Node 22+ — 유튜브 서명 검증용 JS 런타임. 없으면 **403**
- **Chrome(설치본)** — 장면 캡처. Playwright 번들 Chromium 은 H.264 코덱이 없어 재생 못 함
- **opencv 4.x** — 얼굴이 잘 나온 프레임 선별. **5.x 는 `CascadeClassifier` 가 삭제됐으므로 금지** (`requirements.txt` 가 `<5` 로 고정)

### 모드별 예외 처리 — 무엇이 실패하면 어떻게 되는가

**공통 원칙**: 실패해도 글은 나간다. 다만 **틀린 것을 싣느니 비운다.**

| 실패 | 처리 |
|---|---|
| 유튜브 자막 없음 | **기사 모드로 강등** (`resolveMode`). 영상 글을 쓸 근거가 없음 |
| 장면 캡처 실패 | 그 장면만 건너뛰고 나머지로 진행. 전부 실패하면 스톡 사진 |
| **캡처 중 광고** | 건너뛰기 시도 → 안 되면 대기 → 45초 넘으면 **그 장면 포기**. 광고 화면을 싣지 않는다 |
| 촬영 직전 중간광고 | 그 후보를 버리고 다시 광고 처리 |
| 얼굴 선별 실패 | 첫 후보 사용 (경고만) |
| 유튜브 영상 검색 0건 | 임베드 없이 발행. 무관한 영상을 넣지 않는다 |
| SNS 계정 검증 실패 | 임베드 없이 발행. 틀린 계정보다 0개가 낫다 |
| codex 게시물 0건 | **1회 재시도** (편차가 커서) |
| X 타임라인 429 | 그 인물의 X 를 건너뜀. codex 가 준 주소가 있으면 애초에 타임라인을 안 씀 |
| 삭제된 트윗 | `tweet-result` 가 404 → 제외. **타임라인은 삭제된 ID 도 준다** |
| 인스타 게시일 불명 | **제외.** 몇 년 전 사진이 '최신 근황' 으로 실리는 것이 최악 |
| 사진 확보 실패 | 그라디언트 카드로 폴백 |
| 본문 위지윅 0자 | `tinymce.setContent` 폴백 → 그래도 실패하면 스크린샷 남기고 예외 (함정 ③) |
| 카테고리 미지정 | 명시적으로 "카테고리 없음" 선택 + 경고 (함정 ⑤) |

---

## 5. 검증된 티스토리 셀렉터 (2026-07-27 기준)

티스토리는 예고 없이 마크업을 바꿉니다. 깨지면 아래 표와 `npm run probe` 결과를 대조하세요.

| 대상 | 셀렉터 |
|---|---|
| 제목 | `#post-title-inp` |
| 에디터 모드 버튼 | `#editor-mode-layer-btn-open` |
| HTML / 기본모드 | `#editor-mode-html-tistory` / `#editor-mode-kakao-tistory` |
| 본문 iframe | `#editor-tistory_ifr` |
| 첨부 드롭다운 | `#mceu_0-open` (aria-label="첨부", `.mce-i-image` 아이콘) |
| 첨부 → 사진 | `.mce-tistory-attach-item:has-text("사진")` |
| 카테고리 버튼 | `#category-btn` |
| 카테고리 항목 | `.mce-menu-item:visible` (하위 카테고리는 앞에 `- ` 붙음) |
| 태그 | `#tagText` (입력 후 Enter) |
| 완료(레이어 열기) | `#publish-layer-btn` |
| 글 주소 | `#urlPublish` |
| 공개 / 보호 / 비공개 | `#open20` / `#open15` / `#open0` |
| 발행 | `#publish-btn` |

**대화상자 처리** (`src/browser.js`의 dialog 정책):
- `"이어서 작성하시겠습니까?"` (임시저장 복구) → **거절**
- `"작성 모드를 변경하시겠습니까?"` → **수락**
- 카카오 로그인 화면의 `취소` 버튼은 **절대 누르면 안 됨** (로그인이 취소됨)

---

## 6. 저작권 — 지켜야 하는 선

이 프로젝트에서 가장 신경 쓴 부분입니다. **임의로 완화하지 마세요.** 실제 금전적 리스크가 있습니다.

### 기사

- 기사 문장을 옮기지 않습니다. 사실만 취하고 표현은 전부 새로 씁니다
- 직접 인용은 큰따옴표로 한두 문장까지, 발언자를 밝혀서만
- 원문 기사를 `sources` 맨 앞에 강제로 넣습니다 (코드에서 자동 추가)

> 사실(수치·순위·일정)은 저작권 대상이 아니지만 **문장 표현은 보호받습니다.**

### 사진 — 여기가 제일 위험합니다

**언론사 보도사진, 팬이 찍은 직캠 사진, 유튜브·X 캡처는 쓰면 안 됩니다.**
크롭하거나 색 보정하거나 글자를 얹어도 2차적저작물이라 그대로 침해입니다.
국내 저작권 법무법인이 이미지 역검색으로 대량 적발해 건당 수십만 원을 청구합니다.
애드센스가 붙은 티스토리는 표적이 됩니다.

허용 소스만 씁니다:
- 위키미디어 공용 — **CC0 / PD / CC BY / CC BY-SA만**, NC·ND 제외
- Pexels · Unsplash · Pixabay — 상업 이용 무료
- codex가 URL을 줄 때는 `CODEX_ALLOWED_HOSTS` 화이트리스트로 도메인을 강제합니다

> **주의**: 위키미디어에도 **팬이 광고 사진을 "본인 저작물"로 올린 것**이 섞여 있습니다.
> 실제로 임영웅 검색 결과 2장이 전부 광고 스틸이었고 하나는 라이선스 표시가 허위였습니다.
> 그래서 `UNUSABLE_PATTERN`으로 광고·포스터·앨범커버로 보이는 항목을 걸러냅니다.
> **이 필터를 풀지 마세요.**

CC BY 계열은 저작자 표기가 의무라, 카드 우하단과 본문 하단 "이미지 출처"에 자동으로 남깁니다.

### 인물

- 보도된 사실과 해석을 분리하고 "○○ 보도에 따르면" 식으로 귀속
- 미확인 루머·사생활 추측·열애 단정 금지
- 판결 전 사안은 "혐의", "의혹"으로만

### 그래서 실제 장면은 어떻게 보여주나

**공식 영상 임베드**입니다. 임베드는 유튜브가 제공하는 기능이라 문제가 없고,
현장 영상을 그대로 보여줍니다. `src/youtube.js`가 유튜브 검색에서 직접 찾아옵니다.

- 인물 이름 + 사안 키워드가 제목에 **둘 다** 있어야 통과
- 관련 영상이 없으면 **아무것도 넣지 않습니다** (무관한 예능 영상 방지)

---

## 7. 현재 상태

### 동작 확인된 것

전 구간을 실제 블로그에 발행해 검증했습니다.

- 카카오 자동 로그인 + 세션 유지 (재시작 후에도 무인 실행 가능)
- 뉴스 소재 자동 수집 (화제도 점수 순, 차별화 각도 포함)
- 기사 본문 추출 → 교차 검증 집필 (7섹션 5,000자, 출처 5~6곳)
- 이미지 업로드 → 티스토리 매크로로 본문 사이사이 배치
- 카테고리·태그·영문 슬러그·공개 범위·발행
- 유튜브 공식 영상 검색 및 관련성 필터

### 미검증 / 미구현

| 항목 | 상태 |
|---|---|
| 예약 발행 (`publishMode: "reserve"`) | 코드만 있고 실제 검증 안 됨 |
| Windows 작업 스케줄러 정기 실행 | 스크립트는 있으나 실제 스케줄 실행 미검증 |
| 2단계 인증이 걸린 상태의 무인 실행 | 불가능 (설계상. 최초 1회 수동 로그인 필요) |
| X(트위터) 공식 임베드 | **구현·검증 완료** (2026-07-28). 발행글에 실제로 들어간 것 확인 |
| 인스타그램 공식 임베드 | 구현 완료. 동작 확인했으나 **매번 되지는 않음** — 함정 ⑪ 참고 |
| `src/ytShot.js` | **어디서도 import 되지 않는 죽은 코드.** 영상 경로는 `ytClip.js` 만 쓴다 |

---

## 7-1. 세션 인수 상태 (2026-07-28 기준)

이 절만 읽으면 어디까지 됐고 무엇이 남았는지 알 수 있습니다.

### 이번에 만든 것

| 파일 | 하는 일 |
|---|---|
| `src/mode.js` | 글의 용도(주제/기사/영상)와 모드별 수행 단계 — **4-2 절** |
| `src/buzz.js` | 방송 밖 반응(뉴스·커뮤니티)을 직접 수집. 기수 대조로 동명이인 차단 |
| `src/socialEmbed.js` | 공식 X·인스타 근황 임베드 (AI 탐색 + 코드 검증) — 함정 ⑪ |
| `src/ytShot.js` | 영상 장면 캡처. 죽은 코드였던 것을 연결 — 함정 ⑫ |
| `rethumb.mjs` | 대표 이미지 문구만 15초에 다시 그린다 |
| `.claude/skills/benchmark-post/` | 참고 글을 수치로 분석해 지시문에 반영. `references/learned.md` 에 누적 |

### 영상 글의 완성 기준 (발행해 본 결과 정착된 수치)

`npm run draft -- "<유튜브URL>"` 로 뽑고 아래를 확인한 뒤 발행합니다.

| 항목 | 기준 |
|---|---|
| 사진 | 본문 **100자당 1장** (참고 블로그 7편 실측치), 1920×1080 |
| 문단 | 평균 **60~80자**, 2문장 |
| 문체 | 경어체. 평어체 0 |
| 사진 배치 | **시간순** 역행 0, 문단 사이에 끼움 |
| 대표 이미지 | 감정이 터지는 클로즈업 + **궁금증 유발 문구** |
| 하단 영상 | 플레이어가 아니라 **링크** (오류 153 회피) |
| 금지 | 메타 서술("찾지 못했습니다", 조회수 나열), 표, 완충어 남용 |

### 기사 모드도 같은 기준으로 맞췄습니다 (2026-07-28 오후)

영상 모드를 다듬는 동안 기사 모드가 뒤처져 있었습니다. **리팩터링이 만든 버그 세 건**을
같은 날 잡았는데, 전부 *"판단을 한곳에 모았는데 값을 심거나 공유하는 걸 빼먹은"* 유형입니다.

| 버그 | 증상 | 원인 |
|---|---|---|
| `article.mode` 를 영상 글에만 심음 | 기사 글이 **원문 사진 3장을 버리고** 스톡 사진(금괴·열대공항)을 씀 | `photo.js` 가 `article.mode \|\| MODE.TOPIC` 으로 주제 모드 판정 → `sourcePhoto: false` |
| 문단 규칙이 영상 프롬프트에만 있음 | 기사 글 문단 **평균 143자** (영상 76자) | 규칙이 한 프롬프트에만 살아 있었다 → `readabilityRules()` 로 공용화 |
| 원본 비율 유지를 방송 캡처에만 적용 | 포스터(1.78)를 3:2 로 담아 **"AGENT KIM" → "GENT KI"** | 보도사진에도 글자가 박혀 있다 |

그리고 **고치는 로직이 스스로를 배신한** 경우도 있었습니다 — 합성본을 피하려는
대표 이미지 교체가 조건에 얼굴 개수를 넣지 않아 **또 합성본을 골랐습니다.**

**같은 사진 중복**은 파일명·URL·비율로 못 잡습니다. 같은 사진을 여러 매체가 서로
다른 크기·크롭으로 배포하기 때문입니다(포스터가 640×360 과 1000×700 로 들어왔고
비율마저 1.78 / 1.43 로 달랐습니다). `scripts/dupe_photos.py` 가 **dHash 로 내용을
비교**합니다. 임계값 16 은 실측 근거가 있습니다(같은 포스터 거리 12, 다른 사진 최소 22).

### 카드 연출에 대한 정정

이전에 "`cardLayouts.js` 의 연출 5개가 도달하지 않는 죽은 코드" 라고 적었다면
**그 진단은 틀렸습니다.** 연출은 10개 모두 정상 구현돼 있고(editorial·panel·
spotlight·figure·band + neon·pop·zine·poster·clean), 죽은 게 아니라 **설정이
잠가 둔 것**이었습니다. `bodyStyle: "photo"` 라 본문은 카드를 아예 쓰지 않고,
대표만 `thumbLayout: "clean"` 으로 고정돼 있었습니다.

지금은 **사진 종류에 따라 자동으로** 고릅니다.
- 방송 캡처·보도사진 → `clean` (자막·포스터 글자와 겹치지 않게)
- 스톡 사진 → 10개 연출 순환 (`style: "mixed"`)

연예·방송 글은 대부분 `clean` 이 나오고, 주제 모드(재테크·생활정보) 글에서
연출이 살아납니다. 인물 사진일 때 얼굴을 가리는 연출은 자동 제외됩니다(`FACE_SAFE`).

### 남은 과제

1. **카테고리가 매번 `방송잡탕`** 으로 갑니다. `blog.category: "auto"` 가 사실상
   한 곳으로만 보냅니다. 블로그의 실제 카테고리 목록을 보고
   `blog.categoryAliases` 를 잡아야 합니다. (사용자가 직접 처리하기로 함)
2. ~~대표 이미지 해상도를 보지 않습니다.~~ **해결 (2026-07-30)** —
   `pick_face_frame.py` 의 대표 점수에 해상도 계수를 곱한다
   (짧은 변 900px 미만부터 √ 스케일로 감점, 537px → ×0.77).
   합성본 감점(음수)에는 곱하지 않는다 — 감점이 약해지면 안 된다.
3. ~~인스타 임베드는 매번 되지 않습니다.~~ **크게 개선 (2026-07-30)** —
   codex 가 계정만 찾고 게시물을 못 줄 때, **실제 Chrome 으로 프로필 그리드를
   긁는 폴백**을 넣었다 (`socialEmbed.js: scrapeProfilePosts`). 그리드 순서가
   최신순이라 날짜 없이도 근황이 보장되고, 고정글은 숏코드 시대 프리픽스로
   걷어낸다. 실측: 하트시그널5 출연자 7명 전부 codex 게시물 0건 → 프로필
   수집으로 7명 전부 임베드 성공. 익명 HTTP 경로(oEmbed 429 · og:title 로그인
   월)는 여전히 막혀 있다 — 반드시 실제 Chrome 이어야 한다.
4. **취재 섹션이 제목까지 올라오지 않습니다.** 재료는 확보되는데(뉴스 8건)
   codex 가 본문 한 문단에만 녹입니다. 최신 회차 영상으로 시험해 볼 필요가 있습니다.
5. **주제 모드는 오늘 한 번도 돌리지 않았습니다.** 연출 자동 선택이 실제로
   어울리는지, 문단 규칙이 정보성 글에서도 맞는지 확인이 필요합니다.
6. ~~발행 후 검증 단계 (§8 4번)~~ — 해결 (2026-07-30, §8 4번 참고).

### 새 컴퓨터에서 이어받을 때

```powershell
git clone https://github.com/yoo9857/mytis.git
cd mytis
npm install
Copy-Item .env.example .env    # KAKAO_ID / KAKAO_PW / TISTORY_CATEGORY 채우기
npm run login                  # 컴퓨터마다 1회 (2단계 인증은 직접 통과)
pip install -r requirements.txt   # 영상 모드를 쓸 때만
npm run doctor
```

`out/` 은 저장소에 없습니다(gitignore). 쌓이면 `node scripts/clean-out.mjs --apply` 로 정리합니다.

### 작업 방식

- **발행 전에 `npm run draft` 로 뽑아 읽어 봅니다.** 지표만 보지 말고 독자처럼
  읽으면 시간 역행·메타 서술·어미 반복이 드러납니다. 실제로 이렇게 4번 고쳤습니다.
- 참고할 만한 글을 받으면 `benchmark-post` 스킬로 분석합니다. 표본이 하나면
  취향을 법칙으로 오해합니다(경어체를 법칙으로 착각할 뻔했습니다).
- 지시문을 고칠 때는 **실패한 실제 결과물을 나쁜 예로** 넣으면 잘 먹힙니다.

---

## 7-2. 자기학습 기록 (2026-07-29)

이 블로그는 발행할 때마다 **지시문과 코드를 같이 고칩니다.** 한 편 잘 쓰고 끝나면
다음 글에서 같은 실패가 되돌아옵니다. 이 절은 그날 배운 것을 남겨 두는 자리입니다.

**이날 3편을 발행했습니다.** 정숙·영철 2세 계획 / 강미나 캐스팅 / 나는솔로 32기 클립.
첫 글은 세 번 다시 생성했고, 그 과정에서 나온 실패가 아래 규칙이 됐습니다.

### 재발의 원인은 거의 항상 "규칙이 한 모드에만 살아 있는 것"

이날 또 나왔습니다. **두 번째입니다** (7-1 절의 문단 길이 규칙과 같은 유형).

| 규칙 | 있던 곳 | 없던 곳 | 증상 |
|---|---|---|---|
| 강조박스 개수·어미 | 클립 모드 | **기사 모드** | 7개 섹션에 7개, 5개가 같은 훈계 어미 |
| 합성본(이어붙인 사진) 걸러내기 | 대표 이미지 | **본문 사진** | 4분할 방송 캡처가 본문에 실림 |
| alt·caption 쓰는 법 | **아무 데도 없었음** | — | 스톡 검색어를 그대로 묘사 → 사진 교체 후 6개 전부 거짓 |

→ 규칙을 새로 만들 때 **"이게 세 모드 모두에 필요한가"** 를 먼저 물어야 합니다.
`readabilityRules()` · `calloutRules()` 처럼 공용 함수로 빼면 어긋나지 않습니다.

### 지시문끼리 부딪히면 모델은 최소한만 지킨다

`# SEO 요구사항` 이 *"primaryKeyword 를 최소 2개 섹션 heading 에 배치"* 를 요구하고,
바로 아래가 *"프로그램명·기수를 소제목마다 반복하지 마세요"* 를 금지하고 있었습니다.
모델은 **정확히 2개**(첫 소제목·마지막 소제목)에만 넣어 두 규칙을 동시에 지켰습니다.
읽기에는 군더더기였습니다. 규칙을 추가할 때 **기존 규칙과 부딪히는지** 봐야 합니다.

### 저작권 규칙을 세게 걸면 인용이 죽는다

"기사 문장을 옮기지 마세요" 를 강하게 걸었더니 **취재원의 발언까지 지웠습니다.**

> 원문: 정숙이 "아기 낳고 싶은 생각이 드신 거면 바로 준비하세요. **우리는 시간이 금이잖아요**",
> "저도 이제 몸 만들고 있는데 **임신할거에요!!!**" 라고 답했다
> 초안: "2세 준비 의사를 전했다고 보도했다. 몸 상태를 관리하고 있다는 취지로 답했다"

저작권 대상은 **기자의 문장**이고, 인물이 한 말은 **사실 그 자체**입니다.
이 구분을 명시하고 "발언이 있으면 최소 2~3개 살리세요" 로 바꿨습니다.
발언이 사라지면 독자가 반응할 지점이 하나도 남지 않습니다.

### 출처만 달고 내용을 안 쓰면, 출처 목록에 답이 있는데 글이 틀린다

1월 22일 기사를 출처 3번에 넣고도 거기 있는 *"사실 저희는 임신을 준비하고 있는 상황"*
(결혼식 **전**)을 본문에 쓰지 않았습니다. 그래서 7월 발언을 "결혼 뒤 처음 공개한 계획"
처럼 다뤘고, **제목부터 프레임 전체가 틀렸습니다.**

→ 출처마다 자문: **이 기사에서 가져온 사실이 본문 어느 문장인가?**

### 규칙에는 예외가 필요하다 — "출처를 본문에 반영하라" 는 건강·사생활에 적용하지 않는다

위 규칙을 넣은 **그날 오후에 바로 부딪혔습니다.** 채영 출국 글에서 본문은 소속사 표현대로
"건강 관련 사유로 불참" 까지만 썼는데, 출처 목록에는 **진단명이 제목에 담긴 해외 매체
기사**가 그대로 남아 있었습니다.

- 규칙대로면 → 그 기사의 핵심(진단명)을 본문에 넣어야 한다
- 판단대로면 → 넣지 않는 것이 맞다

**후자를 택하고 출처에서 지웠습니다.** 출처 목록은 독자가 눌러 보는 곳이라,
본문에서 안 쓰기로 하고 출처에 남기면 그 판단이 무의미해집니다.

판단 기준: 그 사실이 **당사자나 소속사가 직접 공개한 범위** 안에 있는가.
밖에 있으면 본문에도, 출처에도 넣지 않습니다.

> **여기서 배운 것**: 규칙을 새로 넣으면 그 규칙이 **잘못 적용될 경우**를 같이 적어야 합니다.
> "무조건 반영하라" 만 남기면 다음 글에서 사생활을 끌어옵니다.

### 없는 숫자를 축으로 세우지 마라

날짜 두 개를 빼서 만든 **"114일"** 을 제목·seoTitle·메타·요약·섹션 2개·결론·썸네일에
전부 넣었습니다. "약 3개월 23일로도 계산할 수 있다" 는 문단까지 있었습니다.

판정 기준: 그 숫자가 **빠르다·늦다·많다·적다** 중 무엇인지 말할 수 있어야 축이 됩니다.
제목에 쓸 숫자는 **취재원이 말했거나 기관이 발표한 것**이어야 합니다.

### 배경 취재가 잘되면 주인공이 밀려난다

강미나 캐스팅 글에서 요약 5줄 중 **3줄이 채종협·원지안** 이야기였고, 섹션 하나와
표 전체가 다른 배우 이야기였습니다. 3월 보도의 표현 차이를 찾아낸 것은 좋았지만,
독자는 강미나를 검색해 들어옵니다. 배경은 **본문에 남기고 요약에서는 내립니다.**

### 규칙으로 두 번 새어나가면 코드로 잡는다 — `src/lintKo.js`

조사 오류가 지시문에 규칙을 넣은 뒤에도 또 나왔습니다
("출산 발표**은** 나오지 않았다" · "iMBC연예**은** 보도했다").
받침 유무로 정해지는 규칙이라 사람이 볼 필요가 없습니다.

| 설계 결정 | 이유 |
|---|---|
| **한 방향만** 검사 (받침 없는 음절 + 은/을/으로) | 반대쪽은 동사 어미와 겹친다 — "먹**는**다", "앉**는**" |
| 낱말 **전체**로 예외 비교 | `endsWith('예은')` 로 했더니 잡아야 할 **"연예은"** 이 통과했다 |
| 고치지 않고 **경고만** | "박예은 씨" 같은 사람 이름 오탐이 남는다 — 판단은 사람이 |

`m.index` 는 조사가 아니라 **그 앞 음절**을 가리킵니다. 이걸 빼먹어 "발표은" 이
"발표" 로 잘리고 제안이 "발는" 으로 나왔습니다. 테스트 11개 중 10개 통과.

### 합성본은 비율로 걸러진다 (실측)

`src/photo.js` 의 `looksLikeMontage()` — 비율 **0.5 미만 / 3 초과** 를 제외합니다.

| 사진 | 비율 | 판정 |
|---|---|---|
| SBS Plus 방송 캡처 4장을 세로로 이어붙임 (300×893) | 0.34 | 제외 ✅ |
| 스포츠경향 합성본 (700×1550) | 0.45 | 제외 ✅ |
| 인스타 스토리 세로컷 (466×672) | 0.69 | 통과 ✅ |
| 인터뷰 캡처 (900×505) | 1.78 | 통과 ✅ |

`imageSize()` 는 `src/imageSize.js` 로 분리했습니다 — `images.js` 가 `photo.js` 를
가져오므로 반대로 가져오면 순환이 되고, `photo.js` 만 쓰는 스크립트가 playwright 까지
끌어오게 됩니다.

### 검토 루프의 실제 효과 (같은 소재를 3번 생성)

| 항목 | 1차 | 3차 |
|---|---|---|
| 제목의 축 | 결혼 114일 만에 (없는 숫자) | 1월 발언부터 이어진 계획 |
| 직접 인용 | 0건 | 6건 (전부 원문 확인) |
| 강조박스 | 7개 · 5개가 같은 훈계 | 4개 · 각각 새 사실 |
| 중복 섹션 | S6≈S1, 결혼식 3회 반복 | 없음 |
| 섹션·분량 | 7개 3,529자 | 6개 3,209자 |

3차는 빈 자리를 반복이 아니라 **새 사실**로 채웠습니다 — 4월 6일 결혼식 비하인드에서
정숙 "성별은 상관없이 건강하게만", 영철 "아들 둘을 원한다". 1차에는 없던 소재입니다.

### 네이버 레이아웃 — 우리 발행글을 우리 도구로 재서 고쳤다 (같은 날 오후)

`bench-naver.mjs` 를 상위 글이 아니라 **우리가 방금 발행한 글**에 돌렸다.
감이 아니라 수치로 격차가 나왔고, `naverDoc.js` 를 고쳐 프리뷰로 재검증했다.

| 항목 | 발행글 실측 | 고친 뒤 | 상위 글 법칙 |
|---|---|---|---|
| 문단 중간값 / p75 / 최대 | 26 / 29 / **268** | 19 / 21 / 39 | 8~16 / 14~22 / 26~31 |
| 인용구(한 줄 정리) | **243자** | 35자 | 10~40자 |
| 구분선 | 2개 (하단 출처용뿐) | 8개 (섹션 전환마다) | 1~23개 |

- **268자 벽의 정체**: 결론·FAQ 답·directAnswer 는 한 문자열로 와서 mobileLines 를
  안 거쳤다. `sentences()` 로 문장 단위로 나눠 문장 사이에 여백을 넣었다.
- **한 줄 정리 인용구**에는 첫 문장만 넣고 나머지는 본문 문단으로 푼다.
- **표 펼치기**: 행을 ' · ' 로 이으면 100자를 넘는다. 첫 열(시점)을 굵은 줄로
  세우고 나머지 열을 아래 줄로, 행 사이에 여백.
- **고아 글자**: mobileLines 가 max(30)에 가까운 자리를 골라 29자 줄이 나왔고,
  렌더 폭(22~25자)을 살짝 넘겨 "…보도했 / 고," 처럼 한두 글자가 떨어졌다.
  target 에 가장 가까운 자리로 바꾸고(꼬리 5자 이하 회피), max 를 25로 내렸다.
- **FAQ**: 문답을 컴포넌트 하나로 (인접 컴포넌트 간격이 0 이라 나누면 벽이 된다).
  "A." 는 문장 분리 **뒤** 첫 문장에 붙인다 — 먼저 붙이면 "A." 가 홀로 선다.

독자 피드백("가운데 정렬 안 됨·디자인 손상") 뒤 한 번 더:

- **정렬 축 통일**: 본문만 가운데였고 소제목·표 캡션·FAQ 질문·하단 블록은 왼쪽이었다.
  축이 두 개면 글이 흐트러져 보인다. 전부 가운데로 (발행 실측: 왼쪽 정렬 소제목 0개).
- **nodeStyle 실측** (`scripts/probe-nodestyle.mjs` — 발행 없이 에디터 왕복):
  `underline` · `italic` · `fontColor` · `backgroundColor` **살아남음**,
  `strikethrough`/`strike` 삭제됨, 알 수 없는 컴포넌트 0개. 이 실측이 있어야
  본문 중간 강조를 넣을 수 있다 — 모르는 키는 회색 박스를 만든다.
- **본문 강조 엔진** (`accentNodes`): 따옴표 인용 → 굵게+fs19, 날짜·시각·수치 →
  굵게+밑줄. 색은 쓰지 않는다 — 자동으로 고른 색은 반드시 촌스러워진다.
  발행 실측: 밑줄 23곳 · 굵게 36곳 · fs19 8곳.
- **인용은 줄에 걸치면 강조할 수 없다** — 여는/닫는 따옴표를 문자로 구분 못 해
  "정숙은 " 같은 화자 쪽이 강조되는 오작동이 났다. 잘린 조각 강조를 버리고,
  대신 mobileLines 가 **따옴표 안에서 끊지 않게** 했다 (max 보다 긴 인용만 예외).

세 번째 피드백("세로로 너무 길다") 뒤 — **표 스키마를 실측해 진짜 표를 쓴다**:

- 시점 흐름을 문단으로 펼치니(날짜 이정표 + ↓ 연결) 행마다 4~5줄이었다.
  표는 행 하나가 한 줄이다 — 타임라인 5행이 화면 셋에서 하나로 줄었다.
- 스키마는 **에디터가 만들게 해서 베꼈다**: probe-table.mjs 가 툴바의 표 버튼을
  눌러 생긴 컴포넌트를 떠오고(→ logs/naver-table-schema.json), probe-table2.mjs 가
  셀에 문단·굵게·색·크기를 채운 것이 왕복에서 살아남는지 확인했다 (회색 박스 0).
- "커서가 표 안에 있으면 사진 업로드가 표 안으로" 함정은 여전하지만,
  우리는 사진을 다 올린 **뒤에** 문서를 주입하므로 안전하다.
- 타임라인 표는 머리행을 생략한다(날짜는 설명이 필요 없다). 시점 30 : 내용 70,
  '의미' 열은 내용 아래 회색 작은 글자(fs13)로 접는다.
- **모르는 컴포넌트가 필요해지면 이 순서를 쓰면 된다**: 에디터 UI 로 만들게 한다 →
  getDocumentData 로 떠온다 → 우리 값으로 채워 왕복 검증한다 → 그때 코드에 넣는다.

### 임베드에 2019년 팬 직캠이 들어간 사고 (같은 날)

채영 출국 기사에 "190816 트와이스 말레이시아 출국 4K Fancam"(2019년·팬 채널)이
붙었다. 원인 둘 다 `youtube.js`:

1. OFFICIAL_HINTS 의 `studios?` 가 팬 채널 "**studio** PARN" 을 공식으로 오판
2. 공식 채널이 없으면 **비공식으로 폴백**했다 — 지시문은 "확실하지 않으면 빈 배열"
   인데 코드가 반대로 움직였다. 지시문과 코드가 어긋나면 코드가 이긴다는 것.

고침: `studios?` 제거 · fancam/직캠을 AVOID 에 추가 · 폴백 삭제(공식 없으면 안 넣음) ·
기사 모드는 "N년 전" 영상 제외 (근황 글에 옛 영상은 검색어가 맞아도 틀린 영상이다).

### '오늘 뭐 읽지?' — 네이버 책 에세이 시스템 (같은 날 오후, 두 권 발행으로 검증)

전체 알고리즘은 `.claude/skills/naver-book-post/SKILL.md` 가 원본이다. 뼈대:
`npm run book`(알라딘 월간 문학 베스트 → 중복 제외 → 초안) → 사람 검토(사진
큐레이션·카드·검증 발췌) → `publish --naver`(글감 책 카드 → URL 기록 → 다음 글이
"지난 책" 으로 연결). 발행: 투명한 나선 224361622690 · 헤일메리 224361654741.

이 시스템을 만들며 굳은 교훈:

- **스키마가 프롬프트를 이긴다** — --output-schema 의 필드 설명("질문형 소제목",
  "팁") 때문에 프롬프트 금지가 4차까지 무력했다. 모드가 다르면 스키마도 다르게
  (book.schema.json).
- **글마다 다른 결은 페르소나로** — 목소리 셋(평론가/북튜버/소설가)을 제목 해시로
  로테이션. 같은 책은 재생성해도 같은 목소리라 검토를 반복할 수 있다.
- **정직함의 선**: 읽은 척 감상은 거짓, 책을 만나러 가는 경험(역자의 문장에서
  멈춘 순간, 첫 장을 아껴 두고 싶은 기대)은 진짜다. 책 속 문장은 서점 공개
  발췌('책 속으로')에서 검증한 것만.
- **미지의 컴포넌트는 실측 3단계로 연다**: 에디터 UI 로 만들게 한다 → 떠온다 →
  채워서 왕복 검증. 이 순서로 표·글감 책 카드·nodeStyle(밑줄·색)·align 을 열었다.
- **글감 패널의 함정들**: 본문의 같은 글자를 오클릭(패널 영역으로 제한), 접힌 채
  열림(펼침 재시도), 패널이 열려 있으면 발행 레이어가 안 열림(토글로 닫기),
  선택적 클릭의 기본 30초 대기(짧은 timeout — 71초→14초).

### 이날 만든 것

| 파일 | 하는 일 |
|---|---|
| `src/lintKo.js` | 조사·어미 3연타를 기계 검사 (생성 직후 + 발행 직전 이중) |
| `src/imageSize.js` | 이미지 헤더에서 픽셀 크기를 읽는다 (photo·images 공용, 순환 회피) |
| `prompt.js: calloutRules()` | 강조박스 규칙 공용화 — 기사 모드에 없어서 재발했다 |
| `naverDoc.js: sentences()` | 긴 덩어리(결론·FAQ·directAnswer)를 문장 문단으로 승격 |
| `src/schema/book.schema.json` | 책 에세이 전용 출력 스키마 (필드 설명이 에세이 기준) |
| `scripts/book-today.mjs` | 알라딘 월간 문학 베스트 → 오늘의 책 (중복 제외) |
| `scripts/quote-cards.mjs` | 작가 카드 + 글귀 카드 한 쌍 (HTML 렌더 — 모델은 한글을 깨뜨린다) |
| `scripts/book-shots.mjs` | 책 연출컷 생성 (OPENAI_API_KEY 필요 — 키 대기 중) |
| `scripts/probe-table·nodestyle·material-book.mjs` | 미지 컴포넌트 실측 3단계 도구 |
| `naver.js: attachBookMaterial()` | 글감 > 책 카드 자동 첨부 (대표 아래·가운데) |

### 클립 글도 3층 구조로 (같은 날 저녁 — 신입사원 강회장을 세 번 발행하며)

"~입니다 로만 끝나 재미없다, AI 같다" → "영화 읽어주는 채널처럼 집중하게 될까" 라는
독자 물음을 따라 클립 글을 두 번 갈아엎었다. 완성된 3층:

  ① 리캡의 서사 문법 — 콜드오픈(가장 충격적인 장면 한가운데서 시작) ·
     섹션 끝의 미끼(두세 번만) · 인물의 한 줄 캐릭터("한 번 죽었다 돌아온 회장님") ·
     결과 먼저, 과정은 뒤에 (타임스탬프가 이정표)
  ② 해석자 3인 로테이션 (영상 ID 해시) — 정주행 평론가 / 본방사수 이웃(해요체) /
     작법 뜯어보는 사람. 관찰(무슨 일이) → 해석(이 장면이 하는 일) → 판단.
  ③ 기계 검사 — 조사 · 문단 내 어미 3연타 · **글 전체 단조 종결**(75% 이상 한 종결이면
     경고 — 문단이 2문장씩이라 연타 0인데 42/42문장이 "~니다" 인 글이 나갔던 구멍).

같은 첫 장면의 세 판이 증거다:
  1판 "0분 45초, 심장이 돌아왔다는 말이 들립니다." (안내 방송)
  3판 "죽었다던 사람이 이사회장에 들어옵니다. 14분 28초, '죽긴 누가 죽어?'가
      회의실을 멈춰 세우거든요." (콜드오픈 + 해요체 해석자)

해요체 페르소나의 1인칭은 "저" — "나는 ~보였어요" 가 세 번 재발한 뒤 규칙으로 박았다.
목소리 로테이션은 이제 세 모드 공통 장치다 (책 BOOK_VOICES · 클립 CLIP_VOICES).

---

## 7-3. 사진이 엉뚱하게 실리는 세 가지 경로 (2026-07-30 · 나솔 32기 현커 글)

기사 모드 초안 한 편에서 **사진 6장 중 4장이 잘못 실렸다.** 원인이 서로 다른
세 곳이었다. 하나만 고치면 나머지가 그대로 남는다.

### ① dHash 중복 검사가 조용히 실패하고 있었다 — **고쳤다**

`scripts/dupe_photos.py` 가 결과를 **경로로** 돌려줬다. Windows 기본 인코딩(cp949)
으로 찍혀 한글 경로가 깨지고, 받는 쪽 `photo.js` 는 파일명으로 슬롯을 찾으므로
대조가 전부 빗나갔다. **찾아 놓고도 버리지 않았고, 경고조차 남지 않았다.**

> 실측: 같은 컷을 거리 9 로 정확히 찾아냈는데 본문 1번·2번에 그대로 두 번 실렸다.
> (bnt 가 같은 사진을 `...0211.680x.0.jpg` / `...0212.jpg` 로 내보냈다)

→ 이제 **입력 줄 순번**으로 주고받는다. 순번은 인코딩을 타지 않는다.
**다시 경로로 바꾸지 마세요.**

### ② 4분할 격자 합성본은 비율 필터를 그냥 통과한다

`looksLikeMontage()` 는 비율만 본다(0.5 미만 / 3 초과). **2×2 격자는 16:9(1.78)**
라 걸리지 않는다. 대표 이미지의 얼굴 개수 검사도 *"단독 컷 후보가 없으면 그대로
둔다"* 로 빠져나가서, 자막이 박힌 합성본이 대표가 됐다.

> 실측: 원래 자막("총 네 쌍의 최종 커플 탄생") 위에 우리 헤드라인("4쌍 중 남은
> 3쌍")이 얹혀 글자가 두 겹으로 겹쳤다. 부제·크레딧은 밀려 사라졌다.

**자동 판정을 두 가지 시도했고 둘 다 실패했다. 되풀이하지 마세요.**

| 시도 | 근거 | 결과 |
|---|---|---|
| 중앙 이음새(seam) — 가로·세로 양쪽에서 불연속이 크면 격자 | 합성본 min비 2.4·2.9 vs 정상 1.1~1.9 | **오탐.** 검은 철제 책장 스톡 사진이 min비 3.9 로 잡혔다. 실제 장면의 격자(선반·창틀·타일)와 구분이 안 된다 |
| 로고 반복 — 패널마다 채널 로고가 박히므로 좌상단 패치를 template match | 방송 캡처의 고유 신호 | **실패.** 배경이 평평한 스톡 사진이 반복 4회·유사도 1.00 으로 걸리고, 정작 합성본 하나는 0회였다 |

→ 지금은 **사람이 고른다.** `node scripts/grab-source-photos.mjs out/<글>.json <폴더>`
로 후보를 펼쳐 보고, 쓸 것만 `sourceImages` 에 남긴다(순서 = 슬롯 순서).

**합성본이라고 늘 버릴 것은 아니다.** 대표에는 못 쓰지만(헤드라인이 겹친다),
**본문에는 쓸 수 있다** — 본문 사진에는 글자를 얹지 않는다. 이 글에서는 네 커플이
한 장에 담긴 4분할이 1번 섹션의 내용과 정확히 같아서 본문 1번으로 썼다.

### ③ 스톡 슬롯은 **다른 검색어의 사진**으로 채워진다

`photo.js:940` 은 슬롯에 맞는 후보(`forSlot`)가 없으면 `rest` 로 넘어간다.
빈 슬롯보다 낫다는 판단이지만, 그 결과 **캡션·alt 가 사진을 설명하지 못한다.**

> 실측: '뒷모습 커플'(현숙·경수 관계 정리) 자리에 그린스크린 뉴스 스튜디오가,
> '라이브 마이크' 자리에 외국인 스트리머 얼굴이 들어갔다. 검색어 자체로도
> 실패했다 — "couple talking cafe" 가 프랑스 카페의 서양 노부부를 데려와
> 한국 돌싱 예능 글에 붙었다.

→ **인물이 담긴 스톡 사진은 연예 글에 쓰지 않는다.** 독자가 그 사람을 당사자로
오인한다. 얼굴 없는 상징 컷(뒷모습·사물)만 쓰고, 그것도 슬롯이 맞았는지 눈으로
확인한다. 확인이 안 되면 **사진 없이 낸다** — 실제 장면은 공식 유튜브 임베드가 맡는다.

### 의혹·법적 분쟁 기사 규칙을 지시문에 넣었다 (`prompt.js: legalDisputeRules()`)

황정민 관련 기사(A 씨의 불륜 의혹 제기 + 미성년 자녀에게 DM)를 받고 나서
지시문에 **이 상황을 다루는 규칙이 없다**는 것을 알았다. 있던 것은 한 줄뿐이었다
— *"판결 전에는 혐의·의혹 표현을 쓰고 단정하지 마세요."* 그것만으로는
**주장을 제목·요약의 축으로 세우는 것**을 막지 못한다.

이 사안의 구조가 규칙을 만들게 했다.
- A 씨의 불륜 주장 → **미확인**
- 소속사: "지속적으로 괴롭힌 스토킹 범죄 피의자" · 형사 고소
- 법원: 접근금지 등 잠정조치 3회 · 스토킹 혐의 인정 **벌금 300만 원 약식명령** → **확정된 판단**

즉 **확정된 절차와 미확인 주장이 한 사안에 섞여 있다.** 트래픽이 몰리는 쪽은
주장이고, 검증된 쪽은 절차다. 규칙 없이 쓰면 모델은 트래픽 쪽으로 간다.

→ 두 덩어리를 넣었고 **기사 모드와 클립 모드 양쪽에** 걸었다(한쪽에만 두면 재발한다):
1. **축은 확정된 절차·판단에** 세운다. 주장은 귀속해서 주장으로만, 제목·요약·강조박스
   금지. 양쪽 입장을 모두 싣는다. 판단이 나온 부분과 안 나온 부분을 한 문장에 섞지 않는다.
2. **제3자(특히 미성년자)** 는 사건의 구성요소가 아니라 사람이다. 생년·나이·학교는
   **보도에 나와 있어도 쓰지 않는다.** 판단 기준은 채영 건에서 정한 것과 같다 —
   당사자·소속사가 직접 공개한 범위 밖이면 본문에도 출처에도 넣지 않는다.

### 유튜브 verified 를 '공식 출처' 로 읽으면 가십 채널이 들어온다 — **고쳤다**

`official: it.verified || OFFICIAL_HINTS.test(channel)` 이었다. 체크마크는
**"본인 채널임"** 을 뜻하고 "믿을 만한 출처" 를 뜻하지 않는다. 구독자 많은
가십 채널은 전부 verified 다.

> 실측: 황정민 사안 글에 "연예 뒤통령이진호" 의
> *"「스토킹 당했다는데..」 황정민 사생활 통화 녹취"* 가 **공식으로 분류돼** 뽑혔다.
> AVOID_HINTS 에도 걸리지 않았다. 같은 사안의 JTBC 뉴스와 나란히 실릴 뻔했다.

→ 세 가지를 걸었다.
- `official` 은 **채널명 판정만** 쓴다 (verified 단독으로는 공식이 아니다)
- `GOSSIP_CHANNEL` — 채널명으로 가십 채널 제외 (`뒤통령|렉카|가로세로|폭로|…`)
- `PRIVATE_MATERIAL` — 제목으로 사적 자료 영상 제외 (`녹취|통화 녹음|카톡 공개|…`)

**가십 판정을 제목으로 하면 안 된다.** 같은 사안을 다룬 JTBC 뉴스 제목에도
"사생활 폭로" 가 들어 있어 언론사 보도까지 함께 날아간다. 채널명으로만 본다.

> 대가: 이름에 매체·공식 표시가 없는 **아티스트 공식 채널**(예: `BLACKPINK`)은
> 이제 임베드되지 않는다. "확실하지 않으면 넣지 않는다" 쪽으로 기울인 것이다.
> 아이돌 글에서 임베드가 0개가 되면 이 규칙을 먼저 보라.

### 사진 검색어가 의혹을 대신 주장한다

`smartphone message screen` 이 **이별 문자 스톡**을 물어왔다
("We can not be together… It's not you, it's me. I'm sorry"). 그게
**미성년 가족에게 간 DM** 섹션에 붙었다. 확인되지 않은 불륜 의혹을 사진이
대신 주장하는 모양이 된다.

→ 법적 분쟁·의혹 글에서는 **사람이나 사적 대화가 보이는 스톡을 쓰지 않는다.**
대표도 마찬가지다: 언론사 워터마크(STARNEWS)가 남은 얼굴 클로즈업에
"DM 보도 뒤 쟁점" 이 찍혀 나왔다. 남의 매체 워터마크를 우리 대표로 쓰는 것이고,
의혹 제목을 얼굴에 박는 프레이밍이다. **사진 없이 그라디언트 카드로 갔다.**

`article.photoDir` 로 손수 고른 폴더를 쓸 때 주의: 대표 브리프의 `photoQuery` 를
**빈 문자열로 두면 안 된다.** falsy 라서 `pick()` 이 '지정 없는 슬롯' 으로 보고
폴더의 사진을 먹는다(대표에 인물 사진이 올라가고 본문과 중복됐다).
값을 넣어 두면 '검색으로 채울 슬롯' 으로 보고 건너뛰고, 로컬 폴더 경로는
스톡 검색을 하지 않으므로 그 슬롯은 카드가 된다.

### 원 기사의 훅이 **암시**일 때, 모델은 그 암시를 제목으로 세운다

문근영 결혼 발표 글에서 초안 제목이 *"김호영, 문근영 결혼에 4분 만에 축하"* 였다.
`seoTitle`·대표이미지 수치·이미지 브리프 3개가 전부 "4분" 이었다.

'4분' 은 텐아시아가 게시 시각에서 뽑은 숫자이고, 쓰임은
**"옥주현에게는 침묵했는데 축하는 4분 만에"** 라는 암시다. 더 이상한 건
**본문이 스스로 그 암시를 부정하고 있었다**는 것이다 —
*"축하 댓글 자체가 과거 갈등의 해결이나 입장 변화를 뜻하는 것은 아니다."*
아니라고 적어 놓은 것을 제목으로 세운 셈이다.

이건 §7-2 의 "없는 숫자를 축으로 세우지 마라" 와 같은 유형이지만 한 겹 더 있다.
**숫자가 보도된 것이어도, 그 숫자의 쓰임이 암시라면 축이 될 수 없다.**
판정: 그 숫자를 빼면 문장이 무너지는가? "4분" 을 빼도 사실은 그대로 남는다
(김호영이 축하 댓글을 남겼다). 무너지는 건 암시뿐이다.

→ 축을 당사자가 직접 알린 사실(문근영의 결혼 발표)과 확인된 경과
(2022 고소 취하 ↔ 2026 사과 요구)로 옮겼다. '4분' 은 본문·FAQ 에 귀속된 사실로 남겼다.

### 출처를 열어 보면 초안보다 좋은 발언이 있다

초안은 옥주현의 발언을 *"사과를 받지 못했다는 취지로 말했다"* 로 요약했다.
스포츠동아 원문을 직접 열어 보니 실제 발언이 훨씬 좋았다.

> "난 사과를 받은 적이 없다" — "'고소를 취하해줘서 고맙다. 하지만 난 누나를
> 저격한 적이 없다'는 말과 함께 친구 아버지의 장판을 홍보하려고 올린 글이었다는
> 설명만 들었다"

이 한 발언에 **양측 입장이 함께** 담겨 있다(김호영 쪽은 저격이 아니었다는 것,
옥주현 쪽은 그것을 사과로 받아들이지 않았다는 것). 요약하면 양쪽이 다 사라진다.

또 **날짜가 매체마다 달랐다** — 텐아시아는 "지난 6일", 스포츠동아는 "5일".
1차 보도(스포츠동아 7월 6일 기사)의 "5일" 을 쓰고 매체를 밝혔다.
**출처는 목록에 넣는 것이 아니라 열어 보는 것이다.**

### 위키미디어 인물 검색은 동명이인을 물어온다

`Hwang Jung-min` 검색 결과 1위가 **아나운서 황정민**(`Hwang Jung-Min (announcer).jpg`,
CC BY 2.0, **2592×3888**)이었다. 배우 본인은 2위(`Hwang Jung-Min.jpg`, CC BY 4.0,
649×972 · 제34회 청룡영화상)다. **해상도로 고르면 다른 사람이 실린다.**
`nameEn` 가 있어도(⑦-3 을 통과해도) 동명이인은 남는다 — 파일 설명(행사명)을
보고 사람이 확인해야 한다.

### 티스토리 신 에디터에는 대표 이미지를 고르는 UI 가 없다 (실측 4단계 · 2026-07-30)

"발행 시 대표이미지 지정 단계를 넣자" 는 과제를 실측했더니 **넣을 UI 자체가 없었다.**

| 실측 | 결과 |
|---|---|
| 발행 레이어 `.box_thumb` | 본문 **첫 이미지**를 자동 표시. 클릭 무반응. 삭제 버튼만 있음 |
| 본문에 이미지 0장일 때 | box_thumb 가 빈 박스 (썸네일 없이 발행됨) |
| 에디터에서 이미지 클릭 → 인라인 툴바 | 이미지 편집·크기·정렬·링크·대체텍스트뿐. **'대표 지정' 없음** |

→ 대표를 정하는 유일한 방법은 **원하는 이미지를 본문 맨 앞에 두는 것**이고,
파이프라인은 이미 그렇게 한다({{IMAGE_0}} = 대표). `checkThumbBox()` 가 발행
직전에 박스가 채워졌는지 확인하고, 비어 있으면 경고한다.
**"대표 지정 버튼을 찾아보자" 로 되돌아가지 마세요 — 없는 것을 확인했습니다**
(`scripts/probe-thumb.mjs` ~ `probe-thumb4.mjs`, `logs/probe-thumb*.json`).
발행 뒤에 대표만 바꾸고 싶으면 글 수정 화면에서 첫 이미지를 갈아끼우는 것이
유일한 경로다(사람 손).

### 이날의 정리 (2026-07-30 세션 마감)

**발행 7건** — 티스토리 5: 나솔 32기 현커 / 황정민 법원 조치 / 문근영·옥장판 /
김부장 리캡 / 하트시그널5 인스타 총정리(공식 임베드 7개). 네이버 2:
안녕, 피터팬(스탯 카드 피드백으로 재발행 224362606162) / 싯다르타 224362665051.

**이날 만든 것**

| 파일 | 하는 일 |
|---|---|
| `tistory.js: verifyPublished()` | 발행 후 본문 글자수·이미지 수 검증 (§8-4 해결) |
| `tistory.js: checkThumbBox()` | 대표 이미지 자동 지정 확인 (선택 UI 는 없음 — 실측 4단계) |
| `images.js: noText` | 카드·표지 위에 글 제목이 자동으로 얹히는 것을 끈다 |
| `quote-cards.mjs --palette` | 글귀 카드가 표지 지배색을 입는다 (cv2.kmeans) |
| `prompt.js: legalDisputeRules()` | 의혹·법적 분쟁 기사 — 축은 확정된 절차에, 제3자 신상 배제 |
| `prompt.js` 담백함·사진취재·명단형 | 독자 피드백 3건을 규칙으로 |
| `socialEmbed.js: scrapeProfilePosts()` | 인스타 프로필 그리드 폴백 (남은 과제 3 개선) |
| `youtube.js` GOSSIP/PRIVATE 필터 | verified ≠ 공식 — 가십 채널·사적 자료 차단 |
| `photo.js` 순번 dedup + 캡처 거리 6 | 중복 검사 조용한 실패 수정 + 캡처 오판 방지 |
| `scripts/grab-source-photos.mjs` | 원문 사진 후보를 눈으로 고르는 도구 |
| `scripts/edit-naver-post.mjs` | 네이버 글 수정 (진입로 막힘 — 뚫리면 사용) |

**교훈 한 줄씩**: 임계값은 소재별로 다시 실측한다(보도사진 16 ≠ 캡처 6) ·
플랫폼 표식(verified·오픈그래프)은 신뢰의 근거가 아니다 · 참고 글의 훅이
암시라면 축이 될 수 없다 · 출처는 목록이 아니라 열어 보는 것 ·
네이버는 발행 전에 다 잡는다(수정 불가).

### 원문 사진은 700px 를 넘지 않는다 — 대표가 작아진다

`clampToSource` 가 원본보다 크게 그리지 않으므로(15%까지), 700×394 컷에서 정사각
대표를 뽑으면 **453×453** 이 된다. 언론사 축소본만 있는 기사에서는 이게 상한이다.
공식 유튜브 썸네일(1280×720)은 자체 문구가 커서 헤드라인을 얹을 수 없어 대안이 못 된다.
(§7-1 남은 과제 2번과 같은 문제다)

### 네이버 발행 글은 **같은 주소로 수정할 수 없다** (2026-07-30 실측)

책 글에 사진을 고칠 일이 생겨 편집 진입을 실측했다. **전부 막혀 있다.**

| 시도 | 결과 |
|---|---|
| `postwrite?logNo=<번호>` | 블로그 홈으로 튕김 |
| `postwrite?logNo=...&isUpdate=true` | 블로그 홈으로 튕김 |
| `PostUpdateForm.naver?blogId=&logNo=` | section.blog 홈으로 튕김 |
| `blog.naver.com/<id>?Redirect=Update&logNo=` (\_modifyPost 가 만드는 URL) | mainFrame 이 BlogHome 으로 튕김 (헤드리스·헤드풀 동일) |

PC 블로그의 수정 버튼(`._modifyPost`)은 토큰 기반 폼 전송으로 보이며 URL 직행이
안 된다. → **수정이 필요하면 새로 발행하고 옛 글을 지운다.** 주소가 바뀌므로
`books.done.txt` 를 갱신하고, 그 글을 가리키는 내부 링크가 있는지 먼저 확인할 것.
`scripts/edit-naver-post.mjs` 는 이 진입로가 뚫리면 쓸 수 있게 남겨 뒀다
(문서 비우기 → 재조립 로직은 완성돼 있다).

---

## 7-4. 기록은 블로그가 사실의 출처 · 지시문보다 스키마가 이긴다 (2026-08-01 · 『수족관』)

책 한 편(『수족관』)을 내면서 **네 번 다시 생성**했다. 매번 다른 층에서 막혔고,
막힌 자리가 전부 "규칙은 있는데 다른 것이 그 규칙을 이기고 있던" 곳이었다.

### ① 소재 중복 — 파일만 믿으면 같은 책을 두 번 뽑는다 (**고쳤다**)

`books.done.txt` 에 주소는 **발행 성공 시에만** 붙는다. 다른 컴퓨터에서 작업하면
그 파일이 커밋되지 않아 기록이 사라진다.

> 실측: 다른 기기에서 07-31 에 발행한 『불안의 책』·『사춘기 엄마의 오장육부』가
> 기록에 없어 **『불안의 책』을 두 번째로 뽑아 집필까지 들어갔다.**
> 사용자가 "잠깐 불안의책 써있어" 로 잡았다.

→ `book-today.mjs` 가 후보를 **블로그 RSS 와 대조**한다 (`publishedTitles`).
**기록 파일이 아니라 블로그가 사실의 출처다.** 기록에만 없는 책은 경고로 알린다.

부수적으로 나온 것 둘:
- `doneTitles()` 가 ` -> URL` 을 떼지 않아 `b.title.includes(d)` 가 영영 거짓이었다
  (한 방향으로만 비교됐다).
- `page.evaluate` 안의 `fetch` 로 RSS 를 못 받는다 — 그때 페이지는 알라딘이고
  `rss.blog.naver.com` 은 크로스오리진이라 CORS 로 막힌다. **`context().request`** 를 쓴다.

### ② `toISOString()` 은 UTC 다 — KST 오전에는 어제 날짜가 찍힌다 (**고쳤다**)

> 실측 2026-08-01 08:22 KST: `books.done.txt` 에 `[2026-07-31]` 이 기록되고,
> 집필 지시문의 '오늘 날짜' 도 07-31 로 나갔다.

화제성 글에서 이건 그냥 오타가 아니다. "어제 발표된 순위" 가 "이틀 전" 이 되어
시의성 판단이 어긋난다. → `paths.js: todayStr()` 을 만들어 '오늘' 을 뜻하는 자리
4곳(prompt·newsFeed·socialEmbed·book-today)을 갈아끼웠다.
**'오늘' 에 `toISOString` 을 쓰지 마세요.**

(tweet created_at·기사 날짜를 찍는 `buzz.js`·`socialEmbed.js: fmtDate` 는 그대로 뒀다 —
그건 '오늘' 이 아니라 남의 시각이라 별개 문제다.)

### ③ 화제 수치를 취재문에 앞세우면 **순위 기사**가 된다 (독자 지적)

『수족관』을 고른 이유는 교보문고 주간 1위 역주행이었다. 그 사실을 취재문 맨 앞에
넣었더니 **제목·seoTitle·metaDescription·directAnswer·프롤로그·결말·포인트가 전부
순위 숫자**가 됐다. 제목이 "수족관, 3계단 올라 2년 반 만에 1위" 였다.

> 독자 지적: *"이런 내용은 뉴스스타일이고, 독자가 책을 소개하고 읽는 스타일로 하고싶은데"*

→ `buildBookPrompt` 에 **"이 글은 순위 기사가 아닙니다"** 를 넣었다.
제목에 순위·계단·퍼센트 금지 · 순위는 한 섹션 두세 문장 · **축은 책 안에서** 찾는다
(발췌의 비유, 제목의 뜻, 인물이 원하는 것, 출판사가 세운 질문).

최종 축은 책 안의 **물 이미지**였다 — 갇힌 물(수족관) → 내리는 비(슬픔) → 너른 물(바다).
제목 「수족관, 갇힌 물이 바다를 꿈꾸는 까닭」.

### ④ 없는 숫자의 출처가 **페이지 푸터**였다

1차 초안 제목이 "수족관, 2003년 여름 두 청춘이 꿈꾼 오키나와" 였고,
*"출판사 소개에 따르면 작품의 시간적 배경은 2003년의 뜨거운 여름"* 이라고 귀속까지 했다.

> 추적 결과 '2003' 의 유일한 출처는 알라딘 페이지 **푸터의 통신판매업신고
> `2003-서울중구-01520`** 이었다. '장마' 는 **독자 리뷰** 문장이었다.
> 출판사 소개에는 연도도 계절도 없었다.

§7-2 의 "없는 숫자를 축으로 세우지 마라" 와 같은 유형인데 한 겹 더 있다 —
**서점 페이지에는 책 정보가 아닌 글자가 많다.** → 지시문에 규칙으로 박았다:
푸터(사업자·통신판매업신고) · 이벤트·굿즈 배너 · **독자 리뷰**는 출판사 소개가 아니다.
도서관 소장 검색·가격비교 사이트는 sources 에 넣지 않는다
(1차 초안 출처 4건 중 3건이 그것이었고 keyTakeaways 에 "소장 6건", "10% 할인가" 가 들어갔다).

### ⑤ **스키마가 프롬프트를 이긴다** — 두 번째 확인 (§7-2 와 같은 유형)

사용자가 원한 결은 "독자가 책을 소개하고 읽는" 톤이고, 그에 맞는 페르소나가
이미 있었다(**라디오 북튜버** — 해요체, 1인칭 "저", 프롤로그를 자기 장면으로 여는).
목소리를 고정했는데도 3차 초안이 **전부 문어체 평서문**으로 나왔다.

> 원인: `book.schema.json` 이 문체를 하드코딩하고 있었다.
> `paragraphs`: *"**문학평론가의** 에세이 문단"* · `conclusion`: *"**평론가가** 글을 접는"*

→ 스키마에서 문체를 빼고 **지시문의 '당신의 목소리' 에 위임**했다.
그리고 스키마가 이기는 성질을 반대로 이용해, 지시문에 두 번 썼는데도 두 번 무시된
규칙을 스키마로 옮겼다 — 소제목의 **책 제목·저자명 반복 금지**, 소제목에 순위·날짜 금지
("결말 — 2026년 7월 23~29일 다시 찾은 독자" 가 실제로 나왔다),
**문단마다 문장 수를 달리한다**(3차 초안은 21문단이 전부 정확히 2문장이었다).

**목소리 해시도 틀려 있었다.** 해시가 topic 전체를 보므로 취재 재료를 붙이면
같은 책이 다른 목소리가 됐다(『수족관』이 취재문 유무로 정밀한 평론가 ↔ 마감 노동자 소설가).
→ **첫 줄만** 해시한다. 사람이 고정하려면 `config.json` 의 `article.bookVoice` 에 이름을 적는다
(비우면 3인 로테이션).

### ⑥ 이미지 브리프 규칙이 모드 공용이라 책 글에 **스탯 카드**가 나왔다

스킬은 스탯 카드(수치 얹은 그라디언트 카드)를 **2026-07-29 독자 피드백으로 이미 금지**했는데,
공용 `imageBriefRules` 가 `statValue` 를 계속 요구했다. 그래서 보라색 카드에
"3계단 상승" 이 찍혀 나왔고, 헤드라인 예시도 방송 리캡 어법("옥순이 멈추라고 한 이유")이라
**표지 그림 위에 낚시 문구**가 얹혔다.

→ `imageBriefRules(a, bodyImages, { book: true })` 로 두 규칙을 **갈아끼웠다.**
모순되는 지시를 덧붙이지 않은 이유는 §7-2 의 "지시문끼리 부딪히면 모델은 최소한만 지킨다".
`book.schema.json` 의 statValue/statLabel 설명도 "책 글에서는 항상 빈 문자열" 로 바꾸고
`noText` 를 스키마에 추가했다.

### ⑦ `noText` 이미지에 스크림·룩을 씌우면 **완성된 카드가 뭉개진다** (**고쳤다**)

> 실측: 표지 지배색(#2b6496)으로 맞춘 맑은 하늘색 글귀 카드가 `goldenHour` 필터와
> 스크림 비네트를 먹고 **회색으로 탁해졌다.** 종이 페이지는 누렇게 떴다.

스크림은 **글자를 읽히게 하려고** 까는 것이다. 글자를 얹지 않는 이미지에는
탁하게 만드는 효과만 남는다. → `noText === true` 면 룩을 걸지 않고 스크림을 0 으로 둔다
(대표·본문 양쪽 경로 모두).

**`noText` 는 "글자를 얹지 마라" 가 아니라 "이미 완성된 이미지다" 라는 신호로 읽는다.**

### ⑧ manifest 크레딧이 위키미디어·인스타 두 모양만 모델링했다 (**고쳤다**)

책 글에는 서점 상품컷(예스24·알라딘)과 우리가 만든 카드가 들어온다. 어느 쪽도
`owner`/`permalink` 로 표현되지 않아 **크레딧이 통째로 비었다.**
→ manifest 항목이 `credit`/`photographer`/`license` 를 직접 적어 두면 그대로 쓴다.

### 사진은 결국 사람이 골랐다 (§7-3 ③ 과 같은 결론)

파이프라인이 채운 스톡이 캡션과 **정면으로 모순**됐다.

| 캡션 | 들어온 사진 |
|---|---|
| "구매 독자 가운데 20대 여성 비중은 22.6%" | 카페의 **서양 노년 여성** 독서 컷 |
| "『수족관』은 2023년 12월 29일 출간됐다" | **100년 된 앤티크 양장본** 세 권 |

→ 서점 미리보기에서 실물 6장을 눈으로 골랐다. 그 과정에서 **좋은 재료를 발견했다**:
`09.jpg` 가 책의 **제사 페이지**였다 — *"무언가를 치유하는 것들은 모두 소금물이다.
땀, 눈물, 바다와 같이. — 카렌 블릭센"*. 글의 물 이미지를 책이 스스로 세워 둔 자리라
본문 문단으로도 넣었다.

**빈 미리보기 페이지를 잉크 비율로 걸렀다** — `pv-t1/t3/t5` 가 잉크 0.00% 인 백지였다.
`PIL` 로 회색 변환 후 어두운 픽셀 비율을 재면 한 줄로 판정된다.
(cv2 로 표지 지배색을 뽑을 때는 **한글 경로를 ASCII 임시 경로로 복사**해야 한다 — 함정 ⑫)

### 이날 발행

네이버 1건 — 『수족관』 224364620843 (사진 7장 · 스톡 0장 · 스탯 카드 0장).

### 이날 고친 것

| 파일 | 하는 일 |
|---|---|
| `paths.js: todayStr()` | 로컬 날짜. '오늘' 자리에서 UTC 를 몰아냈다 |
| `book-today.mjs` | 블로그 RSS 대조 · 기록 누락 경고 · 괄호 떼고 제목 비교 |
| `config.js: loadEnvFile` | export — 단독 스크립트도 .env 를 읽는다 |
| `prompt.js` | "순위 기사가 아니다" · 서점 페이지 읽는 법 · 책 모드 이미지 브리프 · 목소리 해시·고정 |
| `book.schema.json` | 문체를 지시문에 위임 · 소제목 제목반복·순위 금지 · statValue 금지 · noText 추가 |
| `images.js` | noText 이미지에 룩·스크림을 걸지 않는다 |
| `photo.js` | manifest 의 명시 크레딧 사용 |
| `config.json` | `article.bookVoice: "라디오 북튜버"` (비우면 3인 로테이션) |

**교훈 한 줄씩**: 기록 파일이 아니라 **발행된 결과**가 사실의 출처다 ·
'오늘' 에 `toISOString` 을 쓰지 마라 · 취재 재료의 **순서가 글의 축을 정한다** ·
숫자를 귀속했다면 **그 페이지의 어디에 있었는지**까지 확인하라 ·
규칙이 무시되면 지시문을 더 세게 쓰기 전에 **스키마를 먼저 보라** ·
`noText` 는 "이미 완성된 이미지" 라는 뜻이다.

---

## 7-5. 모드를 한 파일에 선언한다 · 영화 모드 신설 (2026-08-01 오후)

### ① `buildBookPrompt` 가 **한 번도 호출되지 않았다** (**고쳤다**)

영화 모드를 만들다가 발견했다. 지시문 라우팅이 `clip / fromNews / else` 세 갈래였다.

```js
let prompt = clip ? buildClipPrompt(...) : fromNews ? buildNewsPrompt(...) : buildArticlePrompt(...);
```

`"책: 수족관 — 유래혁"` 은 URL 이 아니므로 **`buildArticlePrompt`(연예 이슈 에디터 톤)** 로
갔다. 책다운 것은 스키마(book.schema.json)뿐이었다.

> 그래서 `BOOK_VOICES` · '읽은 척 금지' · 섹션 7개 구조가 **모델에 닿은 적이 없다.**
> §7-4 에서 "스키마가 프롬프트를 이긴다" 로 보였던 현상의 절반은
> **프롬프트가 아예 없었기 때문**이다. 같은 날 오전에 `buildBookPrompt` 에 넣은
> 규칙들(순위 기사 금지·서점 페이지 읽는 법·bookVoice 고정)도 효과가 없었다 —
> 그때 글을 실제로 바꾼 것은 취재문에 직접 쓴 지시와 스키마 수정이었다.

→ 라우팅을 **모드 기반**으로 바꿨다. 지시문·스키마 모두 모드로 고른다.

### ② 모드 정보가 다섯 곳에 흩어져 있었다 → `src/modes/<id>.js`

`mode.js`(판별·capabilities) · `prompt.js`(지시문·목소리) · `codexWriter.js`(라우팅) ·
스키마 3종 · `config.json`. 한 곳만 빠뜨려도 조용히 다른 모드처럼 동작했다.

영화 모드를 만들면서 **바로 두 곳이 깨졌다**: `imageBriefRules` 누락,
그리고 `movie.schema.json` 을 `book.schema.json` 에서 복제해 "이 책이 무엇이고",
"표지·저자명" 같은 **다른 모드 어휘가 3곳** 남았다.

→ 모드마다 한 파일에 선언한다 — 판별·capabilities·스키마·규칙 목록·목소리·섹션·
플랫폼·**충돌**·강등. `mode.js` 는 기존 import 를 지키는 재수출 창구다.

**`npm run doctor` 가 선언과 실제 지시문을 대조한다**(`lintModes`). 붙이자마자 두 건을 잡았다:
- 영화 모드에 `legalDisputeRules` 누락 (실존 배우를 다루는 글인데 없었다)
- **클립 모드가 문단 규칙을 두 벌** 갖고 있었다 — `readabilityRules()` 로 공용화하면서
  `buildClipPrompt` 의 **원본 사본을 지우지 않아** "6편"·"7편" 서로 다른 버전이
  함께 들어갔다 (§7-1 이 기록한 공용화의 뒷정리가 안 된 것)

선언이 실제로 소비되는지도 감사했다. `schemaFile`·`conflicts`·`platforms` 가
**장식으로 남아 있어서** 각각 스키마 선택·린트 검사·발행 전 경고로 연결했다.
**선언만 두면 거짓말이 된다.**

### ③ 정반대 규칙은 통합하지 말고 `conflicts` 에 명시한다

| | 책 | 영화 |
|---|---|---|
| 소제목에 작품명 | **forbid** | **require** (검색어다) |
| 결말 | never | choice (`config.movie.spoiler`) |

섞으면 책 글이 검색어를 반복하거나 영화 글이 검색 수요를 놓친다. 린트가 지시문에
그 지시가 실제 있는지 센다.

### ④ 목소리 장치가 3벌 중복이었다

해시 3곳·고정 2곳 — `CLIP_VOICES` 만 고정 경로가 없어 클립은 목소리를 못 박았다.
→ `pickVoice()` 하나로 통일. 해시는 **첫 줄만** 본다(취재 재료를 붙여도 목소리가 안 바뀌게).

### ⑤ 영화 모드 (신설) — 벤치마킹 결과를 규칙으로

참고 글 실측(`daraksil_hyejin/224364450785` · 「영화 새 정보 (스포 O) 줄거리 결말」):

| | 참고 글 | 우리 『악의』 |
|---|---|---|
| **글자당 사진** | **82자당 1장** (스틸 19장) | 349자당 1장 |
| 문단 중간값 | 162자 | 18자 |
| 캡션 | 0장 | 6/7장 |
| 해석·평가 | **0** | 있음 |

- **이 글은 감상문이 아니라 정보 상품이다.** 제목·태그가 전부 검색 질의고,
  구조가 `사진 2장 + 문단 1개` × 9 로 기계적이다.
- 82자당 1장은 zumzany 6편(85~105자)·travelhyuk(85자)와 같은 값이다 — **표본 3종에서
  같은 수치가 나왔으니 법칙**이다.
- 따라가지 않은 것: 문단 162자(우리 18자가 모바일에서 이긴다), 캡션 0.
- **스틸컷은 쓰지 않는다** (사용자 결정). 배급사 저작권이라 §6 이 금지한 유형이다.
  장면은 **공식 예고편 임베드**가 맡는다. 82자당 1장은 합법 경로로 닿지 않는 수치라 포기했다.
- 섹션: 프롤로그/영화 정보/줄거리/결말/등장인물/**해석**/포인트.
  참고 글이 해석 0 으로도 잘 됐으므로 **해석 섹션이 우리 차별점**이다.

`"영화: 제목 (감독)"` → 티스토리 Cinematic. **아직 한 편도 뽑아보지 않았다** (검증 미완).

### ⑥ 드라마 모드는 선언만 (`enabled: false`)

사용자 결정: **회차 리캡**. 영상(clip) 모드와 다른 점 —
clip 은 입력이 유튜브 URL 이라 자막에서 장면을 뽑지만, 드라마는 작품명+회차라
자막이 없어 `clipShots` 를 쓸 수 없다. 방영 정보·보도·공식 예고를 취재해야 한다.

### ⑦ 스크립트로 코드를 고칠 때 — 함수를 날렸다

주석 제거 로직이 위쪽 `/**` 까지 거슬러 올라가 **`buildBookPrompt` 를 통째로 삭제**했다.
그리고 함수 추출기가 매개변수 구조분해 `({ a, b })` 의 `{` 에서 균형을 맞춰
**시그니처만 253자** 떠왔다.

→ 함수를 프로그램으로 옮길 때는 **매개변수 목록의 닫는 `)` 뒤에 오는 `{`** 부터
중괄호를 센다. 문자열·템플릿리터럴·주석도 건너뛴다. 앞 주석을 함께 지우려면
그 사이에 다른 코드가 없는지 확인한다. (복원은 커밋된 버전에서 했다)

---

## 7-6. 지어낸 인용을 자막으로 잡는다 (2026-08-01 저녁 · 클립 2편)

### ① 자동 자막에서 딴 인용은 **축자성이 없다** (**코드로 잡는다**)

> 실측 — 나는솔로 32기 미방분(수동 자막 없음, 자동 자막 254줄):
> 초안이 **"영호가 가장 꼴찌예요"** 를 큰따옴표로 실었다. 자막 원문은
> `영 7영호 난 영어가 가장 꼴찌예요`("영어" 는 오인식)였고, 그 문장의 낱말은
> 자막 **888자에 흩어져** 있었다 — 어절을 주워 붙인 인용이다.

**사람을 꼴찌라고 말했다는 발언**을 실존 인물 입에 넣은 것이다. §6 의 보호선을 넘는다.

→ `src/verifyQuotes.js` — 큰따옴표 인용을 자막과 대조한다. 공백·문장부호를 지운 뒤
① 그대로 포함 ② 어절이 순서대로 **가까이** 모여 있으면 재구성 ③ 아니면 없음.
**근접 조건이 없으면 어절 줍기가 통과한다** (첫 판에 그렇게 통과했다).
발행 직전 안전망(`cli.js`)에 걸어 손으로 고친 글도 검사한다.

→ 지시문: **자막이 자동 생성이면 큰따옴표를 쓰지 말고 요지로** 옮긴다.
자막이 이름을 틀리면 정확히 옮겨도 오인용이다. 사람을 **평가하는 말**은 특히 인용하지 않는다.

### ② 캡션이 사진을 설명하지 않았다

스키마가 캡션을 *"이 장면이 왜 볼 만한지 한 줄"* 로 정의했다. 바로 옆 `quote` 는
"자막에 있는 말만, 요약·창작 금지" 인데 **캡션에는 검증 조건이 하나도 없었다.**

> 실측: 정지 화면이 보여줄 수 없는 문장 19개 — "웃음이 번진 직후",
> "대화의 속도가 늦어진다", "번호를 세는 동안 분위기가 다시 가벼워진다",
> "첫인상 평가가 확인된다". 경수 첫인상이 두 캡션에 겹치기도 했다.
> 사용자 지적: *"이런 가짜를 만든 이유는? 자원낭비같은데"*

- 캡션 = **어느 대목인지**, 화면과 자막으로 확인되는 것만. 분위기·흐름 서술 금지.
- **본문이 다루지 않는 장면은 고르지 않는다** — 캡처 한 장에 유튜브 페이지를 새로 열어
  7초가 든다. 19장을 골랐는데 본문이 짚은 대목은 **6개**였다.
  (장수 자체는 과하지 않았다 — 3,111자에 19장이면 164자당 1장으로 법칙의 절반이다.
   낭비는 장수가 아니라 **값이 없는 것**이었다.)
- `run.js: clipAlt()` — `alt` 19개가 **시각만 다른 같은 문장**이었다
  (`제목 — 0:13 장면`). 검색에도, 화면 낭독기에도 쓸모가 없다. 캡션을 반영해 고유 문형 1 → 19.

고친 뒤 같은 소재로 다시 뽑은 결과: 캡션 17개 전부 구체적("영자가 단점이 보이기
시작했다고 말한 순간"), 지어낸 인용 0건, `alt` 18개 전부 다름, 캡션 17개가
**모두 본문이 다루는 대목**.

### ③ 클립 글에 스톡이 섞이는 경로 (**막았다**)

로컬 사진 분기는 `return result; // 스톡 검색으로 섞지 않는다` 로 끝나는데
**장면 캡처 분기는 return 이 없었다.**

> 실측: 캡처 18장 중 2장이 중복 판정으로 지워졌고(264초·593초) `clipShots` 에는
> 경로가 남아, 발행 때 그 2칸을 **스톡으로 채우려** codex 검색이 돌았다.
> 나는솔로 출연자 캡션 옆에 무관한 스톡 인물 사진이 실릴 수 있었다 (§7-3 ③ 과 같은 실패).

→ 캡처가 한 장이라도 들어갔으면 거기서 끝낸다. 빈 칸은 그라디언트로 둔다.
부족한 장면은 **다시 뜬다**(`.tmp/recapture.mjs` 방식 — 파일이 없는 항목만 골라 재캡처).

### ④ 좌우 2분할 합성본은 비율 필터를 통과한다

`looksLikeMontage()` 는 비율 0.5 미만 / 3 초과만 걸러낸다.

> 실측(김우빈·신민아 기사): 방송 캡처 2분할 744×485(1.53)과 **뉴시스 워터마크가 박힌**
> 2분할 690×460(1.50)이 둘 다 통과했다. §7-3 ② 의 "2×2 격자가 16:9 라 안 걸린다" 와
> 같은 문제의 다른 형태다.

§7-3 이 자동 판정 **두 번 실패**를 기록해 뒀으므로 또 시도하지 않았다.
→ 사람이 골랐다. 합성본 2장을 빼고 **라이선스가 분명한 사진**으로 채웠다:
소속사 배포 결혼 사진 · 김우빈(CC BY 3.0) · 신민아(CC BY 4.0) · 제작발표회(CC BY 4.0) ·
얼굴 없는 상징 스톡. CC 는 저작자 표기가 의무이므로 `manifest.json` 에 크레딧을 심었다.

**`sourceImages` 를 지워야 한다** — 남겨 두면 다음 발행 때 합성본을 다시 끌어온다.

디즈니+ 공식 이미지도 받았다가 **뺐다**: 열어 보니 주지훈 스틸이라
"신민아 차기작" 캡션과 어긋났다(§7-3 ③). **밀도를 맞추려 어긋나는 사진을 넣지 않는다.**

### ⑤ 위키미디어 인물 검색이 **지명**을 물어왔다 (**가드 추가**)

> 실측: `Keigo Higashino` 검색 결과 10건이 전부 사람이 아니었다 —
> **東野駅(Higashino station) 6장 · 東野초등학교 2장 · 동명이인(Ayako Higashino) 1장.**
> 전부 CC BY 이고 800px 이상이라 기존 필터(라이선스·크기·광고)를 통과했고
> `isPerson: true` 로 표시됐다. "인물 사진 6장 확보" 라고 로그를 찍었다.
> (히가시노 게이고 본인 사진은 커먼즈에 **없다** — 0장이 정답이었다)

→ 이름의 **모든 낱말**이 파일명·설명에 있어야 통과한다. 성씨만 걸린 것은 버린다.
정규화는 **양쪽 같게** 해야 한다 — 한쪽만 특수문자를 떼면 `Hwang Jung-min` 의
토큰 `jungmin` 이 파일명 `Hwang Jung-Min` 과 안 맞아 **본인 사진이 탈락**한다.
(9 케이스 검증 후 실제 호출로도 확인 — 이제 "찾지 못했다" 로 정확히 판정한다)

### ⑥ 자잘한 것

- `noText` 를 스키마 `properties` 에만 넣고 `required` 에서 빼자 구조화 출력이
  **400(invalid_json_schema)** 을 내며 책 모드 생성이 전부 실패했다.
  **구조화 출력은 properties 의 모든 키를 required 에 요구한다.**
- `quote-cards.mjs` 가 `--author-img` 없이 돌면 **빈 흰 사각형**을 그렸다
  (선택 인자인데 레이아웃이 그대로였다) → 이름을 조판한 명패로 분기.
- `lintKo` 가 "선을 **그은**" 을 "그는" 으로 오탐했다. ㅅ불규칙 활용형
  (그은·이은·지은·저은·부은·나은)을 예외에. 진짜 오류(발표은·연예은)는 그대로 잡힌다.
- 자동 자막 영상은 **수동 자막이 없어도** `ko-orig` 로 동작한다.
- 촌장엔터테인먼트 영상 설명에 **"AI 데이터 활용 금지"** 문구가 있었다.
  SBS Plus 공식 채널에는 없다. 클립 소재를 고를 때 설명을 한 번 읽어 볼 것.

### 이날 발행 (2026-08-01)

네이버 2 — 『수족관』 224364620843 · 『악의』 224364640136
티스토리 2 — 나는솔로 32기 영자·영철 설거지 `solo-32-youngja-youngcheol-dishes` ·
김우빈·신민아 청룡 `kim-woobin-shin-mina-blue-dragon`

**교훈 한 줄씩**: 규칙이 안 지켜지면 **그 규칙이 모델에 닿았는지** 먼저 확인하라 ·
선언만 두면 거짓말이 된다(쓰이는지 감사하라) · 정반대 규칙은 통합하지 말고 명시하라 ·
자동 자막에서 딴 큰따옴표는 인용이 아니다 · 캡션은 **정지 화면이 보여줄 수 있는 것**만 ·
밀도를 맞추려 어긋나는 사진을 넣지 마라 · 코드를 스크립트로 옮길 때 중괄호는
**매개변수 뒤부터** 센다.

---

## 7-7. 영화 모드 첫 발행 · 캡처 화질 (2026-08-01 밤)

발행: 「영화 스파이더맨: 브랜드 뉴 데이 (스포 X) 줄거리 결말 정보」
`classic-m.tistory.com/entry/spider-man-brand-new-day-2026-plot`
(본문 4,905자 · 사진 8장 · 카테고리 Cinematic 자동 선택)

### ⓪ 먼저: **초안을 일곱 번 돌렸다. 그럴 필요가 없었다.**

사용자 지적: *"왜이리오래걸려?"* — 이 글 하나에 40분 넘게 썼다.

캡처 화질 문제(레터박스·선명도·타이틀 카드·자막)는 **이미 받아둔 이미지 파일로
파이썬만 돌려 몇 초에 검증**할 수 있는 것이었다. 그런데 매번 집필 3분 + 캡처 2분을
다시 태웠다. 게다가 단조 종결 재시도를 넣어 집필이 2회로 늘었다.

**부분만 검증하세요.**
- 이미지 배치·화질 → `renderImages(article, cfg)` 만 다시 (집필 없이 5초)
- 파이썬 판정 → 스크립트 단독 실행 (`printf '%s\n' *.jpg | python scripts/pick_face_frame.py`)
- 지시문·스키마 규칙 → `buildXPrompt()` 를 직접 불러 문자열만 확인
- **집필 재생성은 글 내용을 바꿀 때만.**

### ① 단계가 다른 단계의 조건문 안에 얹혀 있으면 새 모드에서 조용히 빠진다

예고편 캡처를 붙였는데 `clipShots: 0` 이었다. 로그에 '장면 캡처' 줄이 아예 없었다.

원인: 캡처 블록이 `if (!can(mode, 'youtubeEmbeds'))` **안에** 있었다. 클립 모드는
임베드를 검색하지 않으니 그 안에 있어도 됐지만, 영화 모드는 `youtubeEmbeds: true` 라
**else(임베드 검색)로 빠져** 캡처를 건너뛰었다.

→ 캡처를 독립 단계로 뺐다. 조건은 `can(mode, 'clipShots')` 하나뿐이다.
**§7-5 ② 와 같은 유형** — 새 모드를 추가할 때 단계가 어디에 얹혀 있는지 봐야 한다.

### ② 캡처 화질 — 여섯 가지를 고쳤다

사용자 지적: *"캡쳐 화면이 너무 지글거림, 선명하게"*

압축이 원인이 아니었다(`quality: 92` 로 이미 높다). 파일이 작았던 것은 화면이
어둡고 레터박스가 검어서였다.

| 문제 | 실측 | 조치 |
|---|---|---|
| **레터박스** | 예고편 2.39:1 을 16:9 에 담아 1080 중 **283px(26%)** 이 검은 띠 | 행 밝기·편차로 판정해 크롭 → 797px |
| 흔들린 프레임 | 선명도 상한 **10점** vs 얼굴 면적 1000점 → 흐린 얼굴이 이겼다 | 곱셈 계수로(흐림 ×0.55 · 선명 ×1.0) |
| 압축 노이즈 | `hd1080` 고정 | `highres/hd2160/hd1440` 있으면 요청 — 소스가 높을수록 노이즈가 적다 |
| **타이틀 카드** | "THIS YEAR" 자막 화면이 본문 사진으로 뽑혔다 (보정 전 평균 밝기 1.6) | 밝기·밝은화소 비율로 −500점 |
| **박힌 자막** | 채널명이 "소니 픽처스 영화" 인데 **일본어 자막**이 박혀 나왔다 | 아래 22%·가로 중앙 60% 의 밝은 획 비율로 감점 |
| 채널 | 호주·영국·일본이 번갈아 걸렸다 | 한국 표식 → 한글 제목 → 그 밖 순서, 아니면 경고 |

**실수 두 번, 둘 다 실측으로 잡았다.**
- 타이틀 카드 필터가 안 먹혔다 — `equalizeHist` 를 거친 `gray` 로 밝기를 재서
  거의 검은 화면도 평균이 올라갔다. **보정 전 원본**으로 재야 한다.
- 자막 임계값(1.2%)이 **오탐**을 냈다 — 밝은 아스팔트·흰 옷이 깔린 컷이 3.06% 로
  걸렸다. 실제 자막은 11~19% 라 **6%** 로 갈랐고, 판정 영역을 가로 중앙으로 좁혔다.

### ③ 사진을 많이 쓰려면 묶어야 한다 — 티스토리 2열 배치 (**실측 확인**)

사용자 요구: *"1/2/2/1/2 이렇게 좀 재밌게 배치할수있어?"*

네이버는 `imageGroup` 으로 이미 1·2·2·1 을 돌리는데 **티스토리에는 코드가 없었다.**
티스토리는 `{{IMAGE_i}}` 를 자체 매크로로 치환하므로 감싼 마크업이 살아남는지
실측해야 한다 → **비공개 글로 재봤다.**

> 결과: 표 2개 · 각 행에 이미지 2장 · 렌더 폭 382px 두 장이 같은 줄.
> (대표 644 / 단독 780 / 묶음 382×382) **티스토리가 2열 표를 그대로 살린다.**

flex 대신 표를 쓴 이유: 메일 HTML 시절부터 가장 잘 살아남는 2열 수단이고,
⑦-2 가 "`<div>` 인라인 style 은 남는다" 를 확인해 둔 것에 기댔다.

**두 곳이 어긋나 있었다.**
- 배치가 섹션마다 **한 장씩** 흩뿌려서 묶기 조건(같은 섹션 2장)이 안 만들어졌다
  → `applyClipShotLayout` 을 1·2·2·1 리듬으로.
- `html.js` 에 "3장 이하면 묶지 않는다" 가드가 섹션당 2장을 한 장씩으로 되돌렸다
  → 섹션에 온 장수를 그대로 존중(홀수면 첫 장 단독, 나머지 짝).
- 배치가 묶음 단위로 바뀌었는데 "자리 못 잡은 사진" 계산이 **묶음 수로 이미지 배열을
  자르는** 꼴이어서 사진이 중복될 수 있었다 → 지웠다(전부 자리를 받는다).

### ④ 단조 종결 — 경고만으로는 안 고쳐진다

세 번 연속 **100% · 99% · 100%** 가 그대로 나갔다. 경고는 매번 찍혔다.

→ 분량·섹션 수와 같은 급으로 **재시도 사유**로 올렸다(`section: 0` 항목일 때만 —
문단 하나의 연타로 4분을 다시 쓰는 것은 과하다). 지시문에도 60% 상한과 섞을 어미
목록을 실측 근거와 함께 넣었다(`readabilityRules`, 5모드 공통).

**그런데 재시도해도 모델은 84~92% 에서 멈춘다.** 경어체 평서를 요구받으면
`~니다` 로 수렴한다. 결국 **손으로 20곳**을 `~죠`·명사 종결로 바꿔 63% 로 내렸다.
→ 다음 후보: `lintKo` 가 세는 것을 후처리 변환기로 (규칙이 기계적이다).

### ⑤ 대표 이미지는 포스터다 — 그런데 두 번 어긋났다

- 예고편 프레임이 대표를 차지해 **퍼니셔(존 번설) 클로즈업**이 스파이더맨 글의
  얼굴이 됐다. 얼굴이 가장 크게 잡힌 컷을 고른 규칙대로였지만 주인공이 아니었다.
  → 영화 모드는 슬롯 0 을 비워 배급사 키아트에 넘긴다(`photo.js` 의 `clipStart`).
- 그 뒤 대표가 **감독 사진**이 됐다. `relatedArticlePhotos: false` 로 끄면서
  **공식 포스터 수집 경로까지 막혀** `sourceImages` 가 비었고, 위키미디어 인물
  검색이 `entities[0]`(감독)을 채웠다.
  → 이번에는 포스터를 직접 받아 `photoDir` 로 고정했다.
  **남은 일: 영화 모드가 배급사 페이지의 키아트만 좁게 가져오게 하는 것** (§8 9번)

`alt` 도 함께 어긋났다 — 대표를 포스터에 넘겼는데 `alt` 는 `"… — 0:58 장면"` 으로
캡처 기준이었다. 영화 모드는 대표 brief 를 캡처 기준으로 덮어쓰지 않는다.

### ⑥ `relatedArticlePhotos` 는 배급사 페이지에서 **다른 작품**을 물어온다

> 실측: `marvel.com` 기사(9,744자)에서 사진을 긁어 **어벤저스 둠스데이 SDCC
> 단체 사진**이 본문에 실렸다. 블랙팬서·고스트라이더도 후보에 있었다.

한 페이지에 여러 작품 사진이 섞여 있다. 클립 모드가 §7-1 사고 ① 로 이미 끈 것과
같은 이유 → 영화 모드도 `false`.

### ⑦ 자잘한 것

- 자막이 **0종**인 영상(예고편)은 클립 모드가 기사 모드로 강등되고, 기사 모드로
  유튜브 페이지를 긁으면 본문이 안 나온다 → **예고편은 영화 모드로 쓴다.**
- `lintKo` 가 `김고은` 을 "김고는" 으로 **5번** 잡았다. 경고가 시끄러우면 진짜 오류가
  묻힌다(그 글의 진짜 오류는 조사가 아니라 **이름**이었다 — 박지훈 ← 박지현).
  → `articleNames(article)` 로 그 글의 이름 목록을 넘긴다(entities·태그·표).
  오탐 5 → 0, 진짜 오류(발표은·연예은)는 그대로 잡힌다.
- 스키마 제목 형식이 `… | <감독> <제목>` 이라 **작품명이 두 번** 들어가 65자가 됐다
  → 45자 제한 + 중복 금지.
- **삭제 자동화는 하지 않았다.** 관리 화면 셀렉터를 모르는 상태에서 삭제 버튼을
  추측으로 누르면 발행된 글을 지울 위험이 있다. 실측용 비공개 글은 사람이 지운다.

**교훈 한 줄씩**: 부분만 검증하라(전체 재생성은 내용을 바꿀 때만) ·
단계가 다른 조건문 안에 얹혀 있는지 보라 · 화질은 압축이 아니라 **프레임 선택과
레터박스**였다 · 밝기 판정은 **보정 전** 값으로 · 임계값은 오탐 쪽도 실측하라 ·
경고만으로는 안 고쳐진다(재시도로 올려도 모델은 수렴한다) ·
필터를 끄면 **그 필터가 겸하던 다른 일도 멈춘다.**

---

## 7-8. 구작 영화 글 — 소재가 없을 때 무엇을 하면 안 되는가 (2026-08-01 밤)

발행: 「국경을 건너도 빚은 끝나지 않는다 — 영화 황해 줄거리 결말 (스포 O)」
`classic-m.tistory.com/entry/the-yellow-sea-2010-plot-ending`
(본문 4,863자 · 사진 9장 · 2열 표 3개 · 임베드 정상)

### ① 벤치마킹: **축 하나로 꿴다** (메모리아 리뷰)

`blog.naver.com/yellowsaucer/224364758026` — 1,494자 · 소제목 0개 · 사진 6장(249자/장)
· 캡션 0개. 여는 인용이 **"눈으로 보기보다 귀로 듣는 영화"** 한 줄이고 그 뒤 모든
문단이 소리였다. 짧은데 남는다.

우리 글은 일곱 갈래로 흩어져 있었다. 그런데 **축의 씨앗은 이미 출처에 있었다** —
AP "외로운 슈퍼히어로", GQ "Peter Parker's Emotional Growth", 우리 프롤로그
"사람 많은 거리에서 혼자". 선언하지 않아 흩어진 것이다.

→ 스키마에 `angle` 신설(30자, required). `keyTakeaways` 처럼 본문에 실리지 않는
**선언용 필드**로, 프롤로그 마지막 문장과 해석의 닫는 판단 **두 곳에서만** 회수한다.
줄거리·인물까지 억지로 꿰면 정보가 흐려진다. 사진 밀도 목표도 **250자당 1장**으로
정정했다(이전 82자/장은 스틸 19장짜리 다른 글의 수치였고 전부 배급사 저작물이다).
자세한 것은 `.claude/skills/benchmark-post/references/learned.md` 2026-08-01 항목.

### ② "평론가 느낌" 은 **본 것을 자랑하는 게 아니다**

사용자 요구: *"영화 유명 평론가가 쓴 느낌이 있어야해, 줄거리·내용·후기·내생각 완벽하게"*

이름난 평론가의 권위는 남보다 많이 본 데서 오지 않고 **계보에 놓고 판단하는 데서**
온다. 그건 보지 않고도 할 수 있다. 목소리 `이름난 평론가` 를 만들어
`config.movie.voice` 로 **고정**했다(해시로 셋 중 하나가 아니라 영화 글 전부).

글의 성격도 **두 층**으로 다시 규정했다.
- **1층**(줄거리·영화 정보·등장인물) — 문체를 자랑하지 않고 사건만. 독자는 "무슨 일이
  일어나는지" 를 알려고 온 사람이다.
- **2층**(프롤로그·해석·포인트) — 여기서 평론가가 된다. 해석 섹션을 가장 길게 쓰고
  **예고편에서 본 것 → 계보 비교 → 포기한 것 → 평론의 합의(귀속) → 판단 한 문장**
  순서로 쌓는다. 포인트에는 **안 맞는 관객을 반드시** 쓴다.
- 섞으면 둘 다 죽는다.

**금지 목록만 적으면 몸을 사려서 후기가 사라진다.** 그래서 쓸 수 있는 것을 같은 분량
으로 적었다 — 예고편(우리가 실제로 본 1차 자료) · 계보와 비교(필모그래피는 사실) ·
구조에 대한 판단 · 평론의 합의 · 누구에게 맞는가. 평론의 재료는 이 다섯이 전부다.
못 쓰는 것은 **체험을 주장하는 문장**뿐이다("숨을 죽였다", "객석이 조용해졌다").

결과(황해): 축 = 빚. 프롤로그 "빚이 사람을 국경 밖으로 몹니다" → 해석 "추적의 끝에
남는 것은 빚" → 맺음 "끝내 갚아지지 않는 누아르의 빚". 계보(추격자·곡성, 하정우·
김윤석 역할 반전), 포기한 것("퍼즐을 풀려 많은 인물을 투입한 대가"), 평론 합의
(씨네21·가디언 귀속)까지 다 섰다.

### ③ 스톡 사진으로 때우면 **바로 들킨다**

황해 예고편은 유튜브 **최대 480p**(리마스터 없음)라 캡처가 거부됐다. 빈 자리를
빈 객석·영사기·밤 항구 같은 스톡으로 채웠더니 사용자가 즉시 잡았다 —
*"아.. 사진을 이따구로 쓸래?"*

> 그리고 대표 이미지는 **나홍진 감독의 위키미디어 사진**이 됐다. imageBriefs 에
> `placement:'thumbnail'` 항목이 아예 없어 코드가 첫 사진을 대표로 삼았다.

**사용자 결정: 구작은 그 영화 화면이 1080p 로 올라온 영상에서 뜬다.**
홍보물인 예고편과 달리 **작품 자체의 화면**이라 위험이 한 단계 높다 — 방송 클립을
쓰는 클립 모드와 같은 기준이고, 위험은 발행자가 진다(§6).

⚠️ **요약·리뷰 채널은 자체 내레이션 자막이 화면 하단에 크게 박혀 있다.**
> 실측: 지무비 영상에서 "흥미롭게 바라봅니다..?", "살벌한 작업기한이 제시됩니다..!"
> 가 빨간 글씨로 박혀 나왔다. 그대로 쓰면 남의 요약 영상에서 가져온 티가 난다.
> `pick_face_frame.py` 의 자막 감점은 여기서 무력하다 — **모든** 후보에 자막이 있어
> 상대 순위가 바뀌지 않는다.

→ **하단 24% 를 잘라낸다.** 1920×620~700 이 남고 화질은 충분하다. 영화 자체 자막
(조선족 대사 번역)도 같이 사라진다. 지금은 손으로 자른다 — 자동화는 §8 후보.

### ④ 단조 종결 — 결국 **코드가 섞는다** (`src/endings.js`)

경고 → 재시도로 올려도 모델은 수렴한다: **100% · 99% · 84% · 93%**.
황해는 재시도 포함 10분 30초를 쓰고 93% 였다. 재시도는 4분을 더 태우고 결과도
나아지지 않는다. 그런데 `~습니다` → `~죠` 는 **뜻이 바뀌지 않는 기계적 변환**이다.

바꾸지 않는 것이 더 중요하다:
- **안전한 일곱 형태만** — `습니다`·`입니다`·`합니다`·`됩니다`·`아닙니다`·`옵니다`·`봅니다`.
  `갑니다`(가다/갈다)·`줍니다`(주다/줄다)·`삽니다`(사다/살다)·`만듭니다`(ㄹ 불규칙)는
  **원형이 둘이라 모르면 반드시 하나가 틀린다** (만드죠 ✗ 만들죠).
- 큰따옴표 안은 건드리지 않는다 — 남의 말을 우리가 고치면 인용이 아니다.
- 목표(60%)를 이미 만족하면 아무것도 하지 않는다.

**후보를 글 전체에서 모아 고르게 흩는다.**
> 실측 — 앞에서부터 예산을 쓰니 30곳을 바꿔 89%→60% 를 맞췄는데 줄거리·결말만
> 바뀌고 **정작 해석 섹션은 한 문장도 안 바뀌었다.** 줄거리는 건조해야 하는 1층이고
> 리듬이 필요한 곳은 2층이다 — 정확히 거꾸로였다.
결과: 해석 섹션이 가장 많이 받는다(8곳). 황해 89% → **60%**, lint 0/0.

### ⑤ 겉으로 티가 안 나는 고장 — 스키마 필드가 **버려지고 있었다**

`normalizeArticle` 은 고정된 모양을 만든다. 그래서 스키마에 필드를 추가해도 그 함수에
한 줄을 안 넣으면 모델이 채워 보낸 값이 조용히 사라진다.

> `spoiler` 는 영화 스키마 required 에 **처음부터** 있었는데 발행된 글 두 편 모두 그
> 키가 없었다. 제목의 "(스포 O)" 는 지시문이 시켜서 됐던 것이라 **겉으로는 아무 문제가
> 없어 보였다.** 같은 날 추가한 `angle` 도 똑같이 사라졌다.

→ `npm run doctor` 가 **스키마 required 와 아티클 실제 모양을 대조**한다. 키 목록은
`articleShapeKeys()` 가 빈 입력으로 한 번 돌려서 얻는다 — 옮겨 적으면 그것부터 낡는다.

### ⑥ 그 밖에

- **결말이 글 맨 위에서 새고 있었다.** 「이 글의 핵심」(keyTakeaways) 상자는 줄거리보다
  **위**에 렌더링되는데 거기에 "구남이 귀환선에서 죽고…" 가 들어 있었다. 스포 O 라도
  이 상자에는 결말을 쓰지 않는다 → 스키마에 명시.
- **제목**: 사용자 지적 *"독자들이 안눌러볼거같음"*. 검색어를 뒤로 돌리고 축을 앞세웠다.
  `영화 황해 정보 (스포 O) 줄거리 결말 | 나홍진`
  → `국경을 건너도 빚은 끝나지 않는다 — 영화 황해 줄거리 결말 (스포 O)`
  검색어는 그대로 살아 있고 앞에 훅이 선다. 제목과 본문이 같은 말을 한다.
- **소제목의 작품명은 세 개까지** (영화 정보·줄거리·결말). 일곱 개 전부에 넣어 같은
  이름이 화면에 일곱 번 찍혔다 — 발행 전에 손으로 줄인 것을 규칙으로 굳혔다.
- **김윤석 사진은 쓰지 않았다.** 위키미디어에 있는 유일한 김윤석 사진이 성희롱 발언
  공개 사과 기자회견 컷이고 **파일명에 그게 박혀 있다.** 출처를 따라간 독자에게는 우리가
  그것을 연결한 것처럼 보인다. 나머지 후보는 전부 **김혜윤**(다른 사람)이라 §7-6 의
  이름 필터가 걸렀다. → 공식 포스터에 두 배우가 다 있어 그것으로 해결했다.
- **`config.movie.voice` 는 죽은 키였다** — `pickVoice` 가 `article.movieVoice` 만 읽었다.
  설정 파일에 아무 것도 하지 않는 칸이 있으면 다음 사람이 채우고 왜 안 되는지 찾는다.
- **`--spoiler` / `--no-spoiler`** 실행별 플래그. 신작은 스포 X, 구작은 결말 검색 수요가
  커서 스포 O — 전역 토글만 있으면 뒤집고 되돌리는 것을 잊는다.
- **`scripts/repreview.mjs`** — 집필 없이 사진·배치만 다시 그린다. 오늘 이 작업을 세 번
  하면서 그때마다 3~10분짜리 집필을 다시 태웠다. **사진은 글을 한 글자도 바꾸지 않는다.**
- **python 으로 소스를 고칠 때 줄 끝을 보라.** 이 레포는 LF 로 저장하는데(`core.autocrlf
  =false`, `.gitattributes` 없음) `io.open(p,'w')` 가 CRLF 로 바꿔 7개 파일이 통째로
  diff 로 잡혔다(2,720줄). `newline=''` 로 읽고 쓸 것.

**교훈 한 줄씩**: 짧은 글이 남는 이유는 **축이 하나**여서다 · 평론가의 권위는 계보와
판단에서 온다(본 것에서 오지 않는다) · 금지만 적으면 후기가 사라진다 ·
**관계없는 스톡은 없는 것보다 나쁘다** · 자막 감점은 모든 후보에 자막이 있으면 무력하다 ·
모델이 세 번 못 하면 그건 **코드가 할 일**이다 · 변환은 앞에서부터가 아니라 고르게 ·
**겉으로 티가 안 나는 고장**은 자동 대조로만 잡힌다.

---

## 7-9. 하네스 — 형식을 사람이 정하지 않게 (2026-08-01 밤)

> 사용자 지적: *"글마다 계속 컴펌을 하여서 형식이 달라지고, 좋지 못한 구성이 되어지고 있음"*

정확한 진단이다. **형식이 흔들린 원인은 규격이 코드에 없었기 때문**이다.
`config.json` 은 모드 구분 없이 한 벌(`minChars:3000`·`sectionCount:7`·`bodyImages:5`)만
갖고 있었고, 모드마다 딴 데서 덧칠했다 — 책은 `codexWriter` 가 `bodyImages+2`,
영화는 사람이 매번 손으로. 그래서 글마다 확인을 받아야 했고, **확인받는 순간 그 글만의
형식**이 됐다.

### ① 세 가지를 분리한다 (`src/contract.js`)

- `measure()` — **재기만** 한다. 판단하지 않는다.
- `checkContract()` — 규격에 **어긋난 것만** 돌려준다. 통과 항목은 조용하다.
- `autoFix()` — **기계적으로 확실한 것만** 고친다. 애매하면 손대지 않고 경고로 넘긴다.

규격은 `src/modes/<id>.js` 의 `contract` 가 갖는다 — 모드에 관한 사실은 한 곳이라는
기존 원칙 그대로다. 값은 **발행 실측 8편에서 뽑았다** (지어내지 않았다).

| | 자 | 사진 | 자/장 | 캡션 | 소제목 작품명 |
|---|---|---|---|---|---|
| 책 | 2850~3400 | 6~9 | 300~450 | free | **0 (금지)** |
| 영화 | 3400~4300 | 6~16 | 250~420 | fact-only | **0~3** |
| 영상 | 2650~3300 | 12~20 | 130~230 | fact-only | — |
| 기사 | 2800~3600 | 5~9 | 300~500 | free | — |
| 주제 | 2800~4000 | 5~9 | 300~500 | free | — |

`막음`(sections·photos·endingMax·captions)이 있으면 발행하지 않는다. `--force` 가 있지만
**쓰는 습관이 들면 게이트가 없는 것과 같다.** 규격에 걸리면 글을 예외 처리하지 말고
**규격을 고치는 것**이 맞다.

### ② 재는 단위를 하나로 못박는다

`flowChars` = 화면에 보이는 본문 전부. `codexWriter.articleCharCount` 와 **같은 값**이다.

> 실수 — 사진 밀도를 "문단+불릿" 으로 재서 황해가 241자/장 이라고 보고했다.
> 참고 글 249는 `.se-main-container` innerText(보이는 글 전부)를 센 값이다.
> 같은 기준이면 3,992 ÷ 9 = **443자/장** — 참고 글의 절반 밀도였다.
> **유리한 쪽 기준을 골라 쓰면 규격이 아니라 자기 위안이다.**

### ③ 감사에서 나온 여섯 — 하네스를 만든 뒤 스스로 검사했다

| 문제 | 영향 |
|---|---|
| 게이트가 `npm run publish` 에만 있었다 | **주력 명령 `post`·`queue` 가 그대로 통과** |
| `headingWorkTitle` 이 항상 0 | 책 모드 "소제목에 제목 금지" 가 **죽은 규칙** |
| 작품명 비교에 문장부호 미제거 | `스파이더맨:브랜드뉴데이` ≠ `스파이더맨브랜드뉴데이` |
| 영화 규격 `photos:[9,16]` | 공급이 6장 한계 → **영화 글 매번 막힘** |
| `autoFix` 가 캡션을 **전부** 삭제 | 자막으로 확인되는 좋은 캡션 18개까지 |
| 규격 검사가 `noSpoilerIn` 을 숫자 범위로 오해 | 다섯 모드 전부 오탐 |

**게이트 우회가 제일 컸다.** 발행 경로가 셋(`runTopic` ×2, `cmdPublishFile` ×1)인데 한
곳에만 뒀다. 검사와 출력을 `assertContract()` **한 벌**로 모으고 두 경로가 그것을 부른다.
복사해 두면 또 갈라진다.

**죽은 규칙 둘은 같은 종류다.** 책 모드 `topic` 은 취재 재료가 붙은 수천 자짜리 문자열인데
첫 줄만 보지 않아 `workTitle` 이 거대해졌고, 어떤 소제목도 포함하지 않아 **항상 통과**했다.
`pickVoice` 가 이미 "첫 줄만" 으로 피한 함정이다. 문장부호 건은 §7-7 의 하이픈 사고
(`Hwang Jung-min` vs `Hwang Jung-Min`)와 판박이다 — **비교 전 정규화는 양쪽 다.**

**캡션은 과했다.** `verified-only` 로 전부 지우게 했는데, 지시문은 좋은 예·나쁜 예까지 들어
캡션 품질을 끌어올리는 중이었다. 코드가 통째로 버리면 그 작업이 헛돈다. 문제는 캡션의
**존재**가 아니라 **빈 말**이었다 → §7-6 에서 지운 19개에서 패턴을 뽑아 `fact-only` 로.
발행된 클립 글의 캡션 18개는 하나도 안 걸린다.

**영화 규격은 정직하게 낮췄다.** 목표(249자/장)를 하한으로 삼으면 매번 막힌다.
하한은 공급이 닿는 6으로 두고 간격은 `photoDensity` **경고**가 계속 가리킨다.
브리프 수만 채우면 빈 그라디언트 카드가 생기고 — **그건 게이트를 통과하는 더 나쁜 상태**다.

### ④ `CLAUDE.md` 를 만들었다 — 읽히지 않으면 없는 것과 같다

이 저장소에는 **자동으로 읽히는 파일이 하나도 없었다.** 그래서 오늘도 "HANDOVER 읽을까요?"
를 묻고 나서야 읽었다. 그 한 번을 건너뛰면 66,000자의 기록이 전부 없는 것과 같다.

`CLAUDE.md` 는 **짧게, 가리키기만** 한다 — 긴 것을 옮겨 적으면 그것부터 낡는다.
발행 전 §6, 하지 말 것, 규격이 형식을 정한다는 것, 부분만 검증한다는 것, 모드를 건드리는 법.

### ⑤ 배운 것이 어디까지 갔는지 (자체 감사)

기록은 강하고(HANDOVER 66k · learned.md 9k), 집행은 절반이고, **적재는 없었다.**
오늘 코드로 승격한 것: 단조 종결 · 스키마 필드 누락 · 화질 게이트 · 이름 오탐 ·
조작된 인용 · 그리고 이 규격 게이트. 아직 산문뿐인 것: 몽타주 걸러내기 · 하단 자막 크롭.

**교훈**: 산문 규칙은 어겨져도 소리가 안 난다 · 검사를 만들었으면 **검사부터 검사한다**
(오탐 하나로 다섯 모드가 막혔다) · 규격은 **파이프라인이 닿는 값**으로 (닿지 않으면
`--force` 습관만 생긴다) · 자동 교정은 **고치지 않을 것을 정하는 편**이 더 중요하다.

---

## 7-10. 모델이 목차를 지어냈다 · 스톡이 남의 얼굴을 물어왔다 (2026-08-03 · 『독서의 기술』)

사용자가 책을 직접 지정한 글. 게이트는 **경고 하나(글자 수 미달)만** 냈고 통과했다.
그런데 사람이 읽으니 **사실 5건이 거짓**이었고 **사진 7장 중 5장이 못 쓸 것**이었다.

### ① 게이트가 통과시킨 거짓

| 초안 | 실제 |
|---|---|
| "핵심 표현은 '전투독서'" | 이 책 목차·발췌·서평 어디에도 없는 말 |
| 목차에 '휴가철 독서법' | 없음 |
| 목차 끝 '고명환이 지금 하고 있는 독서법' | 실제 끝은 '고전을 통해 질문 던지기' |
| FAQ '읽다 만 책 읽는 법' | 없음 |
| callout "책은 마법의 양탄자다…" | 미검증 인용 |

'전투독서'가 가장 나빴다. 저자의 **다른 책·인터뷰**에서 온 말로 보이는데, 모델이
이것을 이 책의 **핵심 표현**으로 세우고 줄거리·생각·FAQ·이미지 브리프까지 그 말로
채웠다. 축이 거짓이면 문장을 아무리 고쳐도 글이 서지 않는다.

→ `prompt.js` 에 **"목차와 핵심어 — 실제로 적혀 있는 것만"** 블록을 넣었다.
   꼭지 제목은 실제 목차에서만, 못 찾으면 **부 구성과 분량만** 말하고 이름은 쓰지 않는다.
   낱말이 목차·발췌·소개에 없으면 **이 책의 용어가 아니다.**

### ② photoQuery 가 사람과 책을 부르면 안 된다

- `author speaking audience` → **처음 보는 외국인 남성**의 강연 스톡. 캡션은
  "고명환의 이력을 함께 본다" 였다. **실존 인물 오귀속**이고 되돌릴 수 없다.
- `book pages window light` · `reading nook warm light` → 둘 다 **꾸란** 사진.
  펼친 책 스톡은 다른 나라 경전과 외국어 책이 가장 많이 걸린다.
- 그리고 그라디언트 문구 카드 1장 (스탯 카드 금지 규칙을 우회해 들어왔다).

→ `prompt.js` 책 모드에 **사람이 주인공인 photoQuery 금지**, **펼친 책 검색어 주의**
   를 실패 실측과 함께 넣었다. 저자 사진은 **sources 의 보도사진에서만** 온다.

### ③ 서점 미리보기가 최고의 재료였다 (새로 배운 것)

알라딘 `letslook/*_b.jpg`(뒤표지) · `_t1..t6.jpg`(본문 미리보기)는 **실물 스캔**이다.
여기서 뒤표지 문구, 제사 페이지(비트겐슈타인), 들어가며 첫 장을 얻었고, 그것이
**검증된 발췌이자 쓸 수 있는 사진**이었다. 표지 띠지 문구도 마찬가지다
(프롤로그 callout 을 여기서 확인했다). `prompt.js` 에 이 지침을 넣었다.

최종 사진 8장: 표지 · 뒤표지 · 제사 · 들어가며 · 본문 7쪽 · 작가 카드 · 글귀 카드 ·
강연 현장(전자신문 보도사진). **스톡은 0장이다.**

### ④ 남는 문제

- 게이트는 **모양**을 본다. 글자 수·사진 수·섹션 수는 셌지만 **목차가 진짜인지**는
  못 본다. 기계로 잡으려면 sources 의 상품 페이지 목차와 본문의 꼭지 이름을 대조하는
  검사가 필요하다 (§8 후보).
- 표지 지배색 추출은 `cv2.imread` 가 **한글 경로를 못 읽는다.**
  `np.fromfile` + `cv2.imdecode` 를 써야 한다. (독서의 기술 = `#e7e7e7` 88% / `#e60419` 7%)

> 발행: <https://blog.naver.com/web_dev5/224366351040>

---

## 7-11. 검색이 끊긴 날 — 규격이 틀이 됐고, 코드가 조용히 잘랐다 (2026-08-03)

발행 5건(네이버 3 · 티스토리 2) 뒤 사용자가 "네이버가 갑자기 다 죽었다" 고 알렸다.
그날 하루의 기록이다.

### ① 진단 — 책 때문이 아니었다

글 **제목 전체**로 검색해 색인 여부를 쟀다(`scripts/check-index.mjs`).
처음엔 책 제목으로 재서 틀렸다 — 그건 순위를 잰 것이지 색인이 아니다.

```
노출됨  07-29  프로젝트 헤일메리      ← 책 글인데 노출
노출됨  07-29  투명한 나선           ← 책 글인데 노출
──────────────── 여기서 잘림 ────────────────
누락    07-29  채영 …               ← 연예 글도 누락
누락    07-30 ~ 08-03  10건 연속 누락
```

07-21 이전 글(ETF·맛집·넷플릭스)은 정상이다. **주제 문제가 아니고 날짜로 잘렸다.**

### ② 우리가 보낸 신호 — 규격이 틀이 됐다

자가진단(`out/*.json` 9건)에서 나왔다.

| 항목 | 실측 |
|---|---|
| 소제목 구조 | 9건 중 **8건이 완전히 동일** (프롤로그>목차>줄거리>생각>기본 정보>결말>포인트) |
| FAQ | 9건 **전부 5개** |
| 표 | 9건 **전부 1개** |
| 소제목의 검색어 | **0개** |

우연이 아니라 우리가 강제한 것이다 — `sections:[7,7]` · `faq:[5,5]` · `tables:[1,1]` ·
`headingWorkTitle:[0,0]`(책 제목 **금지**). 그리고 지시문은 이렇게 적고 있었다:

> 소제목이 문학적이어서 잃는 검색 노출은 title·seoTitle·metaDescription·FAQ 가
> 대신 짊어집니다.

**이 판단이 틀렸다.** 참고 글 7편은 예외 없이 소제목이 검색어였다.

> **교훈 — 하한과 상한을 같게 두면 규격이 아니라 틀이다.**
> CLAUDE.md 의 "형식은 사람이 정하지 않는다" 는 품질 관리로는 옳았지만,
> 같은 뼈대를 하루 2건씩 6일 쌓으면 검색엔진에는 "찍어내는 블로그" 로 읽힌다.
> 일관성이라고 부른 것이 저쪽에선 패턴이다.

→ `[6,9]` · `[3,6]` · `[0,2]` · `headingWorkTitle:[1,3]` 로 풀고,
   지시문의 소제목 규칙을 뒤집었다(「책 제목 + 독자가 찾는 것」).

### ③ 지시문을 고쳐도 안 되던 것 — **코드가 자르고 있었다**

태그가 매 글 8개에서 멈췄다. 지시문·스키마·규격을 다 고쳐도 8개였다.

```js
// codexWriter.js — 범인
.slice(0, Math.max(1, cfg.article.tagCount));   // = 8
```

learned.md 가 영화 모드에서 "태그가 안 붙었다" 며 규격 하한을 10으로 올린 것도
증상 대응이었다. → **모드 규격의 상한**으로 자르게 고쳤다.

그런데 발행해 보니 15개 중 10개만 붙었다. **두 번째 병목**이 있었다 —
`naver.js` 가 `cfg.naver.tagCount`(=10)로 또 잘랐다. 같은 자리에 두 개.

> **교훈 — 지시문이 안 먹히면 규격을 의심하고, 규격도 안 먹히면 코드를 의심한다.**
> 그리고 자르는 곳은 하나라고 믿지 말 것.

### ④ 조용히 틀리던 것들 (검토 요청에서 나옴)

- **`out/` 이 통째로 gitignore** 라 `photoDir` 이 기기 간에 안 따라왔다.
  폴더가 없으면 **조용히 스톡으로 대체**됐다 → 둘 다 멈추게 하고 `out/photos/*/` 추적.
- **네이버에 발행 후 검증이 없었다.** 티스토리에만 있었는데 **수정이 불가능한 쪽은
  네이버다.** → `naver.js`에 추가. 첫 실행에서 `본문 4,099자 · 이미지 9장` 확인.
- **발행 때 스톡을 다시 검색**해 검토한 사진과 달라질 수 있었다 →
  `repreview --pin`. 재렌더가 2분 → 7초가 됐다.
- **티스토리 주 경로가 매번 죽고 있었다** — `기본모드 반영이 부족(0자)` 뒤 폴백으로만
  발행됐다. 측정기는 멀쩡했고 고정 대기 2초가 일렀다 → 10초 폴링.
- **위키미디어 허위 라이선스** — KBS 로고와 자막이 그대로인 방송 캡처가 CC BY 3.0
  으로 올라와 있었다. 방송사명만으로 막았더니 이번엔 'MBC Korean Music Wave'
  관객 사진이 오탐으로 걸렸다 → 스튜디오 프로그램은 단독으로, 방송사는 캡처
  낱말과 함께일 때만. **검사를 만들었으면 검사부터 검사한다.**
- **죽은 사람의 공식 SNS** — 세네카(기원후 65년 사망) 글에 @seneca_theyounger 가
  붙었다. 팬 계정이 인물 이름을 그대로 써서 FAN_PATTERN 이 못 잡는다 →
  `entities.historical` 신설(스키마 3종 + normalizeArticle + socialEmbed).
- **책 callout 을 아무도 대조하지 않았다** → `scripts/verify-callouts.mjs`.
  만들면서 둘 더 잡았다: **따옴표 없는 callout 이 검사를 통째로 빠져나갔고**,
  출처를 한 덩어리로 합치면 개인 블로그에서만 일치한 인용도 통과했다.
  그리고 '없음' 은 유죄가 아니다 — 우리가 쓰는 가장 좋은 발췌는 뒤표지·제사 페이지
  **이미지**에서 눈으로 읽은 것이라 텍스트 검사에 안 잡힌다.

### ⑤ 프롬프트를 쪼갰다

`prompt.js` 1,470줄에 다섯 모드가 엉켜 있었다. 책 모드는 발행이 가장 잦고 규칙이
가장 자주 바뀌는데 고칠 때마다 다른 모드를 넘어가야 했다.

```
src/prompts/shared.js   공용 규칙 6개 (모드 무관)
src/prompts/book.js     책 지시문 전부
src/prompt.js           962줄 · book 은 재export
```

문학책 뼈대도 사용자 지정으로 바꿨다 — **책소개 → 줄거리 → 감상평 → 독자가 찾는 것**
(+ 서지 표 필수, 선택 자리에서 골라 6~9개).

### ⑥ 아직 안 고친 것 — **책 글 소제목이 진짜 소제목이 아니다**

`naverDoc.js` 는 책 글의 소제목을 `sectionTitle` 이 아니라 **인용구**로 낸다
(2026-07-29 사용자 요청 — "왼쪽에 인용구로 세련되게"). 주석에도 이렇게 적혀 있다:

> 대신 sectionTitle 의 GEO(목차 인식)는 잃는다 — title·meta·FAQ 가 짊어진다.

**이것이 ②에서 틀렸다고 판정한 바로 그 논리다.** 소제목에 검색어를 넣게 고쳤는데,
책 글에서는 그 소제목이 네이버의 문서 구조로 나가지 않는다. 절반만 전달된다.
사용자의 디자인 결정이라 임의로 바꾸지 않았다 — **결정이 필요한 항목이다.**
선택지: ⓐ sectionTitle 로 교체 ⓑ sectionTitle + 아래에 lead 인용구 ⓒ 현행 유지.

---

## 7-12. 티스토리 주 경로 — 진단을 두 번 틀렸다 (2026-08-03)

`setBody` 는 HTML 모드로 본문을 넣고 기본모드로 되돌려 위지윅에 반영시킨다.
그런데 **매 발행마다** 이렇게 찍힌다.

```
WARN  기본모드 반영이 부족합니다 (본문 0자). 위지윅 직접 입력으로 재시도합니다.
OK    본문 입력 완료 (위지윅 직접, 본문 6,786자)
```

즉 **주 경로가 한 번도 성공한 적이 없고 폴백으로만 발행되고 있다.** 폴백이
깨지는 날 빈 글이 나간다.

### 틀린 진단 두 개 (반복하지 말 것)

1. **"타이밍이다"** — 고정 대기 2초가 이르다고 보고 10초 폴링으로 늘렸다.
   결과: **10초를 기다려도 0자.** 타이밍이 아니다.
2. **"측정 경로가 문제다"** — `wysiwygTextLength` 의 `frameLocator` 가 새로 붙은
   iframe 을 못 잡는다고 보고 TinyMCE API(`activeEditor.getContent`) 폴백을 넣었다.
   결과: **iframe 도 0, TinyMCE API 도 0.** 측정 경로가 아니다.

폴백으로 위지윅에 직접 쓴 **뒤에는** 같은 함수가 6,786자를 정상으로 읽는다.
그러므로 측정기 자체는 멀쩡하다.

### 남은 가설 (아직 확인 안 함)

**본문이 실제로 안 들어간다.** `writeHtmlMode` 로 HTML 은 넣었는데 기본모드로
전환할 때 티스토리가 그것을 위지윅 모델로 파싱하지 못하는 것. 그렇다면 0자는
정확한 측정이고, 주 경로는 원래부터 동작한 적이 없다.

→ **확인 방법**: 발행하지 말고 `npm run probe` 로 에디터만 열어, HTML 모드에
   내용을 넣고 기본모드로 전환한 **직후의 DOM 을 실제로 떠 본다.**
   추측을 세 번째로 쌓지 말고 여기서 사실을 볼 것.

→ 가설이 맞으면 고칠 것이 아니라 **인정하고 정리**한다 — 매 발행마다 4초를
   버리고 경고를 찍을 이유가 없다. `setBody` 를 위지윅 직접 입력 우선으로 바꾼다.

---

## 8-0. 지금 멈춰 있는 것 (2026-08-03)

**네이버 발행 중단 중.** 07-30 이후 발행분 10건이 검색에서 통째로 누락됐다(§7-11).
재개 기준은 코드에 있다 — `node scripts/check-index.mjs` 가 **최근 3건 연속 노출**이면
재개하라고 찍는다. 2026-08-03 현재 최근 3건 중 0건이다. 티스토리는 별개로 계속한다.

**발행 대기 중인 글** — `out/pending/*.json` (이것만 git 에 올린다)
- `20260803-121055-명화남녀…` — 검토·규격 통과·사진 8장 고정 완료. 색인 회복되면
  `npm run publish -- "out/pending/<파일>" --naver` 로 그대로 내면 된다.

**먼저 확인해야 하는 것**: 네이버 서치어드바이저(searchadvisor.naver.com)의
수집·색인 상태. 여기서 "수집 제외" 가 뜨는지 아닌지에 따라 다음 판단이 갈린다.
이건 사람이 로그인해야 볼 수 있다.

---

## 8. 다음 작업 후보 (우선순위 순)

1. ~~**X·인스타그램 공식 임베드 추가**~~ **완료 (2026-07-28~30)** —
   `src/socialEmbed.js` 구현·검증 끝. X 는 안정적이고, 인스타는 프로필 그리드
   폴백까지 들어갔다 (함정 ⑪ · §7-1 남은 과제 3). 이 항목은 §7 표를 보세요.

2. **작업 스케줄러 실제 검증**
   `scripts/register-task.ps1`로 등록 후 실제 시각에 발행되는지 확인.
   로그오프 상태에서도 되는지(`-RunWhenLoggedOff`)가 관건입니다.

3. **무료 이미지 API 키 등록**
   `.env`에 Pexels/Unsplash/Pixabay 키를 넣으면 사진 확보가 2분 → 몇 초로 줄고
   성공률도 크게 올라갑니다. 전부 무료 발급입니다.

4. ~~발행 후 검증 단계 추가~~ **해결 (2026-07-30)** — `tistory.js: verifyPublished()`.
   발행 직후 글을 다시 열어 본문 글자 수(기준 1,000자)와 이미지 개수를 확인하고,
   부족하면 스크린샷을 남기고 WARN 을 찍는다. 발행을 되돌릴 수는 없으므로
   경고만 한다 — 로그의 `발행 검증` 줄이 WARN 이면 글을 열어 봐야 한다.

5. **예약 발행 검증**

6. **영화 모드로 한 편 뽑아 확인** (2026-08-01 신설, **검증 미완**)
   `npm run draft -- "영화: 제목 (감독)"`. 만들었지만 아직 한 편도 생성하지 않았다.
   확인할 것: 제목이 검색 질의형인가 · 소제목에 작품명이 들어갔나(책 모드와 반대) ·
   `spoiler` 갈래가 제목 표기와 맞나 · 예고편 임베드를 2~4개 찾았나 ·
   스톡이 영화 장면을 재현하려 하지 않나. 자세한 설계 근거는 §7-5 ⑤.

7. **드라마 모드 활성화** (선언만 있음 — `src/modes/drama.js`, `enabled: false`)
   회차 리캡. 남은 일: `buildDramaPrompt` · `drama.schema.json` 작성 후
   `enabled: true`. 스키마는 손으로 복제하지 말 것 — §7-5 ② 의 "책 어휘 3곳" 사고가
   그 방식에서 나왔다. 2단계로 계획한 **스키마 생성기**(공통 골격 + 모드별 override)를
   먼저 만드는 것이 낫다.

8. **`looksLikeMontage()` 가 좌우 2분할을 못 잡는다** (§7-6 ④)
   비율만 보므로 1.5 짜리 2분할이 통과한다. §7-3 이 자동 판정 **두 번 실패**를
   기록해 뒀으니(중앙 이음새·로고 반복) 같은 방향으로 또 시도하지 말 것.
   지금은 `grab-source-photos.mjs` 로 사람이 고른다.

---

## 9. 문제가 생겼을 때

```powershell
npm run verify                          # 로그인 화면 셀렉터 점검 (계정 불필요)
npm run probe                           # 에디터 구조 덤프 → logs/probe-*.json
npm run post -- "주제" --show --verbose  # 브라우저 띄우고 상세 로그
Get-Content logs\2026-07-27.log -Tail 60
```

- `logs/shots/` : 발행 직전 화면과 실패 시점 스크린샷
- `logs/probe-*.json` : 에디터의 버튼·입력창 id 목록
- 로그인이 자꾸 막히면 → `npm run login`으로 직접 한 번 통과
- 본문이 비어 발행되면 → 위 ③번 함정. `setBody`의 위지윅 주입 경로를 확인

---

## 10. 인수인계 시 함께 전달할 것

- 카카오 계정 정보 (**이 저장소에는 없습니다.** 별도 안전한 경로로 전달)
- 대상 블로그 주소와 카테고리 이름
- 발행해 둔 테스트 글이 있다면 정리 여부

> 이전 작업 과정에서 계정 정보가 대화 기록에 남았습니다.
> **인수인계 전에 카카오 비밀번호를 변경하는 것을 권장합니다.**
