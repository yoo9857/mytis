# mytis — 티스토리·네이버 자동 발행

블로그 두 곳에 글을 쓰고 발행한다. 소재에 따라 **모드**가 갈리고, 모드마다 지시문·스키마·
사진 공급·출력 규격이 다르다.

## 먼저 읽을 것

- **`HANDOVER.md` §6** — 저작권과 실존 인물 보호선. 발행 전에 반드시.
- **`HANDOVER.md` §8** — 다음 작업 후보. 새 일을 시작하기 전에.
- §7-1~§7-8 은 사고 기록이다. **필요할 때 찾아 읽는다** (전부 읽을 필요 없다).

## 절대 하지 말 것

- `.env`(카카오·네이버 계정)와 `profile/`(로그인 쿠키)을 **커밋하지 않는다.**
- 발행된 글은 **수정할 수 없다** (편집 진입 URL이 전부 막혀 있다 — 실측).
  새로 발행하고 옛 글을 지우는 수밖에 없다. **그러니 발행 전에 다 잡는다.**
- 관리 화면 셀렉터를 모르는 채로 삭제 버튼을 추측해 누르지 않는다.

## 형식은 사람이 정하지 않는다 — 모드 규격이 정한다

글마다 확인을 받으면 그 글만의 형식이 된다. 그래서 규격을 코드에 뒀다.

- 규격은 `src/modes/<모드>.js` 의 `contract` 가 갖는다. 값은 **발행 실측**에서 뽑았다.
- `src/contract.js` 가 재고(`measure`) 대조하고(`checkContract`) 기계적인 것만 고친다(`autoFix`).
- **세 발행 경로가 모두** `assertContract()` 를 지난다. `막음` 항목이 있으면 발행되지 않는다.

```
npm run gate -- out/<글>.json          규격 대조 (발행하지 않음)
npm run gate -- out/<글>.json --fix    기계적으로 고칠 수 있는 것만
npm run gate -- out                    폴더 전체 요약표
npm run doctor                         모드 선언·스키마·규격 정합
```

규격에 걸리면 **글을 예외 처리하지 말고 규격을 고친다.** 예외를 글마다 두면 원점이다.
`--force` 는 있지만, 쓰는 습관이 들면 게이트가 없는 것과 같다.

## 검증은 부분만 한다

집필은 3~10분이다. 사진·배치·화질은 **글을 한 글자도 바꾸지 않는다.**

```
npm run repreview -- out/<글>.json     집필 없이 사진·배치만 다시 그린다
```

파이썬 판정(프레임 선택 등)은 받아 둔 이미지로 스크립트만 단독 실행해 몇 초에 본다.
**집필 재생성은 글 내용을 바꿀 때만.**

> 2026-08-01 — 캡처 화질을 잡는다고 초안을 일곱 번 돌렸다. 여섯 번은 필요 없었다.

## 모드를 건드릴 때

- 모드에 관한 사실은 **`src/modes/<id>.js` 한 파일**에만 쓴다.
- 모드마다 정반대인 규칙이 있다(소제목에 작품명: 책 금지 / 영화 3개까지).
  통합하지 말고 `conflicts` 와 `contract` 에 **명시**한다.
- 새 필드를 스키마에 넣으면 `codexWriter.normalizeArticle` 에도 한 줄을 넣는다.
  안 넣으면 **조용히 버려진다** (`doctor` 가 대조한다).
- 단계를 다른 단계의 조건문 **안에** 얹지 않는다 — 새 모드에서 조용히 빠진다.

## 배운 것을 남기는 자리

- 사고와 실측 → `HANDOVER.md` §7-x
- 참고 글 벤치마킹 → `.claude/skills/benchmark-post/references/learned.md`
- 규칙이 **기계적으로 확실하면 코드로** 옮긴다. 산문 규칙은 어겨져도 소리가 안 난다.

> 모델이 세 번 못 하면 그건 코드가 할 일이다 (단조 종결 → `src/endings.js`).
