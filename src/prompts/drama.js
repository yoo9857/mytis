/**
 * 드라마 회차 리캡 지시문 — **이 파일 하나가 드라마 글의 전부다.**
 *
 * ## 왜 영상(clip) 모드로 쓰지 않는가 (§7-15)
 *
 * 드라마 하이라이트 영상을 영상 모드로 뽑았더니 규격은 전부 통과하고 사실이 틀렸다.
 * 자동 자막이 근거였기 때문이다:
 *
 *  ① 하이라이트 영상 **제목은 배우 이름**을 쓴다("안희연네 가족"). 모델은 그것을
 *     극중 인물로 보고 극중 사건을 그 이름에 붙였다 — 실존 인물 오귀속이다.
 *  ② 자막이 잘못 받아쓴 "한소" 를 인물 이름으로 실었다. 출연진에 없는 이름이었다.
 *  ③ 이모(피부과 의사)의 상담실 장면을 엄마(반찬가게 직원)에게 붙였다.
 *     자막만 보면 **누가 말했는지 가릴 수 없다.**
 *
 * 그래서 이 모드는 자막을 근거로 삼지 않는다. **출연진 표와 회차 보도를 취재한다.**
 * 입력이 작품명 + 회차이므로 캡처할 영상 파일도 없다 (clipShots: false).
 *
 * 공용 규칙은 ./shared.js 에서 가져온다. 모드를 가리지 않는 규칙을 여기 복사하면
 * 다른 모드에서 같은 실패가 재발한다.
 *
 * 규격(글자·섹션·사진·표·태그 수)은 여기가 아니라 src/modes/drama.js 의 contract 가
 * 갖는다. 숫자를 바꿀 때는 **두 곳을 함께** 고친다.
 *
 * ⚠️ 이 파일은 템플릿 리터럴이다. 본문에 백틱을 쓰면 파일이 깨진다.
 */
import { todayStr } from '../paths.js';
import { MODE, pickVoice, bodyImageCount } from '../mode.js';
import { readabilityRules, calloutRules, legalDisputeRules, imageBriefRules } from './shared.js';

/** "드라마: 사랑이 온다 4회" → { work: "사랑이 온다", ep: "4" } */
export function parseDramaTopic(topic) {
  const body = String(topic).replace(/^드라마\s*:\s*/, '').trim();
  const m = body.match(/^(.*?)\s*(\d+)\s*(?:회|화|부)\s*$/);
  if (m) return { work: m[1].trim(), ep: m[2] };
  return { work: body, ep: '' };
}

export function buildDramaPrompt({ topic, cfg, today = new Date() }) {
  const a = cfg.article;
  const dateStr = todayStr(today);
  /* 사진 수는 **모드 선언(modes/drama.js: bodyImageDelta)** 이 갖는다.
   * 지시문에서 손으로 더하면 렌더 쪽과 어긋난다 — 책·경제 모드가 그렇게 어긋나 있었다. */
  const bodyImages = bodyImageCount(MODE.DRAMA, cfg);
  const { work, ep } = parseDramaTopic(topic);
  const epLabel = ep ? ep + '회' : '해당 회차';
  /* 목소리는 **작품이** 정한다 — 같은 작품의 회차들이 같은 목소리로 이어져야
   * 시리즈로 읽힌다. 고정은 config.json 의 article.dramaVoice. */
  const voice = pickVoice(MODE.DRAMA, work, cfg);

  return `당신은 드라마를 꾸준히 보는 사람의 블로그에 **회차 리캡**을 쓰고 있습니다.
독자는 이미 그 회차를 봤거나, 놓쳐서 무슨 일이 있었는지 확인하러 온 사람입니다.
그래서 이 글은 감상문이 아니라 **그 회차에서 무엇이 어떻게 바뀌었는지의 기록**입니다.

# 작품과 회차
${work} ${epLabel}

# 오늘 날짜
${dateStr}

${voice ? voice.rules + '\n' : ''}
# 반드시 먼저 할 일 (웹 검색) — 이 순서를 건너뛰면 글을 쓸 수 없습니다

**1. 출연진 표를 찾으세요. 이것이 첫 번째 작업입니다.**
배우 이름과 극중 이름을 **짝지어** 확인하세요. 방송사 공식 홈페이지의 인물 소개,
위키백과, 나무위키 등장인물 문서를 씁니다.

**2. ${epLabel} 리캡 보도를 찾으세요.**
방송 다음 날 나오는 리캡 기사가 그 회차에서 무슨 일이 있었는지 정리해 둡니다.
두 곳 이상에서 교차 확인하세요.

**3. 편성과 방영 정보를 확인하세요.**
방송사 · 방영 요일(일일/주말/월화 등) · 총 몇 부작 · ${epLabel} 방송일 · 방송 시각.
시청률은 닐슨코리아를 인용한 보도에서 확인된 것만 씁니다.

**4. 방송사 공식 채널의 예고·하이라이트 영상을 찾으세요.**
공식 채널이 아닌 영상은 쓰지 않습니다.

주의: 셸 명령으로 URL 을 받아오려 하지 마세요. web_search 도구만 사용하세요.

# 인물 이름 — **이 모드에서 가장 자주 나는 사고입니다**

극중에서 그 일을 한 것은 **극중 인물**입니다. 배우는 그 인물을 연기한 사람입니다.
둘을 섞으면 **실존 인물이 하지 않은 일을 그 사람 이름으로 쓰게 됩니다.**
발행된 글은 고칠 수 없습니다.

> 실패 실측 (2026-08-04): 하이라이트 영상 제목의 "안희연" 을 극중 인물로 보고
> "할머니가 안희연을 며느리로 삼겠다고 하자" 라고 썼습니다. 안희연은 배우이고
> 극중 인물은 한규림입니다. 같은 글에서 출연진에 없는 이름 "한소" 를 인물로 실었고,
> 이모(피부과 의사)의 진료 장면을 엄마(반찬가게 직원)에게 붙였습니다.

- 본문 · 소제목 · keyTakeaways · FAQ · 캡션에서 극중 사건의 주체는 **극중 이름**입니다.
- 배우 이름은 **인물이 처음 나올 때 한 번만** "한규림(안희연 분)" 형태로 밝히고,
  그 뒤에는 극중 이름만 씁니다.
- **1번에서 확인하지 못한 이름은 쓰지 마세요.** 관계로 부르면 됩니다 —
  "한결의 이모", "쌍둥이 남동생", "반찬가게 주인". 이름을 지어내는 것보다 낫습니다.
- **누가 무엇을 했는지 확실하지 않으면 그 대목을 쓰지 마세요.** 인물의 직업·관계가
  1번의 출연진 표와 맞는지 대조하세요 (의사의 진료 장면을 다른 인물에게 붙이지 않도록).
- entities 의 nameKo 는 **극중 이름**, role 은 "극중 인물 (배우 <배우명>)" 입니다.

# 스포일러 — 이 글은 **다 말하는 글입니다**

회차 리캡은 그 회차를 이미 본 사람과, 놓쳐서 결과를 알고 싶은 사람이 읽습니다.
그래서 ${epLabel}의 전개와 결말을 **감추지 않고 씁니다.** 제목에 "스포" 표기를
붙이지 않아도 됩니다 — 회차 리캡이라는 형식 자체가 그것을 알립니다.

단, **다음 회차는 다릅니다.** 아직 방송되지 않은 회차의 내용은 공식 예고에서
확인된 것만 쓰고, "예고에 따르면" 을 붙이세요. 예고에 없는 전개를 추측하지 마세요.

# 제목과 소제목

- 제목: "${work} ${epLabel} 줄거리 결말" 로 시작하고, 뒤에 이 회차의 축을 한 구절 붙입니다.
  독자가 검색창에 치는 말이 앞에 있어야 합니다.
- **소제목마다 작품명과 회차 제목을 넣으세요.** "${work} ${epLabel} 줄거리",
  "${work} ${epLabel} 결말" 처럼 씁니다. 회차 리캡의 검색은 회차 단위로 일어나고,
  독자는 소제목을 검색어로 씁니다.
  (예능·영상 리캡과 다릅니다. 그쪽은 이야기의 마디로 나누지만, 회차 리캡은
   검색어를 소제목에 둡니다.)

# 섹션 구성 (6~8개)

1. **이 회차의 문을 여는 장면** — 가장 크게 바뀐 것 하나로 시작합니다.
2. **방영 정보** — 방송사·편성·회차·방송일·시청률을 **표 하나**로 정리합니다.
   표는 이 섹션에만 씁니다. 확인하지 못한 칸은 만들지 마세요.
3. **줄거리** — 시간 순서대로. 인물의 선택과 그 결과를 씁니다.
4. **결정적 장면** — 이 회차에서 관계를 바꾼 대목 하나를 자세히.
5. **인물 관계의 변화** — 회차 전과 후로 무엇이 달라졌는지.
6. **다음 회 예고** — 공식 예고에서 확인된 것만.
7. **정리** — 이 회차가 작품 전체에서 하는 일.

# 사실과 해석을 섞지 않기

- 화면에서 일어난 일과 글쓴이의 읽기를 **문장 단위로 구분**하세요.
- 해석에는 "저에게는 ~로 보였습니다", "~로 읽힙니다" 처럼 주체를 드러냅니다.
- 시청률·순위·편성은 **출처를 밝힌 것만** 씁니다.
- 배우의 실제 발언은 제작발표회·인터뷰에서 확인된 것만, 큰따옴표로 한두 문장까지.

${readabilityRules()}

${calloutRules()}

${legalDisputeRules()}

# 인물 정보 (entities)
- 이 회차에 나온 **극중 인물**을 넣습니다. 첫 번째 항목이 이 회차의 중심 인물입니다.
- nameKo 는 극중 이름. nameEn 은 확실하지 않으면 빈 문자열로 둡니다.
- role 에 "극중 인물 (배우 <배우명>)" 을 씁니다.
- historical 은 실제 역사 인물을 다룬 사극이 아니면 false 입니다.

${imageBriefRules(a, bodyImages, { mode: MODE.DRAMA, maxSection: 8, noStatCard: true })}
- 방송 스틸을 구할 수 없는 자리는 photoQuery 에 작품의 분위기를 나타내는 영어 장면어를
  쓰세요. 예: "korean street market evening", "hospital corridor daylight"
- **극중 인물의 얼굴 사진은 스톡에서 구할 수 없습니다.** alt·caption 에 특정 인물이
  찍혀 있다고 쓰지 마세요.

# 금지
- 배우 이름 자리에 극중 이름을, 극중 이름 자리에 배우 이름을 쓰기
- 출연진 표에서 확인하지 못한 인물 이름을 쓰기
- 공식 예고에 없는 다음 회차 전개를 추측해서 쓰기
- 확인하지 않은 시청률·부작 수·편성을 쓰기
- 배우의 사생활·열애·불화 추측
- 작품 평점을 단정하기 ("올해 최고의 드라마" 같은 말)

# 출력
JSON 하나만 출력하세요. 스키마를 그대로 지키고, 설명하는 문장을 앞뒤에 붙이지 마세요.`;
}
