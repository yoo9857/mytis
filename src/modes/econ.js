/**
 * 경제 블로그(eco-m) 전용 모드.
 *
 * 티스토리가 두 개다 — classic-m(연예·드라마·명화)과 eco-m(경제).
 * 두 블로그는 목표가 정반대다. 연예 글은 화제성을 타야 하고, 경제 글은
 * **틀리지 않는 것**이 전부다. 그래서 모드를 갈라 놓았다. 지시문은
 * src/prompts/econ.js 한 파일이고, 연예 모드와 파일을 공유하지 않는다.
 *
 * 발행 대상은 티스토리(eco-m)뿐이다 — 네이버(web_dev5)는 책 시리즈용이라
 * 경제 글이 섞이면 그 블로그의 주제가 흐려진다.
 */

/** 목소리 — pickVoice() 가 첫 줄 해시로 고르고, config.article.econVoice 로 고정한다 */
const VOICES = [
  {
    name: '확인하는 실무자',
    rules: `당신의 목소리: **확인하는 실무자** (원문을 열어 보고 말하는).
- 평서문. 단정할 수 있는 것만 단정하고, 나머지는 조건을 붙여 말한다.
- "찾아보니", "고시 원문에는", "조건이 하나 더 있다" 같은 **확인의 흔적**을 남긴다.
- 자신을 전문가로 세우지 않는다. 대신 **어디서 확인했는지**로 신뢰를 만든다.
- 시그니처: 한 섹션에서 **자신이 헷갈렸던 지점**을 밝히고 그것을 풀어 준다
  ("저는 이 대목에서 과세표준과 총급여를 헷갈렸습니다").
- 감정을 쓰지 않는다. 놀라거나 안타까워하지 않는다.`,
  },
  {
    name: '옆자리 선배',
    rules: `당신의 목소리: **옆자리 선배** (물어보면 알려 주는).
- **해요체**로 씁니다 ("~인데요", "~거든요", "~하시면 돼요"). 글 전체가 그렇습니다.
- 1인칭은 **"저"** 입니다. 해요체에 "나는" 이 섞이면 격이 깨집니다.
- 독자에게 직접 말을 겁니다 — 다만 "여러분" 은 금지. 옆 사람에게 말하듯.
- 어려운 말이 나오면 **먼저 알아채고** 풀어 줍니다 ("여기서 과세표준이라는 말이 나오는데요").
- 시그니처: 섹션을 **독자가 할 질문**으로 엽니다 ("그럼 얼마까지 넣을 수 있냐면요").
- 친절하지만 없는 것을 있다고 하지 않습니다. 모르면 모른다고 씁니다.`,
  },
  {
    name: '계산기 두드리는 사람',
    rules: `당신의 목소리: **계산기 두드리는 사람** (숫자로 말하는).
- 평서문. 문장은 짧게 탁탁. 형용사를 아낀다.
- 추상적인 표현을 숫자로 바꾼다 — "꽤 큰 금액" 이 아니라 "99만원".
- 계산을 **끝까지** 보여 준다. 중간 단계를 생략하지 않는다.
- 비유는 생활에서 온다 — 월급날, 관리비, 편의점, 자동이체.
- 시그니처: 섹션 하나를 **가상의 인물 한 명의 계산**으로 끝까지 끌고 간다.
- 마지막에 숫자가 아닌 한마디를 슬쩍 남기는 것이 이 목소리의 끝맺음이다.`,
  },
];

/** "경제: 연금저축 세액공제 한도" → eco-m 경제 글 */
export default {
  id: 'econ', key: 'ECON', label: '경제',
  detect: (t) => /^경제\s*:/.test(t),
  capabilities: {
    /* 원문 기사가 없다 — 주제 한 줄을 받아 기관 원자료를 찾아 쓴다. */
    sourcePhoto: false,
    /* 경제 기사의 사진은 주제와 무관하다 (기자회견장·증권사 객장·아파트 외벽).
     * 그런 사진이 붙으면 독자가 글의 내용이라고 오인한다. */
    relatedArticlePhotos: false,
    clipShots: false,
    /* 유튜브 임베드를 끈다. 경제 유튜브에서 공식 채널을 기계적으로 가려내기 어렵고,
     * 종목 추천·리딩방 영상이 한 번이라도 붙으면 이 블로그가 소개글에서 한 약속이
     * 깨진다. 되돌릴 방법은 글을 지우는 것뿐이다(발행 후 수정 불가). */
    youtubeEmbeds: false,
    /* SNS 임베드도 같은 이유로 끈다 — 경제 인플루언서 게시물이 근거처럼 보인다. */
    socialEmbeds: false,
    /* 표는 경제 글의 핵심 도구다. 한도·세율·구간·요건은 문장보다 표가 정확하다. */
    allowTables: true,
  },
  schemaFile: 'econ.schema.json',
  /**
   * 본문 스톡 사진을 **줄인다** (기본 5 → 4장). 2026-08-04 에 +2 에서 내렸다.
   *
   * 왜: 경제 글의 본문 이미지는 **글자를 얹지 않는 순수 스톡 사진**이다
   * (`images.js` 의 bodyStyle==='photo' 경로). "빈 아파트 방", "노트와 계산기" 는
   * 어떤 경제 글에도 붙을 수 있어 독자에게 아무것도 주지 않는다.
   *
   * > 사용자 지적 (2026-08-04, 두 번째): "카드형 보드 설명으로 전문적이게 보였으면"
   * > 실측 — LH 전세임대 글: 시각 자료 9장 중 **스톡 사진 7장 · 정보 카드 1장**.
   *
   * 그 자리를 `cards`(정보 카드)가 채운다. 사진은 호흡의 쉼표로만 남긴다.
   * ⚠️ 더 내리지 말 것: `photoDensity` 는 브리프 수로 나눈다. 4장이면 4,000자에서
   * 800자/장이고 규격 상한이 1,000 이다 — 3장으로 내리면 긴 글이 규격을 넘는다.
   */
  bodyImageDelta: -1,
  rules: ['readabilityRules', 'calloutRules', 'imageBriefRules'],
  voices: VOICES,
  voicePin: 'econVoice',
  /** 자리 이름이 아니라 **역할**이다. 소제목은 검색어로 따로 짓는다(prompts/econ.js).
   *  앞 넷이 뼈대이고 나머지는 주제에 맞는 것만 고른다. */
  /* 두 갈래다 — **절차가 있는 주제는 단계로, 없는 주제는 질문으로** 쓴다.
   * 참고 글 실측(2026-08-03, hye_life 집 구하기)에서 나온 구분이다:
   * 순서가 있는 일을 개념 설명으로 쓰면 독자가 자기 위치를 모른다 (learned.md). */
  sections: [
    // 절차형 (청약·전월세 계약·대출 신청·연말정산)
    'N단계 · 절차 이름',
    // 해설형 (제도·개념)
    '무엇이 정해져 있나', '누가 해당되나', '얼마까지 되나', '언제까지 해야 하나',
    '계산 예시', '놓치기 쉬운 조건', '자주 하는 오해', '어디서 확인하나',
  ],
  /** 출력 규격 — ⚠️ **발행 실측이 아직 없다.** eco-m 은 2026-08-03 개설이고 글이 0편이다.
   *
   * 그래서 값은 topic 모드를 기준으로 경제 글의 성질만 반영한 **잠정값**이다.
   * 세 편쯤 발행하면 실측으로 조인다 — 그때까지 경고가 잦더라도 규격을 먼저 의심하지 말고
   * 글을 보라. 다만 규격이 틀렸다고 판단하면 **글을 예외 처리하지 말고 여기를 고친다.**
   *
   * 연예 모드와 의도적으로 다르게 둔 것:
   *  - photos 4~7 (연예 5~9) — 경제 글에서 사진은 주인공이 아니라 읽는 호흡의 쉼표다
   *  - tables **하한 1** (연예 0 허용) — 한도·세율·요건은 표 없이 쓰면 독자가 자기 칸을 못 찾는다
   *  - figures 3~10 — 경제 모드에만 있는 항목. 수치마다 출처·기준일을 짝지운다
   */
  contract: {
    chars: [2600, 4000],
    sections: [5, 8],
    /** 4~8 — **첫 실측 반영** (2026-08-03, 전월세 절차 글). 처음엔 4~7로 뒀는데
     *  6단계 글이 대표 1 + 단계마다 1장 = 8장을 냈고 그게 자연스러웠다.
     *  단계 글은 단계 수가 사진 수를 정한다. 억지로 7로 자르면 한 단계가 빈다. */
    photos: [4, 8],
    /* 하한·상한은 chars 와 photos 의 모든 조합이 들어오도록 잡았다.
     * 2600자÷8장=325 · 4000자÷4장=1000. 범위를 좁히면 긴 글이 구조적으로 경고가 된다
     * (책 모드가 그렇게 걸렸다 — modes/book.js 주석 참고).
     * 실측 329자/장 (2026-08-03) — 하한 350 이면 첫 글부터 경고였다. */
    photoDensity: [320, 1000],
    captions: 'free',
    endingMax: 0.6,
    headingWorkTitle: null,   // 작품이 없다 — 소제목에 반복할 제목이 없다
    /** 1~6 — **첫 실측 반영.** 처음엔 1~3이었다. 절차 글은 단계마다 확인 항목 표가
     *  붙어서 6개가 나왔고, 눈으로 보니 각 표가 3~4행으로 작아 읽기를 돕고 있었다.
     *  표는 이 모드의 강점이다 (참고 글은 4,499자에 표 0개 — learned.md 2026-08-03).
     *  하한 1은 유지한다 — 한도·요건을 표 없이 쓰면 독자가 자기 칸을 못 찾는다. */
    tables: [1, 6],
    embeds: [0, 0],           // youtubeEmbeds: false
    tags: [10, 14],
    faq: [3, 6],
    /** 경제 모드 전용 — 본문 수치와 출처·기준일의 짝. 하한 3 은
     *  "숫자를 3개 미만으로 쓸 수 있는 주제는 이 블로그의 글감이 아니다" 는 판단이다.
     *
     *  상한 10 → **16 으로 올렸다** (2026-08-04, 기후동행카드 전환 글 실측 14개).
     *  왜: 상한 10 은 지시문과 **정면으로 부딪힌다.** 지시문은 "본문에 쓴 수치는
     *  하나도 빠짐없이 figures 배열에 올리세요" 이고, 규격이 10개에서 막으면
     *  모델은 둘 중 하나를 어겨야 한다 — 그리고 어기는 쪽은 늘 산문 규칙이다
     *  (§7-2 "지시문끼리 부딪히면 모델은 최소한만 지킨다").
     *
     *  실측: 종료 일정 4개(충전 마감·최종 이용·할인 종료·운영 종료) + 제도 값 10개
     *  (한시 기간·시차 시간·정률 4단·시차 4단·정액 기준 2종·최소 횟수·상한 없음).
     *  제도 전환 글에서 14개는 부풀린 것이 아니라 **그 글이 다루는 숫자 전부**다.
     *  하한 3 은 그대로 둔다 — 그쪽이 이 항목을 둔 이유다.
     */
    figures: [3, 16],
    /**
     * 정보 카드 수 (`article.cards` → infographic.js 가 정사각 이미지로 그린다).
     *
     * 이 항목을 둔 이유: 본문 시각 자료가 **스톡 사진으로만 채워지는 것**을 막는다.
     * 지시문에 "카드를 만들라" 고만 쓰면 모델이 한 개만 만들고 끝낸다.
     * > 2026-08-04 실측 — LH 전세임대 글: 지시문이 카드를 요청했는데 **1개**만 왔고
     * >   본문 시각 자료 8장 중 7장이 스톡 사진이었다.
     *
     * 카드에는 **숫자를 넣지 않는다** — 순서·구조·이유만 (infographic.js 머리말의 원칙:
     * 숫자는 제도가 바뀌면 낡는데 이미지는 발행 후 고칠 수 없다).
     */
    cards: [3, 5],
    noSpoilerIn: [],
  },
  platforms: ['tistory'],
  /** 연예·책·영화 모드와 **정반대**인 규칙 — 통합하면 안 된다 */
  conflicts: {
    /* 이미지에 숫자를 박지 않는다. 기준일이 함께 붙지 않고, 발행 후 이미지를 고칠 수
     * 없어 낡은 숫자가 남는다. 연예 글은 스탯 카드가 유효하다(조회수·순위). */
    statCard: 'forbid',
    /* 경제 모드에만 있는 충돌 축 — 미래를 말하지 않는다.
     * 연예 글은 "다음 회차 관전 포인트" 를 쓰는 것이 정상이고, 경제 글은 그것이 사고다. */
    forecast: 'forbid',
  },
  /** 지시문에 섞이면 안 되는 다른 모드 어휘.
   *  ⚠️ '소속사'·'회차' 는 넣지 않는다 — shared.js 의 공용 규칙 본문에 들어 있어서
   *  넣으면 이 모드가 아니라 공용 규칙을 탓하게 된다(readabilityRules 의 예시는
   *  2026-08-03 에 모드별로 갈라 두었다). */
  foreignWords: ['상영시간', '개봉', '배급사', '시청률', '줄거리', '저자', '출판사', '아이돌', '컴백'],
};
