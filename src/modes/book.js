/** 목소리 — pickVoice() 가 첫 줄 해시로 고르고, config.article.<voicePin> 으로 고정한다 */
const VOICES = [
  {
    name: '정밀한 평론가',
    rules: `당신의 목소리: **정밀한 평론가** (영화 평론가의 문장을 닮은).
- 평서문. 쉼표로 호흡을 만드는 길고 정확한 문장 뒤에, 짧은 단정 하나.
- 위트는 절제해서 한 편에 두 번쯤 — 냉소가 아니라 애정이 있는 위트.
- 관찰은 반드시 구체적 근거(장면·문장·수치) 위에서. 감탄사 금지.
- 시그니처: **에필로그를 "한 줄로 줄이면 —" 로 시작하는 한 줄 평**으로 닫는다.
- 감상("생각" 섹션)의 결: 자료에서 발견한 디테일 하나를 확대해 그 책 전체를 읽는다.`,
  },
  {
    name: '라디오 북튜버',
    rules: `당신의 목소리: **라디오 북튜버** (조곤조곤 말을 거는).
- **해요체**로 씁니다 ("~인데요", "~거든요", "~잖아요"). 이 글 전체가 그렇습니다.
- 1인칭은 **"저"** 입니다 — 해요체에 "나는" 이 섞이면 격이 깨집니다 (세 번 재발한 실수).
- 독자에게 직접 말을 겁니다 — 다만 "여러분" 은 금지, 그냥 옆 사람에게 말하듯.
- 자기 책상·이어폰·퇴근길 같은 일상의 소품이 배경으로 스칩니다 (한두 번만).
- 시그니처: 프롤로그를 **자신의 작은 장면**으로 엽니다 ("어젯밤에 예고편을 두 번 봤어요").
  책 내용의 경험은 지어내면 안 되지만, 책을 만나러 가는 일상의 소묘는 이 글의 관습입니다.
- 감상의 결: 궁금함과 설렘을 숨기지 않되, 근거 없는 확신은 하지 않습니다.`,
  },
  {
    name: '마감 노동자 소설가',
    rules: `당신의 목소리: **마감 노동자 소설가** (건조한 직설, 생활의 비유).
- 평서문. 문장은 짧게 탁탁. 접속사를 아낀다.
- 비유는 우주나 문학이 아니라 **생활**에서 온다 — 출근길, 엑셀, 편의점, 계좌이체.
- 숫자를 좋아한다. 수치 하나를 물고 늘어져 이야기로 만든다.
- 냉정한 척하다가 마지막에 슬쩍 무너지는 것이 시그니처 — 에필로그의 마지막 문장은
  체면을 버린 솔직한 한마디.
- 감상의 결: "이 책이 왜 팔리는가" 를 직업인의 눈으로 뜯어보다가 결국 독자로 돌아온다.`,
  },
];

/** "책: 제목 — 저자" → '오늘 뭐 읽지?' 책 추천 (네이버 시리즈) */
export default {
  id: 'book', key: 'BOOK', label: '책',
  detect: (t) => /^책\s*:/.test(t),
  capabilities: {
    /* 처음에는 사진·SNS 를 다 껐다가 되켰다 (2026-07-29 사용자 결정).
     * 작가의 얼굴·강연·사인회 사진은 서평·부고·인터뷰 기사에 있고,
     * 보도사진 위험은 연예 글과 같은 기준으로 발행자가 감수한다. */
    sourcePhoto: true,
    relatedArticlePhotos: true,
    clipShots: false,
    /* 북트레일러가 있는 책도 있지만 공식 채널 판별이 어렵고 없는 책이 대부분이다. */
    youtubeEmbeds: false,
    socialEmbeds: true,           // 저자·출판사 공식 게시물 임베드
    allowTables: true,            // 서지 정보가 표의 정석
  },
  schemaFile: 'book.schema.json',
  /* 지시문은 +4(9개)를 요청하는데 codexWriter 가 심는 값은 **+2** 였다 — 주석까지
   * "프롬프트(bodyImages+2)" 라고 적혀 있었다. 지시문이 나중에 +4 로 바뀌었고
   * 그 줄은 따라가지 않았다. 브리프 2개가 잘려 뒤쪽 절의 사진이 사라졌다.
   * 10장이면 2850~3600자가 285~360자/장으로 규격 [260,420] 안이다. */
  bodyImageDelta: 4,
  rules: ['readabilityRules', 'calloutRules', 'imageBriefRules'],
  voices: VOICES,
  voicePin: 'bookVoice',
  /** 자리 이름이 아니라 **역할**이다. 소제목으로 그대로 쓰지 않는다 —
   *  소제목은 검색어로 따로 짓는다(prompts/book.js).
   *  앞 넷이 뼈대이고 나머지는 책에 맞는 것만 고른다. */
  sections: ['책소개', '줄거리', '감상평', '독자가 찾는 것', '서지 정보', '저자', '책 속 문장'],
  /** 출력 규격 — 실측(2026-08-01, 발행 3편) + 2026-08-03 검색 누락 사고 반영.
   *
   * ⚠️ **하한과 상한을 같게 두지 않는다.** 그러면 규격이 아니라 틀이 되고,
   * 모든 글이 같은 모양으로 찍혀 나온다.
   * > 2026-08-03 실측: 책 글 9건 중 8건의 소제목 구조가 완전히 동일했고
   * > (프롤로그>목차>줄거리>생각>기본 정보>결말>포인트), FAQ 는 9건 전부 5개,
   * > 표는 9건 전부 1개였다. 같은 기간 07-30 이후 발행분 10건이 네이버 검색에서
   * > 통째로 누락됐다. 인과는 확정할 수 없지만 우리가 통제할 수 있는 변수는 이것이다.
   */
  contract: {
    chars: [2850, 3600],
    sections: [6, 9],             // 고정 7 → 범위. 책마다 다른 모양이 나와야 한다
    photos: [6, 10],
    photoDensity: [260, 420],     // 참고 글 실측 249~330. 사진 공급이 병목이다
    captions: 'free',             // 표지·저자·행사 사진은 무엇인지 밝히는 편이 낫다
    endingMax: 0.6,
    /** 소제목에 책 제목을 **1~3회** 넣는다.
     *  0 이면 소제목에 검색어가 하나도 없고(프롤로그·목차·결말…), 7 이면 같은 이름이
     *  화면에 일곱 번 찍힌다(영화 모드 실측). 3 이 상한인 이유가 그것이다. */
    headingWorkTitle: [1, 3],
    tables: [0, 2],               // 서지 표가 어울리지 않는 책도 있다
    embeds: [0, 0],
    tags: [10, 16],               // 8 이면 8에서 멈춘다 (영화 모드 실측) — 하한을 올린다
    faq: [3, 6],
    noSpoilerIn: ['directAnswer', 'keyTakeaways'],
  },
  platforms: ['naver'],
  /** 영화 모드와 **정반대**인 규칙 — 통합하면 안 된다 */
  conflicts: {
    titleInHeading: 'limit',      // 1~3회만 (영화는 require, 예전 책 모드는 forbid 였다)
    spoiler: 'never',             // 결말을 말하지 않는다 (영화는 선택)
    statCard: 'forbid',
  },
  /** 지시문에 섞이면 안 되는 다른 모드 어휘 */
  foreignWords: ['상영시간', '개봉연도', '배급사'],
};
