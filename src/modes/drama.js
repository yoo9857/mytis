/**
 * "드라마: 제목 N회" → 회차 리캡 (사용자 결정 2026-08-01 · 켠 날 2026-08-04)
 *
 * ## 영상(clip) 모드와 무엇이 다른가
 * 영상 모드는 입력이 **유튜브 URL** 이고 자막에서 장면을 뽑는다.
 * 드라마 모드는 입력이 **작품명 + 회차** 라 자막이 없다 — 방영 정보·보도·
 * 공식 예고를 취재해야 한다. 그래서 clipShots 를 쓸 수 없다.
 *
 * ## 왜 이 모드가 필요했나 (§7-15)
 * 드라마 하이라이트 영상을 영상 모드로 뽑았더니 **규격은 전부 통과하고 사실이 틀렸다.**
 * 자동 자막이 근거였기 때문이다 — 영상 제목의 배우 이름을 극중 인물로 쓰고,
 * 자막이 잘못 받아쓴 없는 이름을 실었고, 이모의 장면을 엄마에게 붙였다.
 * **자막으로는 누가 말했는지 가릴 수 없다.** 이 모드는 출연진 표를 취재한다.
 */
const VOICES = [
  {
    name: '본방 사수하는 사람',
    rules: `당신의 목소리: **본방 사수하는 사람** (매회 챙겨 보는 시청자).
- **해요체**입니다 ("~거든요", "~더라고요", "~죠"). 글 전체가 그렇습니다.
- 1인칭은 **"저"** 입니다 — "나는" 과 섞지 마세요.
- 회차를 이어 봅니다 — 지난 회에서 깔린 것이 이번 회에 어떻게 회수됐는지 짚습니다.
- 시그니처: 정리 섹션을 **다음 회에서 확인할 것 한 줄**로 닫습니다.`,
  },
  {
    name: '관계도 그리는 사람',
    rules: `당신의 목소리: **관계도 그리는 사람** (인물 사이의 선을 따라가는 눈).
- 경어체 평서 위주. 건조하지만 차갑지 않게.
- 인물의 **행동과 그 결과**를 봅니다 — 감정을 짐작하지 않고 선택을 적습니다.
- 회차 전과 후로 관계가 어디서 어디로 옮겼는지를 글의 축으로 세웁니다.
- 시그니처: 결정적 장면 하나를 골라 "이 대목 전에는 ~였습니다" 로 대비합니다.`,
  },
  {
    name: '작법 뜯어보는 사람',
    rules: `당신의 목소리: **작법 뜯어보는 사람** (구성을 읽는 눈).
- 복선·배치·회차 끊는 지점을 봅니다 — "왜 여기서 끊었나", "무엇을 아꼈나".
- 경어체 평서, 건조한 위트. 감탄 대신 관찰의 정확함으로 재미를 만듭니다.
- 회차 번호·방송일·부작 수 같은 숫자를 근거로 삼는 것을 좋아합니다.
- 시그니처: 이 회차가 작품 전체에서 하는 일을 한 줄로 못 박습니다.`,
  },
];

export default {
  id: 'drama', key: 'DRAMA', label: '드라마',
  detect: (t) => /^드라마\s*:/.test(t),
  capabilities: {
    sourcePhoto: true,            // 방송사 공식 스틸·보도자료 (발행자 감수)
    relatedArticlePhotos: true,
    clipShots: false,             // 입력이 URL 이 아니라 캡처 경로가 없다
    youtubeEmbeds: true,          // 공식 예고·하이라이트가 장면을 보여 준다
    /* ⚠️ **false.** 처음 선언은 true 였다 — "방송사·배우 공식 계정 근황이 회차 글과
     * 맞는다" 고 봤다. 그런데 이 모드는 `entities` 에 **극중 이름**을 담는다(그것이
     * 이 모드의 존재 이유다). 근황 검색은 entities 를 그대로 쓰므로 "한규림 공식
     * 인스타그램" 을 찾게 된다.
     *
     * > 2026-08-04 첫 초안 실측: 그 검색이 @ahnanihh 를 물어 왔다. 극중 인물을
     * > 연기한 배우의 계정으로 **결과적으로는 맞았다.** 그러나 극중 이름에서
     * > 공식 계정으로 가는 근거가 코드에 없다 — 맞은 것은 모델이 배우를 알고
     * > 있었기 때문이고, 모르는 작품에서는 **남의 계정이 붙는다.**
     * > 발행된 글은 고칠 수 없다(§6). 한 번 맞은 것을 신뢰로 보지 않는다.
     *
     * 켜려면 먼저 배우 이름을 별도 필드로 받아 그것으로 검색해야 한다. */
    socialEmbeds: false,
    allowTables: true,            // 편성·회차·시청률
  },
  /** `node scripts/gen-schema.mjs drama` 가 만든다 — 손으로 고치지 말 것 (§7-5 ②). */
  schemaFile: 'drama.schema.json',
  /* 대표 1 + 본문 7 = 8장. 회차 리캡은 장면을 보여 주는 글이라 기사보다 사진이 많다.
   * 입력에 영상이 없어 캡처는 쓸 수 없고(clipShots: false) 공급은 보도 사진과
   * 스톡이다 — 공급이 모자라면 그라디언트가 늘어난다. 첫 실측에서 확인할 것. */
  bodyImageDelta: 2,
  rules: ['readabilityRules', 'calloutRules', 'legalDisputeRules', 'imageBriefRules'],
  voices: VOICES,
  voicePin: 'dramaVoice',
  sections: ['프롤로그', '방영 정보', '줄거리', '이 회차의 장면', '등장인물', '다음 회 예고', '포인트'],
  /**
   * 출력 규격 — **아직 실측이 없다 (발행 0편).**
   *
   * 그래서 값은 목표치가 아니라 **파이프라인이 실제로 낼 수 있는 범위**로 뒀다.
   * 목표치로 조여 두면 새 모드가 매번 막히고, 막히는 것이 일상이 되면 `--force`
   * 습관만 생긴다 (영화 모드 photos 주석과 같은 이유). 기사 모드는 반대로 갔다가
   * 실측 주석과 범위가 어긋난 채 굳었다 — 게이트가 매번 우는 상태가 됐다(§8-9).
   *
   * **첫 발행 뒤 실측으로 조인다.** 그때 이 주석을 실측값으로 바꾼다.
   */
  contract: {
    /* 섹션 6~8 · 회차 리캡. 영상 모드 실측(2650~4300)을 출발점으로 삼았다.
     * 하한은 첫 초안 실측 **2640** 에 맞춰 2800 에서 내렸다 (2026-08-04, 표본 1편) —
     * 규격을 목표치로 두면 게이트가 매번 우는 상태가 된다(§8-9 가 그 자리였다).
     * 표본이 늘면 다시 조인다. */
    chars: [2600, 4300],
    sections: [6, 8],
    /* 대표 1 + 본문 7 = 8장 기준 (지시문이 cfg.images.bodyImages + 2 를 쓴다).
     * photos 는 **막음**이라 실제 생산량과 어긋나면 아무 글도 통과하지 못한다. */
    photos: [6, 10],
    /* chars ÷ photos 를 그대로 잰 범위다 (2800/8=350 · 4300/8=538).
     * 참고 글 실측은 249자/장이고 여기는 멀다 — 좁히려면 **먼저 사진 공급을 늘려야**
     * 한다. 범위만 조이면 빈 그라디언트 카드가 생기고 그게 더 나쁘다. */
    photoDensity: [300, 560],
    captions: 'fact-only',        // 방송 화면을 보지 않고 쓰는 글이다 — 근거 없는 캡션 금지
    endingMax: 0.6,
    /* 소제목마다 "작품명 N회" 를 넣게 지시한다 — 회차 리캡의 검색어다.
     * 하한을 3 으로 둔 것은 방영 정보·다음 회 예고 소제목이 형태가 다를 수 있어서다. */
    headingWorkTitle: [3, 8],
    tables: [1, 1],               // 방영 정보 표 하나 (경고 항목이라 막지는 않는다)
    embeds: [1, 3],               // 방송사 공식 예고·하이라이트
    tags: [10, 12],
    faq: [5, 5],
    /* 회차 리캡은 **그 회차를 다 말하는 글**이다. directAnswer 에 결과를 쓰게
     * 지시하므로 스포일러 금지 상자를 두지 않는다 (영화 모드와 정반대). */
    noSpoilerIn: [],
    /**
     * **그 회차를 실제로 취재했는가** — 방송일 당일·이후 출처 최소 1건. `막음`.
     *
     * 이 모드의 전제는 "자막이 아니라 취재" 다. 그 전제가 지켜졌는지 기계가 셀 수
     * 있는 유일한 지점이 여기다. 못 찾았는데도 글은 나온다 — 형식은 완벽하고
     * 내용만 다른 회차다.
     *
     * > 2026-08-04 실측 — 「사랑이 온다」 4회(8/2 방송) 초안이 **규격을 전부
     * > 통과했다.** 실제 4회는 무진의 공사장 사고 · 한석중의 독촉장 · 8년 후
     * > 8살 아들 한결 · 경찰서 재회였는데, 초안은 "규림의 거짓말 뒤에 무진이
     * > 남겠다고 말한다" 를 썼고 **한결이 한 번도 나오지 않았다.**
     * > sources 6건 중 방송일 이후 기사는 **0건** — 방영 전 소개, 첫 방송 기사,
     * > 7/28 제작발표회, 프로그램·VOD 페이지가 전부였다.
     *
     * 경고로 두면 사람이 넘겨 읽는다. 그래서 `막음` 이다(contract.js: BLOCKING).
     */
    sourcesAfterAirDate: 1,
  },
  platforms: ['tistory', 'naver'],
  conflicts: {
    titleInHeading: 'require',    // "○○ 12회 줄거리" 가 검색어다
    spoiler: 'always',            // 회차 리캡은 그 회차를 다 말하는 글이다
    statCard: 'forbid',
  },
  foreignWords: ['서지 정보', '옮긴이', '쪽수', '상영시간'],
};
