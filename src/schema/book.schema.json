{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "title",
    "seoTitle",
    "metaDescription",
    "primaryKeyword",
    "urlSlug",
    "secondaryKeywords",
    "tags",
    "directAnswer",
    "keyTakeaways",
    "sections",
    "faq",
    "conclusion",
    "sources",
    "entities",
    "embeds",
    "imageBriefs"
  ],
  "properties": {
    "title": {
      "type": "string",
      "description": "글 제목. \"책제목, 글의 각도를 말하는 구절\" 형태. 32자 내외. 예: \"투명한 나선, 30년 만에 처음 열린 유카와의 과거\". 스펙(쪽수·가격) 금지."
    },
    "seoTitle": {
      "type": "string",
      "description": "검색엔진용 제목. 60자 이내. 핵심 키워드 + 연도 또는 구체 수치 포함."
    },
    "metaDescription": {
      "type": "string",
      "description": "검색 결과 스니펫용 요약. 90~150자. 이 글이 답하는 질문과 결론을 압축해서 담는다."
    },
    "primaryKeyword": {
      "type": "string",
      "description": "이 글이 노리는 메인 검색 키워드 하나."
    },
    "urlSlug": {
      "type": "string",
      "description": "글 주소에 쓸 영문 슬러그. 소문자 영문·숫자·하이픈만. 3~6단어, 60자 이내. 한글을 쓰면 URL이 퍼센트 인코딩되어 SEO에 불리하므로 반드시 영문으로. 예: 'lim-young-woong-brand-reputation-july-2026'"
    },
    "entities": {
      "type": "array",
      "description": "이 책의 저자(공저자·역자 포함 가능). nameEn 은 위키백과·위키미디어 공용에 실제 등재된 로마자 표기 (예: \"Keigo Higashino\"). 이 표기로 저자 실물 사진을 찾으므로 반드시 채운다. 확실하지 않을 때만 nameEn 을 빈 문자열로.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "nameKo",
          "nameEn",
          "role",
          "historical"
        ],
        "properties": {
          "nameKo": {
            "type": "string",
            "description": "한국어 정식 표기. 예: 임영웅"
          },
          "nameEn": {
            "type": "string",
            "description": "영문 표기. 위키백과·위키미디어 공용에서 쓰는 로마자 표기를 그대로 쓸 것. 예: Lim Young-woong, IU, BTS. 모르면 빈 문자열."
          },
          "role": {
            "type": "string",
            "description": "가수 / 배우 / 그룹 / 프로그램 등 짧은 분류."
          },
          "historical": {
            "type": "boolean",
            "description": "역사 인물이거나 이미 고인이라 공식 SNS 계정이 존재할 수 없으면 true. 세네카·플라톤처럼 오래전 사람은 반드시 true. 생존 여부를 모르면 false."
          }
        }
      }
    },
    "secondaryKeywords": {
      "type": "array",
      "description": "본문에 자연스럽게 녹일 보조 키워드 및 롱테일 질의 5~10개.",
      "items": {
        "type": "string"
      }
    },
    "tags": {
      "type": "array",
      "description": "티스토리 태그. 각 태그는 공백 없이 또는 짧은 단어 조합으로. 쉼표를 포함하지 않는다.",
      "items": {
        "type": "string"
      }
    },
    "directAnswer": {
      "type": "string",
      "description": "이 책이 무엇이고 왜 오늘 이 책인지 2~3문장. 에세이 톤과 별개로 명확하게 — 검색과 AI가 인용하는 자리다."
    },
    "keyTakeaways": {
      "type": "array",
      "description": "GEO(생성형 검색)용 사실 요약 4~6개 — 본문에는 실리지 않는다. 서지·순위·출간 사실을 단독 인용 가능한 완결 문장으로.",
      "items": {
        "type": "string"
      }
    },
    "sections": {
      "type": "array",
      "description": "본문 섹션 목록. 각 섹션은 h2 하나에 해당한다.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "heading",
          "paragraphs",
          "bullets",
          "table",
          "callout"
        ],
        "properties": {
          "heading": {
            "type": "string",
            "description": "섹션 이름. 반드시 이 순서: \"프롤로그\" \"목차\" \"줄거리\" \"생각\" \"기본 정보\" \"결말\" \"포인트\". 뒤에 \" — 구절\"을 덧붙일 때 구절은 평서형·명사형으로 끝낸다. 의문형 어미(~인가, ~나, ~까, ~하나) 절대 금지. 좋은 예: \"줄거리 — 결말 직전까지만\", \"생각 — 30년 만에 열린 과거\". 나쁜 예: \"줄거리 — 시신은 왜 유카와를 가리키나\". 구절에 **책 제목·저자명을 반복하지 않는다** — 소제목은 이미 이 책의 글 안에 있다. 나쁜 예: \"프롤로그 — 유래혁 수족관은 어떤 책인가\"(의문형이면서 제목 반복), \"생각 — 유래혁 수족관의 바다\". 순위·날짜·퍼센트도 구절에 넣지 않는다(나쁜 예: \"결말 — 2026년 7월 23~29일 다시 찾은 독자\")."
          },
          "paragraphs": {
            "type": "array",
            "description": "에세이 문단 2~4개. 문체와 어미는 지시문의 '당신의 목소리'를 따른다 — 여기서 문체를 정하지 않는다(해요체 페르소나에 평론가 문체를 섞으면 격이 깨진다). 공통 규칙: \"~이다. ~다.\" 단문 연타 금지, 문단마다 문장 수를 달리한다(2문장 문단만 이어지면 기계처럼 읽힌다), 긴 문장 뒤에 짧은 문장 하나. 관찰→해석→판단으로 흐르고, 앞 섹션의 마지막 문장을 받아 이어진다. 정보는 문장 속에 감춘다.",
            "items": {
              "type": "string"
            }
          },
          "bullets": {
            "type": "array",
            "description": "이 섹션의 불릿 목록. 필요 없으면 빈 배열.",
            "items": {
              "type": "string"
            }
          },
          "table": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "caption",
              "headers",
              "rows"
            ],
            "description": "\"기본 정보\" 섹션에만 넣는 서지 표. headers 는 [\"항목\",\"내용\"]. 행은 제목/저자(역자)/출판사/출간일/쪽수/분야/정가 — 각 행의 첫 칸이 항목 이름이다. 시리즈 연혁이나 순위 비교 표가 아니다. 다른 섹션의 table 은 빈 값.",
            "properties": {
              "caption": {
                "type": "string"
              },
              "headers": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "rows": {
                "type": "array",
                "items": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "description": "서지 행. 예: [[\"제목\",\"투명한 나선\"],[\"저자\",\"히가시노 게이고 (김선영 옮김)\"],[\"출판사\",\"북다\"],[\"출간일\",\"2026년 7월 22일\"],[\"쪽수\",\"412쪽\"],[\"정가\",\"22,000원\"]]"
              }
            }
          },
          "callout": {
            "type": "string",
            "description": "이미 공개된 책 속 문장 한 줄 (출판사 소개·언론 서평·공식 미리보기에서 확인한 것만). 당신의 조언이나 팁이 아니다. 확인된 문장이 없으면 반드시 빈 문자열."
          }
        }
      }
    },
    "faq": {
      "type": "array",
      "description": "자주 묻는 질문. 실제 검색 질의처럼 쓰고, 답변은 2~4문장으로 완결한다.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "question",
          "answer"
        ],
        "properties": {
          "question": {
            "type": "string"
          },
          "answer": {
            "type": "string"
          }
        }
      }
    },
    "conclusion": {
      "type": "string",
      "description": "에필로그. 글을 접는 두세 문장 — 요약 반복이 아니라 여운. 문체는 지시문의 '당신의 목소리'를 따르고, 그 목소리의 시그니처가 있으면 여기서 쓴다. \"~이다\" 연타 금지."
    },
    "sources": {
      "type": "array",
      "description": "웹 검색으로 실제 확인한 출처만 넣는다. 확인하지 못했으면 빈 배열. URL을 지어내지 않는다.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "title",
          "url",
          "publisher",
          "date"
        ],
        "properties": {
          "title": {
            "type": "string"
          },
          "url": {
            "type": "string"
          },
          "publisher": {
            "type": "string"
          },
          "date": {
            "type": "string",
            "description": "YYYY-MM-DD 또는 YYYY-MM. 모르면 빈 문자열."
          }
        }
      }
    },
    "embeds": {
      "type": "array",
      "description": "본문에 넣을 공식 유튜브 임베드. 임베드는 원저작자 서버에서 재생되므로 저작권 문제가 없다. 적절한 영상이 없으면 빈 배열.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "videoId",
          "title",
          "channel",
          "afterSection",
          "startSeconds",
          "quote",
          "caption",
          "speaker",
          "isStudio",
          "isHook"
        ],
        "properties": {
          "speaker": {
            "type": "string",
            "description": "이 장면에서 말하고 있는 사람의 이름(방송에 나오는 이름 그대로). 이 장면을 캡처하면 화면에 이 사람이 나올 가능성이 높으므로, 글의 주인공이 실제로 보이는 장면을 고르는 데 쓴다. 자막 맥락으로 판단이 안 되면 빈 문자열."
          },
          "isStudio": {
            "type": "boolean",
            "description": "스튜디오 MC·패널이 상황을 논평하는 장면이면 true. 데이트·대화 현장이면 false. 스튜디오 컷은 출연자가 화면에 없어 캡처 대상으로 쓸 수 없다. 확실하지 않으면 false."
          },
          "isHook": {
            "type": "boolean",
            "description": "이 장면이 **대표 이미지로 쓸 만한가**. 감정이 터지는 순간(울음·분노·충격·폭소), 표정이 크게 드러나는 클로즈업, 두 사람이 정면으로 부딪히는 대목이면 true. 인물이 걸어오거나 배경만 보이거나 담담히 설명하는 장면은 false. 이 표시가 붙은 장면 중에서 얼굴이 가장 크게 잡힌 것을 대표 이미지로 씁니다. 판단이 안 되면 false."
          },
          "videoId": {
            "type": "string",
            "description": "유튜브 영상 ID 11자리. watch?v= 뒤의 값. 전체 URL이 아니라 ID만. 검색으로 실제 확인한 것만 넣고 절대 추측하지 말 것."
          },
          "title": {
            "type": "string",
            "description": "영상 제목."
          },
          "channel": {
            "type": "string",
            "description": "채널명. 반드시 공식 채널이어야 한다(소속사·방송사·아티스트 공식). 팬 채널이나 재업로드 채널은 금지."
          },
          "afterSection": {
            "type": "integer",
            "description": "몇 번째 섹션 뒤에 넣을지 (1부터 시작)."
          },
          "startSeconds": {
            "type": "integer",
            "description": "재생을 시작할 지점(초). **제공된 자막에 실제로 있는 시각만** 쓸 것. 지어내면 엉뚱한 장면이 재생된다. 지정할 근거가 없으면 0."
          },
          "quote": {
            "type": "string",
            "description": "그 지점의 실제 대사 한두 줄. **자막에 있는 말만** 옮길 것. 요약·창작 금지. 없으면 빈 문자열."
          },
          "caption": {
            "type": "string",
            "description": "이 장면이 왜 볼 만한지 한 줄 설명. 낚시 금지. 없으면 빈 문자열."
          }
        }
      }
    },
    "imageBriefs": {
      "type": "array",
      "description": "생성할 이미지 카드 설명. 첫 항목은 반드시 placement가 thumbnail 인 대표 이미지.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "placement",
          "headline",
          "subline",
          "caption",
          "alt",
          "afterSection",
          "photoQuery",
          "eyebrow",
          "statValue",
          "statLabel",
          "noText"
        ],
        "properties": {
          "placement": {
            "type": "string",
            "enum": [
              "thumbnail",
              "body"
            ],
            "description": "thumbnail은 대표 이미지, body는 본문 삽입 이미지."
          },
          "photoQuery": {
            "type": "string",
            "description": "이 이미지의 배경으로 쓸 실사 사진을 스톡 사진 사이트에서 검색할 영어 키워드 2~4단어. 한국어 금지. 추상 개념이 아니라 카메라로 찍을 수 있는 구체적 장면이어야 한다. 예: 'young woman using banking app', 'korean apartment building exterior', 'calculator receipts desk'."
          },
          "headline": {
            "type": "string",
            "description": "이미지 위에 얹히는 문구. 12자 이내. 이 글의 각도를 한 조각으로 보여 주는 조용한 말 — 순위·퍼센트·계단 같은 뉴스 어법은 금지. **표지 사진을 쓰는 브리프는 headline 을 빈 문자열로 두고 noText 를 true 로 한다** (표지에는 제목·저자명·원제가 이미 인쇄돼 있어 글자가 겹친다)."
          },
          "subline": {
            "type": "string",
            "description": "헤드라인 아래 보조 문구. 30자 이내."
          },
          "eyebrow": {
            "type": "string",
            "description": "헤드라인 위에 작게 들어갈 분류 라벨. 2~8자. 예: '가입 조건', '핵심 정리', '신청 방법'. 이미지마다 다르게."
          },
          "statValue": {
            "type": "string",
            "description": "**책 글에서는 항상 빈 문자열.** 수치를 얹은 그라디언트 스탯 카드는 책 에세이 톤과 맞지 않는다 — 독자 피드백(2026-07-29): \"배경이나 관련 이미지가 아니니 보기 좋지 않음\". 순위·쪽수·연도는 본문과 서지 표가 이미 말한다."
          },
          "statLabel": {
            "type": "string",
            "description": "**책 글에서는 항상 빈 문자열.** statValue 와 같은 이유."
          },
          "caption": {
            "type": "string",
            "description": "이미지 아래 캡션. 본문 이미지에만 사용."
          },
          "alt": {
            "type": "string",
            "description": "SEO용 alt 텍스트. 키워드를 포함한 서술형 문장."
          },
          "afterSection": {
            "type": "integer",
            "description": "body 이미지를 몇 번째 섹션 뒤에 넣을지 (1부터 시작). thumbnail 이면 0."
          },
          "noText": {
            "type": "boolean",
            "description": "사진 위에 글자를 얹지 않는다. 표지·뒤표지·펼침컷처럼 **이미 글자가 인쇄된 사진**과 우리가 만든 카드에는 true. 그 밖에는 false. (구조화 출력이 모든 필드를 요구하므로 항상 명시한다 — 생략하면 스키마 400 이 난다.)"
          }
        }
      }
    }
  }
}
